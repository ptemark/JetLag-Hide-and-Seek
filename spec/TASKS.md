# TASKS

Track implementation progress for the Jet Lag project. Each task is one RALPH loop iteration.
See `RALPH.md` for the loop process and `DESIGN.md` for all design decisions.

---

## Current Task

[x] **169** — Expand Overpass transit query to include ferry terminals and tram stops: `RULES.md §Game Scales` line 43 states "Include transit systems: trains, buses, ferries, trams". The `buildOverpassQuery` function in `functions/zones.js` fetches `public_transport=stop_position`, `railway=station`, `railway=halt`, and `amenity=bus_station` — but is missing `amenity=ferry_terminal` (ferries) and `railway=tram_stop` (trams). In cities with ferry routes (London, Helsinki, Sydney) or tram networks (Amsterdam, Melbourne, Lisbon), valid hiding zones around those stops are excluded entirely. Changes needed: (1) `functions/zones.js` — in `buildOverpassQuery`, add two new node clauses: `node["amenity"="ferry_terminal"](${bbox});` and `node["railway"="tram_stop"](${bbox});`; update the JSDoc comment to list all six transit types. (2) `functions/zones.test.js` — add a test that captures the raw Overpass query string sent to the mock fetch and asserts all six node tag conditions are present; add two tests confirming that ferry terminal nodes and tram stop nodes in the Overpass response are included in the returned zones array.

[x] **167** — Hider in-zone status indicator during seeking phase: `RULES.md §Hiding Rules` rule 3 states "At end of hiding period, Hider must remain within their hiding zone." The managed server enforces capture proximity, but the hider has no real-time feedback about whether their GPS position is inside or outside their zone boundary during seeking. A hider who inadvertently drifts a few metres beyond the 500 m / 1 km radius will see the zone circle on the map, but has no explicit textual status to confirm they are compliant. Adding an in-zone status banner derived from the local GPS fix (already polled every 10–20 s) provides real-time awareness without extra server calls. Changes needed: (1) `src/components/gameUtils.js` — add and export `haversineDistanceM(a, b)` that takes two `{ lat, lon }` objects and returns the distance in metres; delegates to `haversineKm(a, b) * 1000` to avoid duplicating the Haversine formula; add JSDoc comment citing `RULES.md §Hiding Rules` rule 3 and `§Definitions` (Hiding Zone). (2) `src/components/GameMap.jsx` — add `myLocation` state (`useState(null)`) to store the hider's own latest GPS fix; in the GPS success callback, after sending the WS message, call `setMyLocation({ lat: pos.coords.latitude, lon: pos.coords.longitude })`; add `haversineDistanceM` to the existing import from `./gameUtils.js`; derive `const inZone = (phase === 'seeking' && player.role === 'hider' && myLocation && lockedZone) ? haversineDistanceM(myLocation, { lat: lockedZone.lat, lon: lockedZone.lon }) <= (lockedZone.radiusM ?? 500) : null` — `null` means "not applicable, do not render banner"; after the elapsed-timer banner add `{inZone !== null && (<p role="status" data-testid="zone-status" className={styles.zoneStatus + ' ' + (inZone ? styles.zoneInside : styles.zoneOutside)}>{inZone ? '✓ In zone' : '⚠ Outside zone'}</p>)}`. (3) `src/components/GameMap.module.css` — add `.zoneStatus` (`text-align: center; font-family: 'JetBrains Mono', monospace; font-size: 0.875rem; padding: 0.15rem 0; margin: 0`); `.zoneInside` (`color: var(--color-success)`); `.zoneOutside` (`color: var(--color-error); font-weight: 500`). Tests: `src/components/gameUtils.test.js` — (a) `haversineDistanceM` for identical points returns < 1 m; (b) for a point 500 m north (lat delta ≈ 0.004504°) returns a value within 1% of 500; (c) for 1° latitude separation returns a value within 1% of 111 000 m; `src/components/GameMap.test.jsx` — (d) during seeking phase with hider GPS inside `lockedZone.radiusM`, `'✓ In zone'` text appears in the DOM; (e) during seeking phase with GPS outside radius, `'⚠ Outside zone'` text appears; (f) seeker never sees the zone-status banner; (g) during hiding phase, banner is absent even when `lockedZone` is set; (h) before any GPS fix (`myLocation` null), banner is absent.

[x] **165** — Live hider elapsed-time display: during the hiding and seeking phases the hider has no indication of how long they have been hiding or what their current score is. `hidingStartedAtRef` in `GameMap.jsx` captures the client-side timestamp when the hiding phase begins but its value is only used at game end to compute the final score. The existing `setTimerTick` interval already causes a re-render every second, so elapsed time can be derived inline without a new interval. Changes needed: (1) `src/components/gameUtils.js` — add and export `formatDuration(ms)` that formats a duration in milliseconds as `"Xh Ym Zs"` / `"Ym Zs"` / `"Zs"` (same logic currently local to `ResultsScreen.jsx`; extract to eliminate duplication — RALPH.md §No copy-paste duplication; add JSDoc comment citing `RULES.md §Winning: score = hiding time + bonuses`). (2) `src/components/ResultsScreen.jsx` — import `formatDuration` from `./gameUtils.js`; remove the now-redundant local function definition. (3) `src/components/GameMap.jsx` — extend the existing `import { formatCountdown }` to also import `formatDuration`; in the render, derive `const displayElapsedMs = hidingStartedAtRef.current ? (Date.now() - hidingStartedAtRef.current) + bonusSecondsRef.current * 1_000 : null`; after the existing timer banner, add `{player.role === 'hider' && (phase === 'hiding' || phase === 'seeking') && displayElapsedMs !== null && (<p data-testid="elapsed-timer" className={styles.elapsedTimer}>Hiding time: {formatDuration(displayElapsedMs)}</p>)}` — the `setTimerTick` re-render every second drives the update automatically. (4) `src/components/GameMap.module.css` — add `.elapsedTimer`: `text-align: center; font-family: 'JetBrains Mono', monospace; font-size: 0.875rem; color: var(--color-text-secondary); padding: 0.15rem 0; margin: 0`. Tests: `src/components/gameUtils.test.js` — (a) `formatDuration(0)` returns `'0s'`; (b) `formatDuration(59_000)` returns `'59s'`; (c) `formatDuration(90_000)` returns `'1m 30s'`; (d) `formatDuration(3_661_000)` returns `'1h 1m 1s'`; `src/components/GameMap.test.jsx` — (e) elapsed timer renders with `data-testid="elapsed-timer"` for hider during hiding phase; (f) elapsed timer renders for hider during seeking phase; (g) elapsed timer is NOT rendered for seekers; (h) elapsed timer is NOT rendered before hiding starts (no `hidingStartedAtRef.current`).

[x] **164** — ZoneSelector station search filter: large or medium games can have hundreds of transit stations, making the ZoneSelector list very difficult to scroll on mobile. Add a search input that filters the station list as the hider types.

[x] **163** — Zone privacy: `zone_locked` must not reveal the hider's zone to seekers; seekers see the zone only when End Game starts.

[x] **162** — Player names on map markers: map markers for other players show raw player IDs (e.g. `p_abc123`) instead of names, because the `join_game` WS message does not carry `name`, and neither `player_joined` nor `game_state_sync` broadcast names. Players cannot identify each other on the map during a real game. Changes needed: (1) `src/components/GameMap.jsx` — add `name: player.name` to the `joinMsg` object so the server receives the player's name on join; add `playerNamesRef` (`useRef({ [player.playerId]: player.name })`) to track names keyed by playerId; populate it from `player_joined` events (use `msg.name` when present) and from `game_state_sync` players array (each entry now has `name`); update the marker label construction in both marker-add and marker-update paths from `pid` (raw ID) to `playerNamesRef.current[pid] ?? pid` so tooltips show "Alice 🚌" instead of "p_abc123 🚌". (2) `server/wsHandler.js` — extend `_handleJoinGame` signature to destructure `name` from the message; store it in an instance-level `_playerNames` Map (`this._playerNames.set(playerId, name)`); in `_broadcastPlayerJoined`, include `name: this._playerNames.get(playerId) ?? null` in the broadcast payload; in the `game_state_sync` `playersForSync` map, include `name: this._playerNames.get(pid) ?? null` alongside `playerId` and `role`; clean up `_playerNames` entry in `_finalizeDisconnect`. (3) Tests: `server/wsHandler.test.js` — (a) `player_joined` broadcast includes `name` field matching the value sent in `join_game`; (b) `game_state_sync` players array entries include `name`; (c) when `name` is omitted from `join_game`, broadcast uses `null`; `src/components/GameMap.test.jsx` — (d) after `player_joined` WS event with `name: 'Alice'`, the marker tooltip for that player shows `'Alice'` not the raw playerId; (e) after `game_state_sync` with players array including `name`, existing markers show names in their tooltips; (f) `join_game` message sent on WS open includes `name` equal to `player.name`.

[x] **160** — Game session persistence across page reloads: `Lobby.jsx` now saves player identity in localStorage (Task 158) but does not save `game` or `playing` state. A mobile player who is in an active game (`playing === true`) and accidentally refreshes loses both state values, landing on `GameForm` — where they must re-enter the game ID and re-join via WaitingRoom. But the game is already in the `hiding` or `seeking` phase, so the WaitingRoom polls forever waiting for a start that never happens. The player is effectively locked out of an in-progress game. Changes needed: (1) `src/components/Lobby.jsx` — add `GAME_STORAGE_KEY = 'jetlag_game'` and `PLAYING_STORAGE_KEY = 'jetlag_playing'` constants at module scope (alongside the existing `STORAGE_KEY`); add pure `restoreGame()` function that reads `localStorage.getItem(GAME_STORAGE_KEY)`, parses JSON, validates the result has `gameId` and `size` as non-empty strings, and returns the object or `null` on any failure; add pure `restorePlaying()` function that reads `localStorage.getItem(PLAYING_STORAGE_KEY)` and returns `true` only when the stored value is the string `'true'`, otherwise `false`; initialise `game` state with `useState(() => restoreGame())` so a mid-game refresh re-mounts `GameMap` directly; initialise `playing` state with `useState(() => restorePlaying())`; replace `setGame` (passed as `onGameReady`) with `handleGameReady(g)` — calls `localStorage.setItem(GAME_STORAGE_KEY, JSON.stringify(g))` then `setGame(g)`; replace the `onGameStarted` inline `() => setPlaying(true)` with `handleGameStarted()` — calls `localStorage.setItem(PLAYING_STORAGE_KEY, 'true')` then `setPlaying(true)`; replace the `onPlayAgain` inline lambda with `handlePlayAgain()` — calls `localStorage.removeItem(GAME_STORAGE_KEY)`, `localStorage.removeItem(PLAYING_STORAGE_KEY)`, then `setGame(null)` and `setPlaying(false)`; update `handleChangePlayer` to also call `localStorage.removeItem(GAME_STORAGE_KEY)` and `localStorage.removeItem(PLAYING_STORAGE_KEY)` before calling `setPlayer(null)` — changing identity invalidates the session. (2) `src/components/Lobby.test.jsx` — update `beforeEach` to also `localStorage.removeItem('jetlag_game')` and `localStorage.removeItem('jetlag_playing')` for test isolation; add tests: (a) when localStorage has valid game JSON and `jetlag_playing='true'`, `GameMap` is rendered on mount; (b) when localStorage has valid game JSON but `jetlag_playing` is absent, `WaitingRoom` is rendered on mount; (c) when localStorage game JSON is invalid, falls back to `GameForm`; (d) after `onGameStarted` fires, `localStorage.getItem('jetlag_playing')` is `'true'`; (e) after `onGameReady` fires (create game), `localStorage.getItem('jetlag_game')` contains the game JSON; (f) clicking "Not {name}?" clears `jetlag_game` and `jetlag_playing` from localStorage; (g) `handlePlayAgain` clears both localStorage keys and returns to `GameForm`.

[x] **158** — Player identity persistence across page reloads: when a mobile player accidentally refreshes the browser tab or loses connectivity, they are sent back to player registration and their original `playerId` is lost — making WS reconnect to an in-progress game impossible through the normal UI. Changes needed: (1) `src/components/Lobby.jsx` — define `STORAGE_KEY = 'jetlag_player'` constant at module scope; add pure `restorePlayer()` function that reads `localStorage.getItem(STORAGE_KEY)`, parses JSON, validates the result has `{ playerId, name, role }` as non-empty strings, and returns the object or `null` on any failure (parse error, missing fields, wrong types); use `useState(() => restorePlayer())` as the player state initialiser so the identity is restored synchronously before first render (no flash of the registration form); add `handleRegistered(player)` wrapper that calls `localStorage.setItem(STORAGE_KEY, JSON.stringify(player))` then `setPlayer(player)`; pass `handleRegistered` as `onRegistered` to `<PlayerForm>` instead of `setPlayer`; render a `<button type="button" className={styles.changePlayerBtn} onClick={handleChangePlayer} aria-label="Change player identity">Not {player.name}?</button>` below the header when `player` is set and `!playing`; `handleChangePlayer` calls `localStorage.removeItem(STORAGE_KEY)` and `setPlayer(null)`. (2) Create `src/components/Lobby.module.css` — add `.changePlayerBtn`: `background: none; border: none; color: var(--color-text-secondary); font-size: 0.8rem; cursor: pointer; padding: 0.25rem; text-decoration: underline; min-height: 44px; min-width: 44px`. (3) `src/components/Lobby.test.jsx` — add tests: (a) when `localStorage` contains valid saved player JSON, `PlayerForm` is NOT rendered and `GameForm` IS rendered on mount; (b) valid `localStorage` player restores state without calling `registerPlayer`; (c) when `localStorage` contains invalid JSON, falls back to showing `PlayerForm`; (d) when `localStorage` has an object missing required fields (`name`, `role`, `playerId`), falls back to `PlayerForm`; (e) after `onRegistered` is called by `PlayerForm`, `localStorage` contains the player JSON; (f) "Not {name}?" button is visible when player is set and not playing; (g) clicking "Not {name}?" removes `localStorage` entry and shows `PlayerForm` again; (h) "Not {name}?" button is NOT shown during active gameplay (`playing === true`).

[x] **157** — Disable answer submission after question deadline expires in client UI: when a question's `expiresAt` deadline passes, `AnswerPanel` shows `0:00` in the countdown but the Submit button remains enabled — the hider can attempt to submit and only learns the question has expired after a round-trip 409 from the server. Changes needed: (1) `src/components/AnswerPanel.jsx` — derive an `isExpired` boolean per question: `expiryCounts[q.questionId] === '0:00'`; add `disabled={submitting[q.questionId] || isExpired}` to the Submit button; change the button label to `isExpired ? 'Expired' : submitting[q.questionId] ? 'Sending…' : 'Submit answer'`; add `aria-disabled={isExpired}` so screen readers announce the expired state; add a `className` toggle so expired buttons receive a visual style (`styles.expiredBtn`). (2) `src/components/AnswerPanel.module.css` — add `.expiredBtn` rule: `opacity: 0.5; cursor: not-allowed;` — no new colour tokens needed. Tests: `src/components/QA.test.jsx` — (a) when `expiresAt` is in the past (countdown `'0:00'`), the Submit button is disabled; (b) when `expiresAt` is in the past, the button label is "Expired"; (c) when `expiresAt` is in the future, the Submit button is enabled with label "Submit answer"; (d) when `submitting` is true (in-flight), the button is disabled with label "Sending…" regardless of expiry; (e) `handleAnswer` is NOT called when the button is disabled and the form is submitted programmatically (simulate `form.submit()` and assert `submitAnswer` was not called).

[x] **156** — Auto-populate hider ID in QuestionPanel: seekers must currently type the hider's `playerId` into a free-text `targetId` input — but players have no way to discover this UUID. The `game_state_sync` WS message is sent to every player on join but carries no player-role list, so the seeker never learns the hider's ID. Changes needed: (1) `server/wsHandler.js` — extend the `game_state_sync` payload to include a `players` array: `Array.from(gameStateManager.getGameState(gameId)?.players ?? {})` mapped to `[{ playerId, role }]`; avoids leaking location data (do not include `lat`/`lon`). (2) `src/components/GameMap.jsx` — add `hiderId` state (null | string); in the `game_state_sync` handler extract the hider: `msg.players?.find(p => p.role === 'hider')?.playerId ?? null`; call `setHiderId`; also extract in the existing `game_state` handler (line ~319) where `msg.players` is already iterated — set `hiderId` from the player whose `role === 'hider'`; pass `hiderId` as a prop to `<QuestionPanel>`. (3) `src/components/QuestionPanel.jsx` — add `hiderId` prop (string | null, default null); initialise `targetId` state to `hiderId ?? ''`; add a `useEffect([hiderId])` that calls `setTargetId(hiderId ?? '')` so late-arriving state syncs; when `hiderId` is non-null replace the free-text input with a read-only `<p>` showing the hider ID (labelled "Target: Hider") so the field is not editable but remains visible; update JSDoc. Tests: `server/wsHandler.test.js` — (a) `game_state_sync` message includes `players` array; (b) players array contains `{ playerId, role }` entries; (c) `lat`/`lon` are NOT present in the players array. `src/components/GameMap.test.jsx` — (d) `game_state_sync` with a hider player sets `hiderId` and passes it to QuestionPanel; (e) `game_state` message with hider player also sets `hiderId`. `src/components/QA.test.jsx` — (f) QuestionPanel with `hiderId` prop pre-populates `targetId`; (g) when `hiderId` is provided the free-text input is replaced by a read-only display; (h) when `hiderId` is null the free-text input is shown.

[x] **154** — Player ready confirmation in WaitingRoom: RULES.md §Setup requires "All players begin at a common starting point." Currently the host can start immediately without players confirming they have gathered. Changes needed: (1) `functions/games.js` — add in-process `_readyPlayers: Map<gameId, Set<playerId>>`; add `markReady(req, pool)` handler (`POST /games/:gameId/ready` with `{ playerId, ready: boolean }` body) — validates `gameId` and `playerId` present (400 if missing), adds or removes playerId from set, returns `{ readyCount, totalCount }` where `totalCount = _gamePlayers.get(gameId)?.size ?? 0`; add `getReadyStatus(req, pool)` handler (`GET /games/:gameId/ready`) — returns same shape; export `_clearReadyPlayers` and `_getReadyPlayers` for test isolation. (2) `functions/router.js` — import `markReady`, `getReadyStatus`; add routes `POST /games/:gameId/ready → markReady` and `GET /games/:gameId/ready → getReadyStatus`. (3) `src/api.js` — add `markPlayerReady({ gameId, playerId, ready })` (`POST /api/games/${gameId}/ready`); add `fetchReadyStatus(gameId)` (`GET /api/games/${gameId}/ready`); both use `fetchWithTimeout`; throw on non-ok response. (4) `src/components/WaitingRoom.jsx` — add `isReady` state (bool, default false); add `readyCount`/`totalCount` state (both 0); add `handleToggleReady` async function: calls `markPlayerReady({ gameId, playerId, ready: !isReady })`, toggles `isReady`, updates counts from response; add `readyPollIntervalRef`; start a `setInterval(POLL_INTERVAL_MS)` that calls `fetchReadyStatus` and updates `readyCount`/`totalCount` — clear on unmount; render `"(readyCount/totalCount ready)"` text; render "I'm Ready" / "Cancel Ready" `<button>` for all players (including host); host's "Start Game" button remains enabled regardless of ready count (soft enforcement per RULES.md — players may still start early); show any `readyError` state in an `<Alert>`. (5) `src/components/WaitingRoom.module.css` — add `.readyRow` and `.readyCount` styles. Tests: `functions/games.test.js` — (a) `markReady` with `ready: true` returns `{ readyCount: 1, totalCount }` after one player joined; (b) `markReady` with same playerId twice is idempotent; (c) `markReady` with `ready: false` removes from set and returns updated count; (d) 400 on missing `playerId`; (e) 400 on missing `gameId`; (f) `getReadyStatus` returns current `{ readyCount, totalCount }`; `src/components/WaitingRoom.test.jsx` — (g) "I'm Ready" button is rendered; (h) clicking calls `markPlayerReady` with `ready: true`; (i) after marking ready, button text changes to "Cancel Ready"; (j) clicking "Cancel Ready" calls `markPlayerReady` with `ready: false`; (k) ready count text is shown; (l) `markPlayerReady` rejection surfaces an error alert; (m) poll interval is cleared on unmount (no leaked timer).

[x] **153** — `Lobby.jsx` reads `VITE_GAME_SERVER_URL` which is not defined in `.env.example` or `config/env.js`, causing WebSocket connections to always use an empty string URL in any environment where `VITE_GAME_SERVER_URL` is not manually set. The correct env var is `VITE_WS_URL`, which is already defined in both `config/env.js` (as `ENV.wsUrl`) and `.env.example`. `ENV` is already imported in `Lobby.jsx`. Changes needed: (1) `src/components/Lobby.jsx` — replace `const SERVER_URL = import.meta.env.VITE_GAME_SERVER_URL ?? '';` with `const SERVER_URL = ENV.wsUrl;` — this uses the single source of truth in `config/env.js` and eliminates the undefined env var reference. No new API surface changes. Tests: `src/components/Lobby.test.jsx` — add test: the `serverUrl` prop passed to `GameMap` matches `ENV.wsUrl` (mock `config/env.js` to return a known wsUrl value and assert it is forwarded). No changes to CI pipeline or other files needed.

[x] **152** — Architecture review cleanup (periodic review at task 150): `src/components/QuestionPanel.jsx` has two RALPH guardrail violations found during the scheduled 30-task architecture review. (1) Magic literal `1000` on line 59 (`setInterval(..., 1000)`) — RALPH.md mandates named constants for every magic value; fix: add `const COUNTDOWN_TICK_MS = 1_000;` at module scope with a comment. (2) `curseCountdown` IIFE on lines 64–70 reimplements the `M:SS` countdown format already exported as `formatCountdown` from `./gameUtils.js` — RALPH.md §No-Shortcut Rules forbids copy-paste duplication; fix: import `formatCountdown`; replace the IIFE with `const curseCountdown = isCurseActive ? formatCountdown(curseEndsAt) : null;`. No new tests needed — the existing `QA.test.jsx` curse-banner suite covers all render paths; `formatCountdown` is already tested in `gameUtils.test.js`.

[x] **151** — Hardcoded hex literals in `AppHeader.jsx`: seven `fill="..."` colour literals in the SVG logo element are not extracted to named constants — violating RALPH.md §JavaScript / JSX Style "Named constants for every magic value". The colours are `#1B2A3A` (used twice: background rect and airplane fill), `#C83A18`, `#E05828`, `#F08730`, `#F5C84A`, `#FDFAF4`. Changes needed: (1) `src/components/AppHeader.jsx` — add six constants at module scope with comments citing DESIGN.md §22: `const SVG_BG = '#1B2A3A'` (--color-bg); `const SVG_SUNSET_4 = '#C83A18'` (--color-sunset-4); `const SVG_SUNSET_3 = '#E05828'` (--color-sunset-3); `const SVG_SUNSET_2 = '#F08730'` (--color-sunset-2); `const SVG_SUNSET_1 = '#F5C84A'` (--color-sunset-1); `const SVG_WHITE = '#FDFAF4'` (--color-white); replace all seven `fill="..."` literals with the constants using JSX expression syntax `fill={CONSTANT}`. No new tests needed — these are constant-extraction changes with no observable behaviour change; the existing `AppHeader.test.jsx` suite covers all render paths.

[x] **150** — Three RALPH guardrail violations in `GameForm.jsx`: (1) `style={{ height: '100%', width: '100%' }}` static inline style on `<MapContainer>` (line 326) — RALPH.md CSS Conventions forbids inline `style` objects for static values; fix: add `.mapContainer { height: 100%; width: 100%; }` to `GameForm.module.css` and replace the inline style with `className={styles.mapContainer}`. (2) Hardcoded `'#F08730'` hex literals in `LocationCircle.pathOptions` (lines 50, 52) without a named constant — RALPH.md mandates named constants for every magic value; fix: add `const ZONE_COLOR = '#F08730'` (--color-accent, DESIGN.md §22) at module scope with a comment and replace both literals. (3) Hardcoded `#F08730` and `rgba(255,255,255,0.8)` hex literals embedded in `divIcon` HTML strings (lines 93, 101) without named constants; fix: add `const MARKER_BORDER_COLOR = 'rgba(255,255,255,0.8)'` at module scope and use template literals to interpolate `ZONE_COLOR` and `MARKER_BORDER_COLOR` into the `divIcon` HTML strings. No new tests needed — these are constant-extraction and CSS-class changes that do not alter any observable behaviour; the existing `GameForm.test.jsx` suite covers all affected render paths.

[x] **149** — Score submission error silently swallowed + photo image inline style: two guardrail violations in `GameMap.jsx` and `QuestionPanel.jsx`. (1) `src/components/GameMap.jsx` line 354: `submitScore(...).catch(() => { /* fire-and-forget */ })` swallows all score-submission errors silently — RALPH.md §No-Shortcut Rules forbids empty catch blocks. When the serverless `/api/scores` endpoint is unreachable (network error, 5xx, timeout) the player's score is lost with no feedback. Fix: add `scoreError` state (`null | string`); replace the fire-and-forget with `.catch((err) => setScoreError(err.message ?? 'Score could not be saved.'))` ; render `scoreError` as a dismissible `role="alert"` banner beneath the results screen (so the results are still shown); dismiss button calls `setScoreError(null)`. (2) `src/components/QuestionPanel.jsx` line 308: `style={{ maxWidth: '200px', display: 'block' }}` on the photo `<img>` — RALPH.md CSS Conventions §"No inline style objects for static values". Fix: add `.questionPhoto { max-width: 200px; display: block; border-radius: 4px; margin-top: 0.5rem }` to `src/components/QuestionPanel.module.css` and replace the inline style with `className={styles.questionPhoto}`. Tests: `src/components/GameMap.test.jsx` — (a) when `submitScore` rejects, a `role="alert"` error message appears in the DOM; (b) clicking the dismiss button removes the error message. No new tests needed for the CSS-only photo change.

[x] **148** — GPS errors silently suppressed in GameMap: `navigator.geolocation.getCurrentPosition` error callback is empty (`() => {}`), so players with denied permissions, unavailable GPS, or unsupported devices get no feedback — they appear to be playing normally but their location is never tracked, breaking capture detection and zone enforcement. Changes needed: (1) `src/components/GameMap.jsx` — add `gpsError` state (null | string); replace the empty error callback with a handler: code 1 (PERMISSION_DENIED) → set `gpsError` to "Location access denied — enable location in your browser settings to continue playing."; code 2 (POSITION_UNAVAILABLE) → set `gpsError` to "Location unavailable — your device cannot determine its position."; code 3 (TIMEOUT) → no banner (transient; next tick retries); on a successful position fix, if `gpsError` is non-null, call `setGpsError(null)` to clear the banner; when `navigator.geolocation` is absent (early return on line 472), call `setGpsError('Your device does not support location access — this game requires GPS.')` immediately. (2) `src/components/GameMap.jsx` JSX — render `gpsError` as a dismissible `role="alert"` banner using `className={styles.alertBanner}` alongside the existing banners; dismiss button calls `setGpsError(null)`. Tests: `src/components/GameMap.test.jsx` — (a) when `navigator.geolocation` is undefined, renders GPS-unsupported alert; (b) error callback with code 1 renders "Location access denied…" alert; (c) error callback with code 2 renders "Location unavailable…" alert; (d) error callback with code 3 (TIMEOUT) renders no GPS error alert; (e) successful position fix after error clears the GPS error banner.

[x] **145** — Manual bounds fields do not sync the preview map. Fixed: `setBound` now computes `center` (midpoint) and `radiusKm` (half the shorter dimension) when all four bounds fields are valid finite numbers with `lat_max > lat_min` and `lon_max > lon_min`; preview map and Zone radius output update on manual field edits per DESIGN.md §22. 3 new tests; 1633 tests pass, build clean.

[x] **143** — Preview map in GameForm; three hardcoded hex literals outside named constants in `GameMap.jsx`. Two related issues: (1) **Transit marker update bug** — when a `player_transit` WS event updates `players[pid].onTransit`, the marker update `useEffect` only calls `setLatLng` for existing markers, so the transit icon (🚌) in the tooltip and the reduced `fillOpacity` never reflect live transit-status changes; the visual only updates if the player later sends a `player_location` event. Seekers cannot see in real time when another seeker goes on or off transit — which matters for End Game capture eligibility. (2) **Hardcoded hex literals** — three colour literals are not extracted to named constants: `'#3388ff'` (line 123 — game bounds rectangle border; Leaflet default, off-brand); `'#8b5cf6'` (lines 205–206 — false zone circle; off-brand purple); `'#0000cc'` (line 271 — trail polyline; off-brand dark blue). Changes needed: (1) `src/components/GameMap.jsx` — add `const BOUNDS_BORDER_COLOR = '#9EB3C8'` (--color-text-secondary); `const FALSE_ZONE_COLOR = '#9EB3C8'`; `const TRAIL_COLOR = '#F5C84A'` (--color-sunset-1) at module scope with comments; replace the three inline literals with the constants; in the marker update `useEffect`, extend the "marker already exists" branch: after `setLatLng`, compute `isOnTransit`, `label` and call `markersRef.current[pid].setStyle({ fillOpacity: isOnTransit ? 0.3 : 0.7 })` and `markersRef.current[pid].setTooltipContent(label)`. (2) `src/components/GameMap.test.jsx` — add `setStyle: vi.fn()` and `setTooltipContent: vi.fn()` to `mockMarker` in `vi.hoisted`; restore them in `beforeEach`; update the existing "handles player_transit WS event without crashing" test to first send a `player_location` event for `p2` (creates the marker), then send `player_transit` with `onTransit: true`, and assert `mockMarker.setTooltipContent` was called with a string containing `🚌`; add a second test verifying the 🚌 icon is absent from the tooltip when `onTransit` transitions back to `false`.

[x] **139** — Inline styles and hardcoded colours in GameMap.jsx, QuestionPanel.jsx, and ZoneSelector.jsx: eleven `style={{}}` objects in `GameMap.jsx` use hardcoded hex values (`#fecaca`, `#fef3c7`, `#dcfce7`, `#ffe`, `#fee2e2`, `#fbbf24`, `#d1fae5`) and static layout values directly on JSX elements — violating both the "No inline `style` objects for static values" and "All colours via CSS custom properties" guardrails. `QuestionPanel.jsx` has a hardcoded `#fee2e2` background on the curse banner. `ZoneSelector.jsx` uses an inline `style` for `fontWeight` and `cursor` on each zone list button. Changes needed: (1) `src/components/GameMap.module.css` — add `.gameHeader` (`display: flex; justify-content: space-between; padding: 0.5rem 0`); `.alertBanner` (error-tinted panel: `background: rgba(200,58,24,0.15); border-left: 3px solid var(--color-error); color: var(--color-text-primary); border-radius: 0 4px 4px 0; padding: 0.5rem 0.75rem; font-weight: 600`); `.warningBanner` (amber-tinted: `background: rgba(245,200,74,0.15); border-left: 3px solid var(--color-sunset-1); color: var(--color-text-primary); border-radius: 0 4px 4px 0; padding: 0.5rem 0.75rem; font-weight: 600`); `.infoBanner` (green-tinted: `background: rgba(76,175,130,0.15); border-left: 3px solid var(--color-success); color: var(--color-text-primary); border-radius: 0 4px 4px 0; padding: 0.5rem 0.75rem; font-weight: 600`); `.captureBanner` (same as `.alertBanner` but with success tint for the capture message); `.dismissBtn` (`background: none; border: none; cursor: pointer; margin-left: 0.5rem; color: inherit; padding: 0 0.25rem; min-width: 44px; min-height: 44px`); `.spotRejectedMsg` (`display: block; color: var(--color-error); font-size: 0.875rem; margin-top: 0.25rem`); `.transitToggleWrapper` (`padding: 0.5rem 0`); `.transitToggle` (base button: `border: 1px solid var(--color-border); border-radius: 4px; padding: 0.375rem 0.75rem; cursor: pointer; font-family: 'Inter', sans-serif; font-weight: 600; min-height: 44px; min-width: 44px`); `.transitToggleOn` (`background: var(--color-sunset-1); color: var(--color-bg)`); `.transitToggleOff` (`background: var(--color-success); color: var(--color-bg)`). (2) `src/components/GameMap.jsx` — replace all eleven `style={{}}` occurrences with the corresponding CSS module classes; transit toggle button: apply `${styles.transitToggle} ${myOnTransit ? styles.transitToggleOn : styles.transitToggleOff}` as a template-literal className. (3) `src/components/QuestionPanel.module.css` — add `.curseBanner` matching the alert-panel pattern: `background: rgba(200,58,24,0.15); border-left: 3px solid var(--color-error); color: var(--color-text-primary); border-radius: 0 4px 4px 0; padding: 0.25rem 0.5rem`. (4) `src/components/QuestionPanel.jsx` — replace `style={{ background: '#fee2e2', padding: '0.25rem 0.5rem' }}` with `className={styles.curseBanner}`. (5) `src/components/ZoneSelector.module.css` — add `.zoneBtn` (`cursor: pointer; background: none; border: 1px solid var(--color-border); border-radius: 4px; padding: 0.375rem 0.75rem; width: 100%; text-align: left; color: var(--color-text-primary); min-height: 44px`); `.zoneBtn:disabled { cursor: not-allowed; opacity: 0.6 }`; `.zoneBtnSelected` (`font-weight: bold; border-color: var(--color-accent); color: var(--color-accent)`). (6) `src/components/ZoneSelector.jsx` — replace `style={{ fontWeight: ..., cursor: ... }}` with `className={[styles.zoneBtn, staged?.stationId === zone.stationId ? styles.zoneBtnSelected : ''].join(' ').trim()}`. No new tests needed — all existing tests query by role/text/data-testid and are unaffected by CSS class changes; run `npm run ci:local` to confirm zero regressions.

[x] **137** — Admin Dashboard UI Component: `ENV.features.adminDashboard` is `false` by default; when set to `true`, operators expect a UI to view live server health. `functions/admin.js` and `GET /api/admin` are fully implemented — returning `{ connectedPlayers, activeGameCount, games: [{ gameId, phase, phaseElapsedMs, playerCount }] }` — but the frontend has no component that calls this endpoint. Changes needed: (1) `src/api.js` — add `fetchAdminStatus(apiKey)` that calls `GET /api/admin` with `Authorization: Bearer ${apiKey}` using `fetchWithTimeout`; on non-ok response reads JSON body and throws `new Error(body?.error || 'fetchAdminStatus failed')`. (2) Create `src/components/AdminDashboard.jsx` — accepts no props; renders: a `<form>` with a password `<input>` labeled "Admin API Key" (`aria-label`) and a "Connect" `<Button>`; on submit calls `fetchAdminStatus`; while loading shows `<p>Loading…</p>`; on success shows three metric boxes (Connected Players, Active Games, Games table with columns Game ID / Phase / Elapsed / Players) where elapsed is `Math.floor(phaseElapsedMs / 60000) + 'm ' + Math.floor((phaseElapsedMs % 60000) / 1000) + 's'`; on error shows `<Alert>`; shows a ghost "Refresh" button that re-fetches with the stored key; shows "No active games" when `games` array is empty. (3) Create `src/components/AdminDashboard.module.css` — `.container` (panel look), `.statsRow` (flex row, gap 1rem), `.statCard` (surface-2 bg, border, border-radius 6px, padding 0.75rem, flex 1), `.statLabel` (text-secondary, font-size 0.75rem, uppercase), `.statValue` (Oswald 700, font-size 1.75rem, accent colour), `.table` (width 100%, border-collapse collapse), `.actions` (flex row gap 0.5rem, margin-top 1rem). (4) `src/components/Lobby.jsx` — import `ENV` from `../../config/env.js`; import `AdminDashboard`; add `showAdmin` state (default false); when `ENV.features.adminDashboard` is true, render a ghost "Admin" `<Button>` alongside the Leaderboard toggle; when `showAdmin` is true render `<AdminDashboard />` with a "Back" button to close. Tests: `src/components/AdminDashboard.test.jsx` — (a) renders "Admin API Key" input; (b) renders "Connect" button; (c) submitting key calls `fetchAdminStatus` with the key; (d) shows `connectedPlayers` after success; (e) shows `activeGameCount` after success; (f) renders a table row per active game; (g) on 401/error renders `role="alert"`; (h) "Refresh" button re-fetches; (i) empty games array renders "No active games"; `src/components/Lobby.test.jsx` — (j) Admin button absent when `ENV.features.adminDashboard` is false; (k) Admin button present when flag is true; (l) clicking Admin button shows AdminDashboard.

### Previously completed (moved to log)

[x] **138** — React Error Boundary: any unhandled render error crashes the entire React tree to a blank screen because `src/App.jsx` has no error boundary. Changes needed: (1) Create `src/components/ErrorBoundary.jsx` — class component (the only legitimate exception to the no-class-components rule: React Error Boundaries cannot be implemented as function components); implements `static getDerivedStateFromError(error)` returning `{ hasError: true, message: error?.message ?? 'Unknown error' }`; implements `componentDidCatch(error, info)` calling optional `this.props.onError(error, info)` if provided; renders `this.props.children` when `!hasError`; on error renders a full-screen fallback: `<div role="alert" className={styles.container}>` containing `<h2>Something went wrong</h2>`, `<p className={styles.message}>{this.state.message}</p>`, and `<button type="button" className={styles.reloadBtn} onClick={() => window.location.reload()}>Reload</button>`; accepts optional `onError` prop (function) and `children` prop. (2) Create `src/components/ErrorBoundary.module.css`. (3) `src/App.jsx` — wrap `<Lobby />` inside `<ErrorBoundary>`. (4) `src/components/ErrorBoundary.test.jsx` — 4 tests.

[x] **134** — `question_pending` and `question_expired` WS events do not refresh AnswerPanel or QuestionPanel: `GameMap.jsx` handles both events but never increments `qaRefresh`. For `question_pending` (line 377–378): only `setPendingQuestionExpiresAt` is called — the hider's AnswerPanel does not re-fetch, so a newly submitted question never appears in the pending list. For `question_expired` (line 365–366): only `setPendingQuestionExpiresAt(null)` is called — the expired question remains visible in AnswerPanel as "pending" until a later `question_answered` or remount triggers a re-fetch. The hider can even attempt to answer an already-expired question, receiving a 409 from the server with no explanation. Additionally, `AnswerPanel` does not display a per-question answer deadline, so the hider has no warning that a question is about to expire. Changes needed: (1) `src/components/GameMap.jsx` — in the `question_pending` handler, add `setQaRefresh((n) => n + 1)` so AnswerPanel fetches the new question immediately; in the `question_expired` handler, add `setQaRefresh((n) => n + 1)` so the expired question is removed from the list. (2) `src/components/AnswerPanel.jsx` — add `expiresAt` countdown to each pending question: add an `expiryCounts` state `{ [questionId]: string }` updated by a single `setInterval` (1 s) that iterates pending questions and calls the shared `formatCountdown(isoString)` helper; display `<p className={styles.expiry}>Answer by: <time>{expiryCounts[q.questionId]}</time></p>` beneath each pending question; clear the interval on unmount. (3) Extract `formatCountdown` from `GameMap.jsx` into a new `src/components/gameUtils.js` module and import it in both `GameMap.jsx` and `AnswerPanel.jsx` — eliminates duplication. Tests: `src/components/GameMap.test.jsx` — (a) `question_pending` WS event increments `qaRefresh`; (b) `question_expired` WS event increments `qaRefresh`; `src/components/QA.test.jsx` — (c) AnswerPanel renders "Answer by:" for a pending question that has `expiresAt`; (d) a pending question without `expiresAt` does not crash; (e) interval is cleaned up on unmount (no leaked timers).

[x] **133** — `game_players` table never populated: `dbJoinGame` is defined in `db/gameStore.js` and exported, but is never called from any application code. When a player creates or joins a game via `GameForm`, only `createGame` or `lookupGame` is called — neither records the player in `game_players`. When a player connects via WebSocket, `wsHandler._handleJoinGame` updates in-memory state only. As a result, `dbGetGamePlayerCounts` (used by `handleStartGame` to validate player counts) always returns `{ hiderCount: 0, seekerCount: 0 }` for real DB-backed deployments, making it impossible to ever start a game in production. Changes needed: (1) `db/gameStore.js` — update `dbJoinGame` to use `INSERT ... ON CONFLICT (game_id, player_id) DO NOTHING RETURNING ...`; if no rows returned (player already joined), re-fetch and return the existing row (idempotent reconnect support). (2) `functions/games.js` — add `joinGame(req, pool)` handler: reads `gameId` from `req.params.gameId`, `{ playerId, role, team }` from `req.body`; validates `playerId` present and `role` in `VALID_ROLES`; with pool calls `dbJoinGame`; without pool stores in in-process `_gamePlayers` Map; returns `{ status: 200, body: { gameId, playerId, role, team } }`. Export `joinGame`, `_clearGamePlayers`, `_getGamePlayers` for tests. (3) `functions/router.js` — add `POST /games/:gameId/join → joinGame` after the `/start` route. (4) `src/api.js` — add `joinGame({ gameId, playerId, role, team })` using `fetchWithTimeout`; POSTs to `/api/games/${gameId}/join`; throws on non-ok. (5) `src/components/GameForm.jsx` — import `joinGame`; in `handleCreate`, after `createGame` resolves, call `await joinGame({ gameId: game.gameId, playerId: player.playerId, role: player.role })`; in `handleJoin`, after `lookupGame` resolves, call the same; if `joinGame` throws, set error and abort. Tests: `functions/games.test.js` — (a) `joinGame` returns 200 with `{ gameId, playerId, role }` in-process; (b) 400 on missing `playerId`; (c) 400 on invalid role; (d) in-process: same player joining twice is idempotent (returns same record); (e) with pool calls `dbJoinGame`; `src/components/Lobby.test.jsx` — (f) `joinGame` is called with correct args after `createGame` resolves; (g) `joinGame` is called with correct args after `lookupGame` resolves; (h) if `joinGame` rejects after create, error is displayed; (i) if `joinGame` rejects after join lookup, error is displayed.

### Completed Tasks Log

| # | Date | Task | Files | Notes |
|---|------|------|-------|-------|
| 169 | 2026-03-22 | Expand Overpass transit query to include ferry terminals and tram stops | functions/zones.js, functions/zones.test.js, spec/TASKS.md | Added amenity=ferry_terminal and railway=tram_stop node clauses to buildOverpassQuery; updated JSDoc to list all six transit types; 3 new tests (query string assertions + ferry/tram node inclusion); 1757 tests pass, build clean |
| 167 | 2026-03-22 | Hider in-zone status indicator during seeking phase | src/components/gameUtils.js, src/components/gameUtils.test.js, src/components/GameMap.jsx, src/components/GameMap.module.css, src/components/GameMap.test.jsx, spec/TASKS.md | Added haversineDistanceM delegating to haversineKm*1000; myLocation state set on GPS success callback; inZone derived for hider during seeking; "✓ In zone" / "⚠ Outside zone" role="status" banner with zoneStatus/zoneInside/zoneOutside CSS classes; 8 new tests (3 gameUtils + 5 GameMap); 1750 tests pass, build clean |
| 166 | 2026-03-22 | Zone circle absent after page refresh/reconnect | src/components/GameMap.jsx, src/components/GameMap.test.jsx, spec/TASKS.md | Removed unused syncedZones state; game_state_sync handler now calls setLockedZone from zones[0] when player is hider (always) or seeker + endGameActive (zone privacy preserved); updated 1 existing test + 3 new tests; 1742 tests pass, build clean |
| 160 | 2026-03-21 | Game session persistence across page reloads | src/components/Lobby.jsx, src/components/Lobby.test.jsx | Added GAME_STORAGE_KEY + PLAYING_STORAGE_KEY + restoreGame() + restorePlaying() + handleGameReady() + handleGameStarted() + handlePlayAgain() to Lobby; game and playing state initialised from localStorage; handleChangePlayer clears all three keys; 7 new tests; 1704 tests pass, build clean |
| 159 | 2026-03-21 | Pending-question countdown and submit-lock in QuestionPanel | src/components/QuestionPanel.jsx, QuestionPanel.module.css, QA.test.jsx | Derived pendingQuestion from allQuestions; disabled submit button when pending; added pendingBanner with formatCountdown countdown; 4 new tests; 1697 tests pass, build clean |
| 158 | 2026-03-21 | Player identity persistence across page reloads | src/components/Lobby.jsx, src/components/Lobby.module.css, src/components/Lobby.test.jsx, src/test-setup.js | Added STORAGE_KEY + restorePlayer() + handleRegistered() + handleChangePlayer() to Lobby; useState lazy initialiser restores saved player synchronously; "Not {name}?" button clears identity; localStorage mock added to test-setup.js (jsdom has stub only); 8 new tests; 1693 tests pass, build clean |
| 156 | 2026-03-21 | Auto-populate hider ID in QuestionPanel | server/wsHandler.js, src/components/GameMap.jsx, QuestionPanel.jsx, QuestionPanel.module.css, wsHandler.test.js, GameMap.test.jsx, QA.test.jsx | game_state_sync now includes players:[{playerId,role}] (no lat/lon); GameMap extracts hiderId from both game_state_sync and game_state messages; QuestionPanel accepts hiderId prop — pre-populates targetId and shows read-only label when known; 8 new tests; 1680 tests pass, build clean |
| 155 | 2026-03-21 | Pre-populate hider answer from computed hint | src/components/questionHints.js, src/components/AnswerPanel.jsx, src/components/QA.test.jsx | Added 5 *AnswerText helper functions (thermometer, tentacle, measuring, transit, matching); AnswerPanel pre-fills textarea on question load via functional setAnswers merge that preserves user edits; 8 new tests; 1671 tests pass, build clean |
| 154 | 2026-03-21 | Player ready confirmation in WaitingRoom | functions/games.js, functions/router.js, src/api.js, src/components/WaitingRoom.jsx, WaitingRoom.module.css, WaitingRoom.test.jsx, functions/games.test.js | RULES.md §Setup — "all players begin at a common starting point"; added markReady/getReadyStatus handlers, POST+GET /games/:gameId/ready routes, markPlayerReady/fetchReadyStatus API fns, "I'm Ready" toggle + ready count in WaitingRoom; 17 new tests; 1663 tests pass, build clean |
| 151 | 2026-03-21 | AppHeader.jsx hardcoded hex constants | src/components/AppHeader.jsx | Extracted 6 constants (SVG_BG, SVG_SUNSET_4/3/2/1, SVG_WHITE) from DESIGN.md §22; replaced 7 fill="..." literals with fill={CONSTANT} JSX expressions; no new tests needed; 1645 tests pass, build clean |
| 150 | 2026-03-20 | GameForm.jsx guardrail fixes: inline style + hardcoded hex constants | src/components/GameForm.jsx, GameForm.module.css | Added ZONE_COLOR + MARKER_BORDER_COLOR constants; replaced #F08730 literals in pathOptions and divIcon HTML strings; added .mapContainer CSS class replacing static inline style on MapContainer; 1645 tests pass, build clean |
| 149 | 2026-03-20 | Score submission error surfaced; photo img inline style moved to CSS | src/components/GameMap.jsx, GameMap.test.jsx, QuestionPanel.jsx, QuestionPanel.module.css | Replaced fire-and-forget catch with scoreError state; dismissible role="alert" banner shown on submit failure; photo img style moved to .questionPhoto CSS class; 2 new tests; 1645 tests pass, build clean |
| 148 | 2026-03-21 | GPS errors surfaced to players | src/components/GameMap.jsx, GameMap.test.jsx | Added gpsError state; replaced empty GPS error callback with handler for codes 1/2/3; unsupported device sets error on mount; successful fix clears banner; 5 new tests; beforeEach default no-op geolocation mock prevents cross-test interference; 1643 tests pass, build clean |
| 145 | 2026-03-20 | Manual bounds fields sync preview map | src/components/GameForm.jsx, GameForm.test.jsx | setBound now computes center+radius from midpoint/half-shorter-dimension when all four fields are valid; preview map and Zone radius output update on manual field edits; 3 new tests; 1633 tests pass, build clean |
| 143 | 2026-03-20 | Preview map in GameForm | src/components/GameForm.jsx, GameForm.module.css, GameForm.test.jsx, Lobby.test.jsx | MapContainer (CartoDB dark tiles) + LocationCircle + draggable centre Marker shown when center is set; .previewMap CSS (260px/320px); react-leaflet and leaflet mocked in both test files; fixed duplicate data-testid by removing it from wrapper div (mock MapContainer provides it); 3 new tests; 1624 tests pass, build clean |
| 142 | 2026-03-20 | Nominatim geocoding search in GameForm | src/components/GameForm.jsx, GameForm.module.css, GameForm.test.jsx, gameUtils.js | Geocoding search with 500ms debounce, listbox results, centerRadiusToBounds helper, Advanced collapsible for manual bounds; 1610 tests pass, build clean |
| 141 | 2026-03-20 | Game creation failing in production — SCHEMA_SQL out of sync | db/db.js, db/db.test.js, functions/games.test.js | Root cause: SCHEMA_SQL in db/db.js missing seeker_teams and host_player_id columns in games table + indices on non-existent tables; dbCreateGame INSERT failed in production; fixed by replacing SCHEMA_SQL with full schema matching db/schema.sql; 8 new tests; 1610 tests pass, build clean |
| 140 | 2026-03-20 | Transit marker visual update bug + hardcoded Leaflet colours | src/components/GameMap.jsx, GameMap.test.jsx | Fixed: player_transit events now update existing marker tooltip (transit icon) and fillOpacity via setTooltipContent/setStyle; extracted BOUNDS_BORDER_COLOR, FALSE_ZONE_COLOR, TRAIL_COLOR constants to replace #3388ff, #8b5cf6, #0000cc literals; 2 new tests; 1602 tests pass, build clean |
| 139 | 2026-03-20 | CSS module cleanup: inline styles and hardcoded colours | src/components/GameMap.module.css, GameMap.jsx, QuestionPanel.module.css, QuestionPanel.jsx, ZoneSelector.module.css, ZoneSelector.jsx | Eliminated all 11 inline style={{}} objects and hardcoded hex colours; added alertBanner/warningBanner/infoBanner/captureBanner/dismissBtn/spotRejectedMsg/transitToggle*/gameHeader/zoneBtn* CSS classes using design-system tokens; no new tests required; 1600 tests pass, build clean |
| 138 | 2026-03-20 | React Error Boundary | src/components/ErrorBoundary.jsx, ErrorBoundary.module.css, ErrorBoundary.test.jsx, src/App.jsx | Class-component ErrorBoundary wraps Lobby; full-screen fallback with reload button; 4 tests; 1600 tests pass, build clean |
| 137 | 2026-03-20 | Admin Dashboard UI Component | src/api.js, src/components/AdminDashboard.jsx, AdminDashboard.module.css, AdminDashboard.test.jsx, Lobby.jsx, Lobby.test.jsx | fetchAdminStatus added to api.js; AdminDashboard renders key-entry form, stat cards, games table, Refresh; Lobby gates Admin button on ENV.features.adminDashboard; 12 new tests; 1596 tests pass, build clean |
| 135 | 2026-03-20 | SPOT_RADIUS_M configurable via env var | config/env.js, server/index.js, .env.example, config/env.test.js | Added spotRadiusM to ENV; removed local DEFAULT_SPOT_RADIUS_M from index.js; added SPOT_RADIUS_M to .env.example; 2 new tests; 1576 tests pass, build clean |
| 134 | 2026-03-20 | question_pending/expired WS events now refresh AnswerPanel; per-question expiry countdown added | src/components/GameMap.jsx, src/components/AnswerPanel.jsx, src/components/AnswerPanel.module.css, src/components/gameUtils.js, src/components/GameMap.test.jsx, src/components/QA.test.jsx | question_pending and question_expired both increment qaRefresh; formatCountdown extracted to gameUtils.js; AnswerPanel shows countdown per question; 5 new tests; 1574 tests pass, build clean |
| 133 | 2026-03-20 | game_players table population via POST /games/:gameId/join | db/gameStore.js, functions/games.js, functions/router.js, src/api.js, src/components/GameForm.jsx, functions/games.test.js, src/components/Lobby.test.jsx | dbJoinGame made idempotent with ON CONFLICT DO NOTHING; joinGame handler + VALID_ROLES exported; route added; api.js joinGame with error body propagation; GameForm awaits joinGame after create and join; 12 new tests; 1569 tests pass, build clean |
| 132 | 2026-03-20 | Transit station fetching for ZoneSelector | src/api.js, src/components/GameMap.jsx, src/components/GameMap.test.jsx | Added listZones() to api.js; added availableZones/zonesError state + useEffect to fetch on hiding phase; added lockedZoneLayerRef + useEffect to draw zone circle on map; ZoneSelector now receives fetched stations instead of empty syncedZones; 3 new tests; 1557 tests pass, build clean |
| 117 | 2026-03-20 | Motion and entry animations | src/index.css, src/components/Alert.module.css | Added fadeSlideIn + shake keyframes to index.css; applied fadeSlideIn 200ms ease-out to .panel; removed duplicate @keyframes shake from Alert.module.css; prefers-reduced-motion media query; 1541 tests pass, build clean |
| 116 | 2026-03-20 | Leaderboard styles | src/components/Leaderboard.module.css, Leaderboard.jsx | CSS module with container panel, table layout, th/td styles, rank1/rank2/rank3 colour classes, empty state; 1541 tests pass, build clean |
| 114 | 2026-03-19 | GameMap visual overhaul | src/components/GameMap.jsx, GameMap.module.css, GameMap.test.jsx | CartoDB dark tiles, brand zone/marker colours, CSS module classes for banners and spot FAB button; 90 GameMap tests pass, build clean |
| 113 | 2026-03-19 | WaitingRoom, ZoneSelector, ResultsScreen styles | src/components/WaitingRoom.module.css, ZoneSelector.module.css, ResultsScreen.module.css; WaitingRoom.jsx, ZoneSelector.jsx, ResultsScreen.jsx | CSS modules for inviteCode, inviteLink, teamBadge, durationRow, instructions, confirmBtn, overlay, winner, stats; removed static inline styles; 1540 tests pass, build clean |
| 112 | 2026-03-19 | PlayerForm and GameForm styles | src/components/PlayerForm.module.css, GameForm.module.css; PlayerForm.jsx, GameForm.jsx | CSS module classes for form layout, role group, label, tabs, tabActive, fieldsGrid; 1540 tests pass, build clean |
| 111 | 2026-03-19 | App header with logo | src/components/AppHeader.jsx, AppHeader.module.css, AppHeader.test.jsx; Lobby.jsx | Branded AppHeader with SVG sunset semicircle logo and airplane silhouette; replaces bare h1/p in Lobby; 1540 tests pass, build clean |
| 110 | 2026-03-19 | Alert component | src/components/Alert.jsx, Alert.module.css, Alert.test.jsx; PlayerForm.jsx, GameForm.jsx, WaitingRoom.jsx | Shared Alert component with shake animation; replaced bare `<p role="alert">` in three components |

### Phase 48 — Configuration and Rules Compliance

- [x] **135** — Surface `SPOT_RADIUS_M` as a configurable env var: `server/index.js` and `server/wsHandler.js` each define `DEFAULT_SPOT_RADIUS_M = 30` as a local hardcoded constant, inconsistent with the pattern established by Task 126 (which moved `END_GAME_TIMEOUT_MS` into `config/env.js`). Operators have no way to tune the spot radius without a code change. Changes needed: (1) `config/env.js` — add `spotRadiusM: number('SPOT_RADIUS_M', 30)` to the `ENV` object (server-side only, same pattern as `endGameTimeoutMs`). (2) `server/index.js` — remove the local `const DEFAULT_SPOT_RADIUS_M = 30` constant; change the `createServer` parameter default from `DEFAULT_SPOT_RADIUS_M` to `ENV.spotRadiusM`. (3) `.env.example` — add a `SPOT_RADIUS_M=30` entry with a comment explaining the GPS-practical-range rationale. Tests: `config/env.test.js` — (a) `ENV.spotRadiusM` is a number; (b) `ENV.spotRadiusM` defaults to 30 when `SPOT_RADIUS_M` env var is not set.

- [x] **136** — Card draw probabilities per question category: `RULES.md` §Cards states "Each category has card costs and effects" but `functions/cards.js` draws a card with uniform random probability regardless of which question category was answered. Changes needed: (1) `config/gameRules.js` — add `CARD_DRAW_WEIGHTS` constant mapping each of the 6 question categories (`matching`, `measuring`, `transit`, `photo`, `thermometer`, `tentacle`) to a `{ time_bonus, powerup, curse }` probability triple (values sum to 1.0 per category); define sensible defaults (e.g., `photo` has higher `time_bonus` weight since it costs the hider more effort; `tentacle` has higher `curse` weight; `transit` and `measuring` balanced). (2) `functions/cards.js` — update `drawCardInProcess` and `drawCard` (DB path) to accept an optional `questionCategory` parameter; use `CARD_DRAW_WEIGHTS[questionCategory]` (falling back to equal weights when omitted or unrecognised) to select the card type via a weighted random draw instead of `Math.random() * 3 \| 0`. (3) `functions/questions.js` — in `submitAnswer`, pass `question.category` to `drawCardInProcess`/`drawCard`. Tests: `functions/cards.test.js` — (a) `drawCardInProcess` with `questionCategory: 'photo'` draws `time_bonus` cards at the configured higher rate over 1000 iterations (statistical check within tolerance); (b) unknown category falls back to equal weights; (c) each configured category produces valid card type.

### Phase 47 — Bug Fixes and Gap Closure

- [x] **132** — Transit station fetching for ZoneSelector: `src/api.js` has no `listZones` function, so `ZoneSelector` always shows "No stations available in the game area." The `/api/zones` serverless endpoint is complete but never called from the frontend. Currently `GameMap` passes `syncedZones` (the locked zone from WebSocket, initially `[]`) to `ZoneSelector`, but what `ZoneSelector` needs is the full list of selectable transit stations fetched from the server. Changes needed: (1) `src/api.js` — add `listZones({ scale, bounds })` that calls `GET /api/zones?bounds=${bounds.lat_min},${bounds.lon_min},${bounds.lat_max},${bounds.lon_max}&scale=${scale}` using `fetchWithTimeout`; returns the `zones` array from the response; throws on non-ok status. (2) `src/components/GameMap.jsx` — add `availableZones` state (default `[]`) and `zonesError` state; add a `useEffect` that fires when `phase === 'hiding'` and `player.role === 'hider'`; fetches `listZones({ scale: game.size, bounds: game.bounds })` and sets `availableZones`; on rejection, sets `zonesError` and shows it in an alert. Pass `availableZones` to `<ZoneSelector zones={availableZones}>` instead of `syncedZones`. The `syncedZones` state continues to be used for rendering the locked zone circle on the map — these are two separate concerns. (3) `src/components/GameMap.test.jsx` — add tests: (a) `listZones` is called when hider enters `hiding` phase; (b) ZoneSelector receives the fetched zones; (c) when `listZones` rejects, a `role="alert"` error message appears and ZoneSelector receives `[]`.

- [x] **120** — `phase_change` message property name mismatch: `server/index.js` broadcasts `{ type: 'phase_change', gameId, oldPhase, newPhase }` (line 669), but `src/components/GameMap.jsx` reads `msg.phase` on line 285 (`setPhase(msg.phase)`) which is always `undefined` — causing the displayed phase to go blank and all phase-gated UI (ZoneSelector, Q&A panels, spot button, timer banner at lines 541/571/614) to disappear after the first phase transition. Lines 290 and 296 partially compensate by checking both `msg.newPhase` and `msg.phase`, but line 285 still poisons the `phase` state. Changes needed: (1) `src/components/GameMap.jsx` — on line 285 change `setPhase(msg.phase)` to `setPhase(msg.newPhase)`; on lines 290 and 296 remove the redundant `|| msg.phase === '...'` guards — keep only the `msg.newPhase` check; audit all other references to `msg.phase` in the `phase_change` handler branch and replace with `msg.newPhase`. (2) `src/components/GameMap.test.jsx` — add tests: (a) after a `phase_change` WS event the `phase` state reflects `newPhase`; (b) ZoneSelector renders during `hiding` phase after phase_change; (c) Q&A panels render during `seeking` phase after phase_change; (d) results overlay renders when `newPhase === 'finished'`.

- [x] **121** — Leaderboard score omits bonus seconds: `src/components/Leaderboard.jsx` displays `fmtMmSs(s.scoreSeconds)` but the spec (Task 53) defines total score as elapsed hiding time plus time-bonus card values. The API returns both `scoreSeconds` and `bonusSeconds`; the leaderboard silently ignores the bonus column, showing lower scores than players earned. Changes needed: (1) `src/components/Leaderboard.jsx` — render a total column computed as `scoreSeconds + (bonusSeconds ?? 0)` using `fmtMmSs`; add a "Bonus" sub-column (formatted as `+Xm`) when `bonusSeconds > 0`; update column headers accordingly. (2) `src/components/Leaderboard.test.jsx` — add tests: (a) entry with `bonusSeconds: 0` shows only `scoreSeconds` formatted; (b) entry with `bonusSeconds: 120` shows total `scoreSeconds + 120` in the score column; (c) sort order is by total score descending, not raw `scoreSeconds`.

- [x] **122** — QuestionPanel team filter reads non-existent property: `src/components/QuestionPanel.jsx` line 70 calls `listQuestions({ gameId: game.gameId, teamId: game.teamId ?? undefined })`, but the `game` prop is the DB game record which has no `teamId` field — `game.teamId` is always `undefined`, so in two-team games all questions from both teams are returned to every seeker (breaking the Task 93 isolation). The player's team assignment is delivered via the `joined_game` WebSocket message and stored as `myTeam` state in `GameMap.jsx`. Changes needed: (1) `src/components/GameMap.jsx` — pass `myTeam` to `<QuestionPanel>` as a `teamId` prop. (2) `src/components/QuestionPanel.jsx` — replace `game.teamId ?? undefined` with the `teamId` prop received from `GameMap`; update the prop documentation comment. (3) `src/components/QA.test.jsx` — add test: when `teamId` prop is provided, the `listQuestions` fetch URL includes `teamId=A` (or `B`); when `teamId` is absent the param is omitted.

- [x] **123** — API fetch calls have no timeout: every function in `src/api.js` uses `fetch()` with no `AbortController` or timeout. If the serverless function or managed server becomes unresponsive, the UI hangs indefinitely — the player sees a spinner with no way to recover. Changes needed: (1) `src/api.js` — define `const FETCH_TIMEOUT_MS = 10_000` at module scope; add a helper `fetchWithTimeout(url, options)` that creates an `AbortController`, calls `setTimeout(() => controller.abort(), FETCH_TIMEOUT_MS)`, and clears the timer on resolution; replace all `fetch(...)` calls with `fetchWithTimeout(...)`; on `AbortError`, throw `new Error('Request timed out')`. (2) `src/api.js` tests (if a dedicated test file exists) or `src/components/Lobby.test.jsx` — add tests: (a) a fetch that does not resolve within `FETCH_TIMEOUT_MS` throws "Request timed out"; (b) a fetch that resolves before the timeout succeeds normally and does not leave a dangling timer.

- [x] **124** — `SCALE_DURATION_RANGES` constant duplicated: `functions/games.js` (line 23) and `server/index.js` (line 35) each define an identical `SCALE_DURATION_RANGES` constant with the same keys and values. If scale ranges change (e.g., small max extended from 60 to 90 min) only one file is typically updated, creating silent divergence. Changes needed: (1) Create `config/gameRules.js` — export `SCALE_DURATION_RANGES` and `SCALE_DURATIONS` (also defined separately in both files) as the single source of truth. (2) `functions/games.js` — remove the local definition; import from `config/gameRules.js`. (3) `server/index.js` — remove the local definition; import from `config/gameRules.js`. (4) Verify all existing tests that import or reference these constants still pass after the import path changes. Tests: `functions/games.test.js` and `server/server.test.js` scale-validation tests must pass unchanged.

- [x] **125** — Missing database performance indices: the `questions`, `cards`, `game_players`, and `scores` tables have no indices on their most common filter columns. At scale, `SELECT ... WHERE game_id = $1 AND status = 'pending'` on questions and `SELECT ... WHERE game_id = $1 AND player_id = $2` on cards perform full-table scans. Changes needed: (1) `db/schema.sql` — add idempotent index statements: `CREATE INDEX IF NOT EXISTS idx_questions_game_status ON questions (game_id, status)`; `CREATE INDEX IF NOT EXISTS idx_questions_game_asker ON questions (game_id, asker_id)`; `CREATE INDEX IF NOT EXISTS idx_cards_game_player ON cards (game_id, player_id)`; `CREATE INDEX IF NOT EXISTS idx_scores_game ON scores (game_id)`; `CREATE INDEX IF NOT EXISTS idx_game_players_game ON game_players (game_id)`. (2) No application code changes. Tests: `db/db.js` `createTables` test or a new smoke test that calls `createTables()` on a test DB and confirms the function completes without error; `npm run ci:local` passes.

- [x] **126** — End Game timeout hardcoded: `server/index.js` defines `DEFAULT_END_GAME_TIMEOUT_MS = 10 * 60_000` (10 minutes) as an in-file constant not surfaced to operators. There is no way to tune this for different deployments or game scales without a code change. Changes needed: (1) `config/env.js` — add `END_GAME_TIMEOUT_MS: number(process.env.END_GAME_TIMEOUT_MS, 600_000)` using the existing `number()` helper (fallback 600 000 ms = 10 min). (2) `server/index.js` — replace the literal `DEFAULT_END_GAME_TIMEOUT_MS` definition with `import { ENV } from '../config/env.js'` (already imported) and use `ENV.END_GAME_TIMEOUT_MS`; remove the now-redundant constant. (3) `.env.example` — add `END_GAME_TIMEOUT_MS=600000  # End Game phase duration in milliseconds (default 10 min)`. Tests: `server/server.test.js` — add test: `createServer` with a custom `endGameTimeoutMs` option uses that value for the End Game timer rather than the env default; existing End Game tests continue to pass.

- [x] **127** — Rate limiter store-full prune clears all counters: `functions/rateLimiter.js` calls `this._prune()` when the store reaches `maxEntries` (10 000). The prune deletes expired entries; if the store is still at the limit afterwards, it calls `this.store.clear()`, resetting every IP's window counter — a sudden traffic spike resets all rate limits and creates a window of unprotected access. Changes needed: (1) `functions/rateLimiter.js` — remove the `this.store.clear()` fallback; instead, when the store is still at limit after pruning, reject the current request with 429 immediately (the store is saturated; a new entry cannot safely be created); log the event with `console.warn` so operators know the limit is being hit. (2) `functions/rateLimiter.test.js` — add tests: (a) when store is at `maxEntries` with no expired entries, subsequent request returns `{ limited: true }` without clearing existing counters; (b) after the prune removes expired entries and store drops below limit, a new IP is admitted.

- [x] **128** — Silent failure when managed server unreachable during question hint fetches: `functions/questions.js` wraps all `fetchTentacleData`, `fetchMeasuringData`, `fetchTransitData`, and `fetchMatchingData` calls in try/catch blocks that silently discard errors. When the managed server is down, questions are created with all computed fields set to `null` and players receive no hint data — with no log entry indicating why. Changes needed: (1) `functions/questions.js` — in each catch block for hint-data fetches, add `console.error('[questions] hint fetch failed', { type, gameId, questionId, error: err.message })` before continuing; use a consistent `[questions]` prefix for easy log filtering. (2) Similarly update `functions/cards.js` and `functions/games.js` notify/fire-and-forget catch blocks to log structured errors. No new tests needed; verify build passes and no existing tests regress.

- [x] **129** — Photo upload lacks client-side size pre-check: `src/components/AnswerPanel.jsx` reads the selected file as base64 and calls `uploadQuestionPhoto` before checking its size. If the photo exceeds 500 KB the API returns 413, but the player only learns this after a potentially slow upload attempt. The `file.size` property is available immediately from the `<input>` change event before any reading occurs. Changes needed: (1) `src/components/AnswerPanel.jsx` — define `MAX_PHOTO_BYTES = 500_000` as a module-level constant; in the photo `<input onChange>` handler, before calling `FileReader.readAsDataURL`, check `file.size > MAX_PHOTO_BYTES`; if so, set an inline error state "Photo must be under 500 KB" and return without reading or uploading. (2) `src/components/QA.test.jsx` — add tests: (a) selecting a file under the limit proceeds to `FileReader` without setting an error; (b) selecting a file over the limit sets the error message and does NOT call `uploadQuestionPhoto`.

- [x] **130** — Game start silent failure when managed server is unavailable: `functions/games.js` `notifyGameStart` wraps the managed server notification in a fire-and-forget try/catch that swallows all errors. If the managed server is down, `handleStartGame` returns 204 (success) to the host, the host's UI transitions to playing, but the server never begins the hiding phase — all players are stuck in a non-functional game with no error message. Changes needed: (1) `functions/games.js` — change `notifyGameStart` from fire-and-forget to an awaited call; if the notification fetch fails or returns a non-2xx status, `handleStartGame` returns `{ status: 503, body: { error: 'game_server_unavailable', message: 'Game server could not be reached. Please try again.' } }`; only return 204 after a confirmed successful notification. (2) `src/api.js` — `startGame` already propagates error messages from the response body on failure (Task 98); no change needed. (3) `src/components/WaitingRoom.jsx` — existing error display via `role="alert"` already handles this; no change needed. Tests: `functions/games.test.js` — (a) `handleStartGame` returns 503 when `notifyGameStart` throws; (b) `handleStartGame` returns 503 when managed server responds with 500; (c) `handleStartGame` returns 204 when notification succeeds.

- [x] **131** — Game zones: single-zone model inconsistency between DB and server memory: `db/schema.sql` enforces at most one zone per game (`UNIQUE` on `game_id` in `game_zones`) which is correct per RULES.md (one hiding zone per hider). However `server/gameState.js` stores zones as an array (`zones: []`) and `server/index.js` (false-zone decoy logic) iterates this array assuming multiple entries are possible. `src/components/GameMap.jsx` also renders all zones from an array. The mismatch creates confusion about whether N zones are intended. Changes needed: (1) `server/gameState.js` — rename `zones` to `zone` (singular) in the game entry; `setGameZones` becomes `setGameZone(gameId, zone)` accepting a single zone object; `getGameZones(gameId)` is kept as an alias returning `[zone]` (array-of-one) for backward compatibility with broadcast code that iterates zones; add JSDoc clarifying that one real zone per game is the invariant. (2) `server/index.js` — update the false-zone decoy code to use `getGameZones(gameId)[0]` explicitly (already does `[0]` effectively via `zones[0]`); add a comment that the single-zone model is intentional. (3) `src/components/GameMap.jsx` — no change needed (already handles single-element array correctly). Tests: `server/gameState.test.js` — (a) `setGameZone` stores the zone and `getGameZones` returns it wrapped in an array; (b) calling `setGameZone` twice replaces the first zone (no accumulation).

### Phase 46 — Frontend Visual Design

- [x] **104** — CSS token foundation and global base styles: `src/index.css` does not exist and `index.html` carries a stale `theme-color` of `#1a56db`. All colour tokens from DESIGN.md §22 must be defined as CSS custom properties before any component styling can begin. Changes needed: (1) Create `src/index.css` — add `:root` block with all 15 colour tokens exactly as specified in DESIGN.md §22 Colour Palette (`--color-bg`, `--color-surface`, `--color-surface-2`, `--color-border`, `--color-text-primary`, `--color-text-secondary`, `--color-sunset-1` through `--color-sunset-4`, `--color-accent`, `--color-accent-hover`, `--color-success`, `--color-error`, `--color-white`); add universal `box-sizing: border-box` reset; add `body` base styles: `background-color: var(--color-bg)`, `color: var(--color-text-primary)`, `font-family: 'Inter', sans-serif`, `margin: 0`, `min-height: 100vh`. (2) `index.html` — add `<link rel="preconnect" href="https://fonts.googleapis.com">` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` before the closing `</head>`; add Google Fonts stylesheet link loading `Oswald:wght@600;700`, `Inter:wght@400;500`, and `JetBrains+Mono:wght@400`; update `<meta name="theme-color" content="#1B2A3A">`. (3) `src/main.jsx` — add `import './index.css'` as the first import so tokens are available globally. Tests: `npm run build` succeeds with the new file; `npm run test` — all existing tests continue to pass (pure CSS/HTML change, no behaviour change).

- [x] **105** — Typography scale: no heading, body, or code element has the brand fonts applied. Changes needed: (1) `src/index.css` — add element selectors after the `:root` block: `h1` — `font-family: 'Oswald', sans-serif; font-weight: 700; font-size: 2rem; margin: 0`; `h2` — `font-family: 'Oswald', sans-serif; font-weight: 600; font-size: 1.4rem; margin: 0 0 0.5rem`; `h3` — `font-family: 'Oswald', sans-serif; font-weight: 600; font-size: 1.1rem; margin: 0 0 0.25rem`; `body` (extend existing) — `line-height: 1.5`; `code`, `pre`, `kbd` — `font-family: 'JetBrains Mono', monospace; font-size: 0.9em`; `p` — `margin: 0 0 0.5rem`; `a` — `color: var(--color-accent); text-decoration: none`; `a:hover` — `text-decoration: underline`. Tests: build passes; all existing tests pass.

- [x] **106** — Page layout shell: `App.jsx` renders `<Lobby />` directly with no layout wrapper, so content spans the full viewport width on desktop and has no consistent horizontal padding. Changes needed: (1) Create `src/App.module.css` — define `.root` class: `max-width: 480px; margin: 0 auto; padding: 0 1rem; width: 100%`; add `@media (min-width: 600px) { .root { max-width: 560px; font-size: 1.05rem } }`. (2) `src/App.jsx` — import `styles from './App.module.css'` and wrap the `<Lobby />` return in `<div className={styles.root}>`. Tests: `src/App.test.jsx` — (a) renders without error; (b) rendered output contains the `<Lobby>` component (verify presence of the lobby heading via `screen.getByRole('heading', { name: /JetLag/i })`); mock `src/components/Lobby.jsx` as needed to keep test isolated.

- [x] **107** — Button component: bare `<button>` elements are unstyled throughout the app. Create a `Button` component that encapsulates all three variants from DESIGN.md §22 and enforces the 44 px touch target, press animation, and focus ring. Changes needed: (1) Create `src/components/Button.jsx` — accepts props `variant='primary'|'ghost'|'danger'` (default `'primary'`), `children`, `disabled`, `type='button'`, `onClick`, `className`, `aria-label`; spreads any remaining HTML button attributes; applies the correct CSS module class for the variant plus a base class that all variants share. (2) Create `src/components/Button.module.css` — `.btn` base: `display: inline-flex; align-items: center; justify-content: center; min-height: 44px; padding: 0.6rem 1.4rem; font-family: 'Oswald', sans-serif; font-weight: 600; font-size: 1rem; text-transform: uppercase; letter-spacing: 0.05em; border: none; border-radius: 4px; cursor: pointer; transition: transform 80ms, background-color 150ms; outline-offset: 2px`; `:active` — `transform: scale(0.97)`; `:focus-visible` — `outline: 2px solid var(--color-accent)`; `:disabled` — `opacity: 0.45; cursor: not-allowed`; `.primary` — `background: var(--color-accent); color: var(--color-bg)`; `.primary:hover:not(:disabled)` — `background: var(--color-accent-hover)`; `.ghost` — `background: transparent; border: 1px solid var(--color-accent); color: var(--color-accent)`; `.ghost:hover:not(:disabled)` — `background: rgba(240,135,48,0.12)`; `.danger` — `background: var(--color-sunset-4); color: var(--color-white)`. Tests: `src/components/Button.test.jsx` — (a) renders children text; (b) primary variant applies `primary` class; (c) ghost variant applies `ghost` class; (d) danger variant applies `danger` class; (e) disabled prop sets the disabled HTML attribute; (f) onClick fires when clicked; (g) does not fire onClick when disabled; (h) aria-label is forwarded to the underlying `<button>` element; (i) default type is `'button'` (prevents accidental form submit).

- [x] **108** — Global input and select styles: form inputs and selects render with the browser's default white background, clashing with the dark navy theme. Changes needed: (1) `src/index.css` — add global styles for `input`, `select`, `textarea`: `background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 4px; padding: 0.5rem 0.75rem; color: var(--color-text-primary); font-family: 'Inter', sans-serif; font-size: 1rem; width: 100%; box-sizing: border-box`; `input:focus, select:focus, textarea:focus` — `outline: 2px solid var(--color-accent); outline-offset: 2px; border-color: var(--color-accent)`; `input::placeholder, textarea::placeholder` — `color: var(--color-text-secondary)`; `input[type="radio"], input[type="checkbox"]` — `width: auto; accent-color: var(--color-accent)`. Tests: build passes; all existing form component tests (`PlayerForm`, `GameForm`, `WaitingRoom`) pass without regression — these are pure CSS additions with no JSX or logic changes.

- [x] **109** — Panel utility class and section spacing: components like `WaitingRoom`, `PlayerForm`, and `GameForm` are visually flat with no card/panel treatment. Apply the panel spec from DESIGN.md §22. Changes needed: (1) `src/index.css` — add `.panel` utility class: `background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 6px; padding: 1rem 1.25rem; box-shadow: 0 2px 8px rgba(0,0,0,0.4)`; add `.section-gap` utility: `display: flex; flex-direction: column; gap: 1.5rem`. (2) `src/components/WaitingRoom.jsx` — add `className="panel"` to the root `<div aria-label="Waiting room">`. (3) `src/components/PlayerForm.jsx` — add `className="panel"` to the root form element (check existing `aria-label` attribute). (4) `src/components/GameForm.jsx` — add `className="panel"` to the root container div. Tests: all existing tests for `WaitingRoom`, `PlayerForm`, and `GameForm` pass without regression; adding a `className` to the root element does not break any selector-based queries in the tests since all queries use `getByRole`, `getByLabelText`, or `getByText`.

- [x] **110** — Alert component: error messages across components use bare `<p role="alert">` elements with no consistent styling. Introduce a shared `Alert` component implementing the DESIGN.md §22 alert spec and the shake animation. Changes needed: (1) Create `src/components/Alert.jsx` — accepts `children`; renders `<div role="alert" className={styles.alert}>{children}</div>`; exported as default. (2) Create `src/components/Alert.module.css` — `.alert`: `background: rgba(200,58,24,0.15); border-left: 3px solid var(--color-error); border-radius: 0 4px 4px 0; padding: 0.5rem 0.75rem; color: var(--color-text-primary); animation: shake 300ms ease-in-out`; add `@keyframes shake`: `0%, 100% { transform: translateX(0) } 25% { transform: translateX(-4px) } 75% { transform: translateX(4px) }`. (3) `src/components/PlayerForm.jsx` — replace `<p role="alert">` with `<Alert>`. (4) `src/components/GameForm.jsx` — replace `<p role="alert">` with `<Alert>`. (5) `src/components/WaitingRoom.jsx` — replace `<p role="alert">` with `<Alert>`. Tests: `src/components/Alert.test.jsx` — (a) renders `role="alert"`; (b) renders children text; (c) element has the module CSS class applied. All existing `PlayerForm`, `GameForm`, and `WaitingRoom` tests that assert `screen.getByRole('alert')` must continue to pass unchanged — the `role` is preserved on the new component.

- [x] **111** — App header with logo: `Lobby.jsx` renders a plain `<h1>JetLag: The Game</h1>` with no brand identity. Replace it with a styled `AppHeader` component that includes the airplane-on-sunset-semicircle logo from the brand image. Changes needed: (1) Create `src/components/AppHeader.jsx` — renders: an `<svg aria-label="JetLag logo">` containing the airplane silhouette (simple geometric path, dark navy fill `#1B2A3A`) centred on a sunset semicircle (five concentric arcs in the five sunset colours, bottom half only — implemented as SVG `<path>` arcs or `<ellipse>` clips); an `<h1>` with text "JetLag: The Game"; a `<p>` tagline "Hide and seek across transit networks." (2) Create `src/components/AppHeader.module.css` — `.header`: `text-align: center; padding: 1.5rem 0 1rem`; `.logo`: `width: 120px; height: auto; display: block; margin: 0 auto 0.75rem`. (3) `src/components/Lobby.jsx` — import `AppHeader`; replace the existing `<h1>` and `<p>` tagline with `<AppHeader />`; remove the now-redundant heading and paragraph. Tests: `src/components/AppHeader.test.jsx` — (a) renders heading with text "JetLag: The Game"; (b) renders tagline; (c) SVG element is present with `aria-label="JetLag logo"`. `src/components/Lobby.test.jsx` existing test "renders the app title and tagline" must continue to pass since `AppHeader` preserves both elements.

- [x] **112** — PlayerForm and GameForm styles: both forms display fields with no visual separation between sections and no styling on their tab buttons. Changes needed: (1) Create `src/components/PlayerForm.module.css` — `.form`: `display: flex; flex-direction: column; gap: 0.75rem`; `.roleGroup`: `display: flex; gap: 1rem; align-items: center`; `.label`: `display: block; font-size: 0.875rem; color: var(--color-text-secondary); margin-bottom: 0.25rem`. (2) `src/components/PlayerForm.jsx` — import module and apply classes; ensure the form element keeps its `aria-label="Player registration"` attribute. (3) Create `src/components/GameForm.module.css` — `.container`: `display: flex; flex-direction: column; gap: 1rem`; `.tabs`: `display: flex; border-bottom: 1px solid var(--color-border); margin-bottom: 0.75rem`; `.tab`: `padding: 0.5rem 1rem; background: none; border: none; color: var(--color-text-secondary); font-family: 'Oswald', sans-serif; font-weight: 600; cursor: pointer; border-bottom: 2px solid transparent`; `.tabActive`: `color: var(--color-accent); border-bottom-color: var(--color-accent)`; `.fieldsGrid`: `display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem`. (4) `src/components/GameForm.jsx` — import module and apply classes to the tab buttons and layout divs; tab buttons keep their existing `role="tab"` and `aria-selected` attributes. Tests: all existing `PlayerForm` and `GameForm` tests in `Lobby.test.jsx` and dedicated test files pass unchanged — class names are additive and no accessible attributes are removed.

- [x] **113** — WaitingRoom, ZoneSelector, and ResultsScreen styles: these components are unstyled beyond their functional HTML. Changes needed: (1) Create `src/components/WaitingRoom.module.css` — `.inviteCode`: `font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; background: var(--color-surface-2); padding: 0.25rem 0.5rem; border-radius: 4px`; `.inviteLink`: `word-break: break-all; color: var(--color-accent); font-size: 0.875rem`; `.teamBadge`: `display: inline-block; background: var(--color-accent); color: var(--color-bg); padding: 0.15rem 0.5rem; border-radius: 12px; font-weight: 600`; `.durationRow`: `display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap`. (2) `src/components/WaitingRoom.jsx` — import module and apply classes. (3) Create `src/components/ZoneSelector.module.css` — `.instructions`: `font-size: 0.875rem; color: var(--color-text-secondary); margin-bottom: 0.5rem`; `.confirmBtn`: compound with panel and spacing. (4) `src/components/ZoneSelector.jsx` — apply module classes. (5) Create `src/components/ResultsScreen.module.css` — `.overlay`: `position: fixed; inset: 0; background: var(--color-bg); z-index: 100; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 2rem`; `.winner`: `font-family: 'Oswald', sans-serif; font-size: 2.5rem; font-weight: 700; color: var(--color-sunset-2); text-align: center`; `.stats`: `display: flex; flex-direction: column; gap: 0.5rem; margin: 1.5rem 0; width: 100%; max-width: 320px`. (6) `src/components/ResultsScreen.jsx` — apply module classes. Tests: all existing `WaitingRoom.test.jsx`, `ZoneSelector.test.jsx`, `ResultsScreen.test.jsx` tests pass unchanged.

- [x] **114** — GameMap visual overhaul: the map uses the default OpenStreetMap light tiles (bright background), the hiding zone polygon has no brand styling, and the End Game banner has no visual treatment. Changes needed: (1) `src/components/GameMap.jsx` — update the `TileLayer` URL from the current OSM URL to `https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png` with `attribution='© <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> © <a href="https://carto.com/attributions">CARTO</a>'`; update the hiding zone `Polygon` (or `Circle`) options to use `fillColor: 'rgba(240,135,48,0.15)'`, `color: '#F08730'`, `weight: 2`, `fillOpacity: 1` (CSS rgba covers opacity); update the seeker `CircleMarker` to use `fillColor: '#F08730'`, `color: '#F08730'`; during End Game only, show the hider position marker with `fillColor: '#C83A18'`, `color: '#C83A18'`. (2) Create `src/components/GameMap.module.css` — `.endGameBanner`: `background: linear-gradient(to right, #F5C84A, #F08730, #E05828); color: #1B2A3A; text-align: center; padding: 0.5rem; font-family: 'Oswald', sans-serif; font-weight: 600; position: relative; z-index: 500`; `.reconnectingBanner`: `background: var(--color-surface-2); color: var(--color-text-secondary); text-align: center; padding: 0.4rem; font-size: 0.875rem`; `.spotButton`: same sizing/padding as Button primary but positioned as a floating action button at bottom-centre of map; `.timer`: `text-align: center; font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; padding: 0.25rem 0`. (3) `src/components/GameMap.jsx` — import and apply module classes to banner divs. Tests: `src/components/GameMap.test.jsx` — existing tests all pass; add one test: the `TileLayer` rendered in the component uses a URL containing `cartocdn.com` (check the `url` prop on the mocked TileLayer or the rendered HTML).

- [x] **115** — CardPanel and Q&A styles: the card hand and question/answer panels are functional but visually undifferentiated from plain text. Changes needed: (1) Create `src/components/CardPanel.module.css` — `.panel` (card hand container): applies the global `.panel` look via composition or direct rules; `.hand`: `display: flex; flex-direction: row; gap: 0.5rem; overflow-x: auto; padding-bottom: 0.5rem`; `.card`: `min-width: 120px; background: var(--color-surface-2); border: 1px solid var(--color-border); border-radius: 6px; padding: 0.75rem; flex-shrink: 0; display: flex; flex-direction: column; gap: 0.25rem`; `.cardType`: `font-family: 'Oswald', sans-serif; font-weight: 600; font-size: 0.9rem; color: var(--color-accent); text-transform: uppercase`; `.playBtn`: use Button ghost variant via composition. (2) `src/components/CardPanel.jsx` — import and apply module classes. (3) Create `src/components/QuestionPanel.module.css` — `.history`: `display: flex; flex-direction: column; gap: 0.75rem; margin-bottom: 1rem`; `.historyItem`: `border-left: 2px solid var(--color-border); padding-left: 0.75rem`; `.category`: `font-size: 0.75rem; font-weight: 600; color: var(--color-text-secondary); text-transform: uppercase; letter-spacing: 0.08em`; `.hint`: `font-size: 0.875rem; color: var(--color-sunset-2); font-style: italic`. (4) `src/components/QuestionPanel.jsx` — apply module classes. (5) Create `src/components/AnswerPanel.module.css` — similar layout: `.pendingQuestion` panel card; `.hintText` styled as `--color-sunset-2` italic. (6) `src/components/AnswerPanel.jsx` — apply module classes. Tests: existing `QA.test.jsx` and `CardPanel.test.jsx` tests pass unchanged; class additions are non-breaking.

- [x] **116** — Leaderboard styles: the leaderboard is an unstyled list. Apply table-style layout with rank, name, scale, and score columns. Changes needed: (1) Create `src/components/Leaderboard.module.css` — `.container`: applies `.panel` styles; `.table`: `width: 100%; border-collapse: collapse`; `th` — `font-family: 'Oswald', sans-serif; font-weight: 600; text-align: left; padding: 0.4rem 0.5rem; border-bottom: 1px solid var(--color-border); color: var(--color-text-secondary); font-size: 0.8rem; text-transform: uppercase`; `td` — `padding: 0.4rem 0.5rem; border-bottom: 1px solid var(--color-border); font-size: 0.9rem`; `.rank1` — `color: var(--color-sunset-1); font-weight: 700`; `.rank2` — `color: var(--color-text-secondary); font-weight: 600`; `.rank3` — `color: var(--color-sunset-3); font-weight: 600`; `.empty` — `text-align: center; color: var(--color-text-secondary); padding: 1.5rem`. (2) `src/components/Leaderboard.jsx` — import and apply module classes; change the rendered structure from a `<ul>` to a `<table>` with `<thead>` / `<tbody>` rows if not already tabular; apply rank-specific classes for top 3 rows. Tests: existing `Leaderboard.test.jsx` tests pass; if the rendered structure changes from list to table, update the existing query selectors in tests from `getByRole('listitem')` to `getByRole('row')` etc. — the data rendered must remain identical.

- [x] **117** — Motion and entry animations: panels and alerts appear instantly with no transition, making the UI feel abrupt. Changes needed: (1) `src/index.css` — add `@keyframes fadeSlideIn`: `from { opacity: 0; transform: translateY(8px) } to { opacity: 1; transform: translateY(0) }`; add `@keyframes shake`: `0%, 100% { transform: translateX(0) } 25% { transform: translateX(-4px) } 75% { transform: translateX(4px) }` (move this out of `Alert.module.css` if defined there, into `index.css` so it is reusable); add `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important } }`. (2) `src/index.css` — apply `animation: fadeSlideIn 200ms ease-out` to the `.panel` class. Tests: build passes; all existing tests pass — animation properties do not affect test assertions since jsdom does not execute CSS animations; verify `prefers-reduced-motion` query is syntactically valid by confirming build output.

- [x] **118** — PWA icons and manifest refresh: `public/icon-192.svg` and `public/icon-512.svg` contain placeholder icons that do not reflect the JetLag brand. `manifest.json` has a placeholder `theme_color`. Changes needed: (1) `public/icon-192.svg` — replace content with a 192×192 SVG: dark navy circle background (`#1B2A3A`), five concentric sunset semicircle arcs (bottom half, colours `#FDFAF4`, `#F5C84A`, `#F08730`, `#E05828`, `#C83A18`), a simplified airplane silhouette path in `#1B2A3A` centred on the circle. (2) `public/icon-512.svg` — same design at 512×512. (3) `public/manifest.json` — update `theme_color` to `#1B2A3A`; update `background_color` to `#1B2A3A`; update `name` to `"JetLag: The Game"` and `short_name` to `"JetLag"` if not already set. (4) `index.html` — update `<meta name="theme-color">` to `#1B2A3A` (matches Task 104). Tests: `src/pwa.test.js` — add or update tests: (a) `manifest.json` can be fetched and contains `theme_color: '#1B2A3A'`; (b) both SVG icon files are valid XML (parse with `DOMParser` or check the dist build output for the files); build passes.

- [x] **119** — Accessibility hardening pass: after the visual design tasks are applied, perform a targeted audit of all components for violations of the accessibility rules in RALPH.md §Frontend Guardrails. Known gaps to fix: (1) `src/components/GameMap.jsx` — the "I See the Hider!" spot button currently renders as a plain `<button>` with text; verify it has `type="button"` and a minimum 44 px touch target via its module CSS class. (2) `src/components/CardPanel.jsx` — each "Play" card button must have a descriptive `aria-label` such as `"Play ${card.type} card"` rather than just text "Play" (which is ambiguous when multiple cards are in hand). (3) `src/components/ZoneSelector.jsx` — the station tap/select interaction on the Leaflet map must announce selection to screen readers; add an `aria-live="polite"` region that updates with the selected station name. (4) All interactive elements — run a sweep: every `<button>` that contains only an icon SVG (no text) must have `aria-label`; every `<a>` must have meaningful text or `aria-label`; every `<input>` must have a `<label>` or `aria-label`. Fix any violations found. (5) Verify focus rings are visible by confirming no component CSS contains `outline: none` or `outline: 0` without a replacement. Tests: `src/components/CardPanel.test.jsx` — add test: each play button has a unique `aria-label` containing the card type; `src/components/ZoneSelector.test.jsx` — add test: selecting a station updates the `aria-live` region text; all other existing tests pass.

### Phase 45 — Lobby Correctness

- [x] **103** — Non-host player game start detection: When the host clicks "Start Game", non-host players remain permanently in the WaitingRoom because `Lobby.jsx` only passes `onStart` to the host; non-hosts have no trigger to call `setPlaying(true)`. The game status changes from `'waiting'` to `'hiding'` on the server when the host starts, but non-host players never detect this. Changes needed: (1) `src/components/WaitingRoom.jsx` — accept a new `onGameStarted` prop (callback fired when game transitions away from 'waiting'); when `onStart` is not provided (non-host) but `onGameStarted` is, start a `setInterval` polling `lookupGame(game.gameId)` every 3 s; when the returned `status !== 'waiting'`, call `onGameStarted()` and clear the interval; clear the interval on unmount via `useEffect` cleanup. (2) `src/components/WaitingRoom.jsx` — update `handleStart` (host path): after `startGame` resolves successfully, call `onGameStarted?.()` (in addition to the existing behaviour). (3) `src/components/Lobby.jsx` — pass `onGameStarted={() => setPlaying(true)}` to `WaitingRoom` for all players (both host and non-host); remove the `setPlaying(true)` from the inline `onStart` callback (it is now handled by `onGameStarted`); the host `onStart` prop becomes a function that just triggers the API call (via WaitingRoom's `handleStart` which calls `startGame` + then `onGameStarted`). Tests: `src/components/WaitingRoom.test.jsx` — (a) non-host player starts polling when `onStart` is absent and `onGameStarted` is provided; (b) polling fires `onGameStarted` when `lookupGame` returns `status !== 'waiting'`; (c) polling does NOT start when `onStart` is provided (host); (d) interval is cleared on unmount (no leaked timers); (e) host path: `handleStart` calls `onGameStarted` after `startGame` resolves; `src/components/Lobby.test.jsx` — (f) non-host player transitions to playing state when `onGameStarted` fires.

### Phase 44 — Game Lifecycle Management

- [x] **102** — Host player tracking: The `games` table has no `host_player_id` column, so the system has no record of who created a game. The Lobby currently passes `onStart` to `WaitingRoom` for every player, meaning any player in the waiting room sees the "Start Game" button and can start the game — not just the creator. Changes needed: (1) `db/schema.sql` — add idempotent migration `ALTER TABLE games ADD COLUMN IF NOT EXISTS host_player_id UUID REFERENCES players(id) ON DELETE SET NULL;`. (2) `db/gameStore.js` — `dbCreateGame` accepts optional `hostPlayerId` and includes it as `$4` in the INSERT; returns `hostPlayerId` in the result; `dbGetGame` SELECTs `host_player_id` and returns it as `hostPlayerId`. (3) `functions/games.js` — `createGame` accepts `hostPlayerId` and stores it in the in-process game entry; `handleCreateGame` reads `playerId` from request body and passes as `hostPlayerId` to `createGame`. (4) `src/api.js` — `createGame` accepts `playerId` and includes it in the POST body. (5) `src/components/GameForm.jsx` — passes `player.id` when calling `createGame`. (6) `src/components/Lobby.jsx` — passes `onStart` to `WaitingRoom` only when `player.playerId === game.hostPlayerId`; non-host players see a read-only waiting room. Tests: `db/gameStore.test.js` (dbCreateGame with hostPlayerId passes it as 4th param and returns hostPlayerId; dbCreateGame without hostPlayerId defaults to null; dbGetGame returns hostPlayerId); `functions/games.test.js` (createGame stores hostPlayerId in in-process entry; handleCreateGame reads playerId from body and stores as hostPlayerId); `src/components/Lobby.test.jsx` (Start Game button visible when player is host; Start Game button absent when player joined but is not host).

### Phase 43 — Operational Hardening

- [x] **99** — Container health check endpoint: The Docker container has no `HEALTHCHECK` instruction and the server has no dedicated `/health` endpoint. Container orchestrators (ECS, Fly.io, Kubernetes) need a fast, unauthenticated health probe to determine container readiness and route traffic correctly. Currently any unmatched request returns `{ status: 'ok' }` with no operational metadata. Changes needed: (1) `server/index.js` — add `GET /health` HTTP endpoint (no auth required) that returns 200 `{ status: 'ok', uptimeMs: number, activeGames: number, connections: number }`. Place it before the catch-all response. (2) `Dockerfile` — add `HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 CMD wget -qO- http://localhost:${PORT:-3002}/health || exit 1`. Tests: `server/server.test.js` — (a) `GET /health` returns 200 with `status: 'ok'`; (b) `uptimeMs` is a non-negative number; (c) `activeGames` is 0 when no games are active; (d) `connections` is 0 when no players are connected; (e) no `Authorization` header required (unlike `/internal/admin`).

- [x] **100** — Stale game cleanup: Games that enter `waiting` status but are never started accumulate indefinitely in the DB (no TTL, no cleanup). Over time this pollutes the `games`, `game_players`, and related tables. Changes needed: (1) `db/gameStore.js` — add `dbCleanupStaleGames(pool, maxAgeMs)` that deletes rows from `games` (and cascades to `game_players`, `scores`, `questions`, `answers`, `cards`, `game_zones`) where `status = 'waiting'` AND `created_at < NOW() - interval '$1 milliseconds'`; returns `{ deletedCount: number }`. (2) `functions/games.js` — add `cleanupStaleGames(req, res, pool)` handler for `POST /games/cleanup`; reads optional `maxAgeHours` from body (default 24); calls `dbCleanupStaleGames`; returns `{ deletedCount }`. (3) `functions/router.js` — add route `POST /games/cleanup` (admin-auth protected via `requireAuth`). Tests: `db/gameStore.test.js` (dbCleanupStaleGames deletes stale waiting games; leaves non-waiting games untouched; leaves recent waiting games untouched; returns correct deletedCount); `functions/games.test.js` (cleanupStaleGames returns deletedCount; default maxAgeHours = 24; custom maxAgeHours passed through; 401 without auth token via router).

- [x] **101** — Hider zone requirement before game start: `POST /api/games/:gameId/start` validates player counts (Task 98) but not whether the hider has selected a hiding zone. A game can start with the hider having no zone, which makes capture detection return no capture zone and the hider can never be found. Changes needed: (1) `db/gameStore.js` — add `dbGetGameZone(pool, gameId)` that queries `game_zones` for the row with `game_id = $1`; returns `{ stationId, lat, lon, radius }` or `null` when no zone is set. (2) `functions/games.js` — in `handleStartGame`, after the player count check and before notifying the server, call `await dbGetGameZone(pool, gameId)` when `pool` is provided; if no zone found return `{ status: 400, body: { error: 'no_hider_zone', message: 'Hider has not selected a hiding zone' } }`. Skip the check when `pool` is null. (3) `server/index.js` — in `POST /internal/games/:gameId/start`, after the player count check, verify `gameStateManager.getGameZones(gameId).length > 0`; if empty respond 400 `{ error: 'no_hider_zone', message: 'Hider has not selected a hiding zone' }`. (4) `src/api.js` — no change needed (already propagates server error messages from Task 98). Tests: `db/gameStore.test.js` (dbGetGameZone returns null when no zone; returns zone object when set); `functions/games.test.js` (handleStartGame returns 400 no_hider_zone when pool has no zone; 204 when zone exists; skips zone check when pool is null); `server/server.test.js` (start endpoint returns 400 no_hider_zone when GSM has no zones for game; 204 when zones registered).

### Phase 42 — Gameplay Integrity

- [x] **98** — Minimum player count validation before game start: `POST /api/games/:gameId/start` currently succeeds regardless of how many players have joined, allowing the host to start a game with no seekers (breaking capture detection) or no hider (breaking End Game logic). Changes needed: (1) `db/gameStore.js` — add `dbGetGamePlayerCounts(pool, gameId)` returning `{ hiderCount: number, seekerCount: number }` via `SELECT role, COUNT(*)::int AS count FROM game_players WHERE game_id = $1 GROUP BY role` aggregated into the two fields (0 when a role has no rows). (2) `functions/games.js` — make `handleStartGame` async; when `pool` is provided, call `await dbGetGamePlayerCounts(pool, gameId)` before notifying the server; if `hiderCount < 1` return `{ status: 400, body: { error: 'insufficient_players', message: 'Game requires at least one hider' } }`; if `seekerCount < 1` return `{ status: 400, body: { error: 'insufficient_players', message: 'Game requires at least one seeker' } }`; only fire-and-forget to managed server when counts are valid. When `pool` is null (in-process / test path), skip the DB check and rely on the server's own check. (3) `server/index.js` — in `POST /internal/games/:gameId/start`, after parsing the body, count players by role in `gameStateManager` for the gameId (`gameStateManager.getGamePlayers(gameId)` returns an array of `[playerId, playerEntry]` pairs; count those with `role === 'hider'` and `role === 'seeker'`); if `hiderCount < 1` or `seekerCount < 1`, respond 400 `{ error: 'insufficient_players', message: 'Game requires at least one hider and at least one seeker' }`. (4) `src/api.js` — update `startGame` to read the JSON response body on error and throw `new Error(body?.message || \`startGame failed: ${res.status}\`)` so WaitingRoom shows the server's descriptive message rather than a generic status code. Tests: `db/gameStore.test.js` (dbGetGamePlayerCounts returns `{ hiderCount: 0, seekerCount: 0 }` when no players; `{ hiderCount: 1, seekerCount: 0 }` with only hider; `{ hiderCount: 1, seekerCount: 2 }` with both roles); `functions/games.test.js` (handleStartGame returns 400 `insufficient_players` when pool shows no hider; 400 when no seeker; 204 + fires notify when both present; without pool fires notify immediately); `server/server.test.js` (start endpoint returns 400 when no players in GSM; 400 when only hider; 400 when only seeker; 204 when hider+seeker present).

### Phase 41 — Answer Integrity

- [x] **97** — Reject answers for expired questions: `submitAnswer` currently accepts answers for questions that have already expired or been answered. `dbSubmitAnswer` only checks that the question row exists (`WHERE id = $1`) before inserting the answer, so a hider can answer a question the server has already marked `'expired'` via `dbExpireStaleQuestions`. Changes needed: (1) `db/gameStore.js` — in `dbSubmitAnswer`, change the check query to also fetch `status` (`SELECT id, game_id, status FROM questions WHERE id = $1`); when `status !== 'pending'` return `{ questionExpired: true }` (without inserting an answer or updating the row). (2) `functions/questions.js` — in the DB path of `submitAnswer`, handle the new `{ questionExpired: true }` return with `{ status: 409, body: { error: 'question_expired' } }`; in the in-process path, check `question.status !== 'pending'` before proceeding and likewise return 409. Add exported test helper `_setQuestionStatus(questionId, status)` to allow in-process tests to mark a question expired. Tests: `db/gameStore.test.js` (dbSubmitAnswer returns `{ questionExpired: true }` when status is `'expired'`; returns `{ questionExpired: true }` when status is `'answered'`; still returns `null` when question not found; succeeds when status is `'pending'`); `functions/questions.test.js` (submitAnswer returns 409 with `error: 'question_expired'` for in-process expired question; submitAnswer returns 409 for in-process already-answered question; submitAnswer returns 409 via pool when dbSubmitAnswer returns `{ questionExpired: true }`).

### Phase 36 — Real-time Card Notifications

- [x] **92** — Card draw WS notification to hider: After a hider answers a question, `submitAnswer` calls `drawCardInProcess` but never notifies the managed server (and thus the frontend). The hider's `CardPanel` must poll or manually refresh to see the new card. Changes needed: (1) `functions/questions.js` — after `drawCardInProcess` succeeds (returns a non-null card), fire-and-forget notify the managed server with `{ type: 'card_drawn', gameId, playerId: responderId, cardId: newCard.cardId, cardType: newCard.type }` (same fire-and-forget pattern as other card notifies; use `gameServerUrl` and `adminApiKey` from the handler closure). (2) `server/index.js` — inside the `eventType` handling in `/internal/notify`, add a `card_drawn` branch that broadcasts `{ type: 'card_drawn', gameId, playerId: rest.playerId, cardId: rest.cardId, cardType: rest.cardType }` to the game via `wsHandler.broadcastToGame`. (3) `src/components/CardPanel.jsx` — accept an `onMessage` prop (WS message handler registration function) and call it in a `useEffect`; when a `card_drawn` WS event arrives whose `playerId === playerId` prop, call `fetchCards` to refresh the hand. Tests: `functions/questions.test.js` (submitAnswer fires notify with type `card_drawn` containing cardId and cardType when drawCardInProcess returns a card; skips notify when drawCardInProcess returns null — i.e., hand is full); `server/server.test.js` (card_drawn notify broadcasts to game with playerId/cardId/cardType); `src/components/CardPanel.test.jsx` (card_drawn WS event with matching playerId triggers fetchCards; ignored when playerId does not match).

### Phase 37 — Two-team Data Isolation

- [x] **93** — Two-team question history scoping: In two-team games, `GET /questions?gameId=` returns all questions from both teams, so Team A seekers can read Team B's investigative history and vice versa. Changes needed: (1) `db/gameStore.js` — add optional `teamId` param to `dbGetQuestionsForGame`; when provided, JOIN `game_players gp ON gp.player_id = q.asker_id AND gp.game_id = q.game_id` and add `AND gp.team = $2` to filter by the asker's team; when `teamId` is null/undefined return all questions (existing behaviour). (2) `functions/questions.js` — in `listQuestions`, when `req.query.gameId` is provided also read `req.query.teamId`; pass it through to `dbGetQuestionsForGame`; for the in-process path, filter `_questions` by checking the `askerId` against a per-game team membership map (built from `_teamMemberships` exported Map, keyed `gameId:playerId → team`). (3) `src/components/QuestionPanel.jsx` — pass `teamId` (from game join state) as a query param alongside `gameId` when fetching question history via `GET /api/questions`. Tests: `db/gameStore.test.js` (teamId filter returns only that team's questions; no teamId returns all); `functions/questions.test.js` (listQuestions with teamId filters pool and in-process paths; without teamId returns all); `src/components/QA.test.jsx` (teamId is appended to the question history fetch URL when provided).

### Phase 38 — Gameplay Integrity

- [x] **94** — Player location bounds validation: When a player reports a `location_update` with coordinates outside the game's configured bounds, the server silently accepts them. This can cause hider out-of-zone false negatives and confuse capture detection. Changes needed: (1) `server/gameState.js` — add `getGameBounds(gameId)` returning `{ latMin, latMax, lonMin, lonMax } | null` from the game entry (bounds stored when the game is registered via `setGameZones` or a new `setGameBounds` call). Since bounds currently come from zone registration, read them from the first zone's parent game or store them explicitly: add optional `bounds` param to `initGame` and persist it in the game entry. (2) `server/wsHandler.js` — in `_handleJoinGame`, if the `join_game` message includes a `bounds` field, call `gameStateManager.setGameBounds(gameId, bounds)`. In `_handleLocationUpdate`, after the phase/role checks, call `gameStateManager.getGameBounds(gameId)`; if bounds exist and `lat`/`lon` are outside them, send `{ type: 'location_rejected', code: 'OUT_OF_BOUNDS', message: 'Location is outside game bounds' }` to the sender and return without updating state. (3) `src/components/GameMap.jsx` — handle `location_rejected` WS event with code `OUT_OF_BOUNDS` by showing a brief dismissible banner "Your location is outside game bounds". Tests: `server/gameState.test.js` (setGameBounds/getGameBounds); `server/wsHandler.test.js` (location inside bounds accepted; location outside bounds sends location_rejected and does not update state; no bounds set — all locations accepted); `src/components/GameMap.test.jsx` (location_rejected with OUT_OF_BOUNDS renders banner).

### Phase 39 — Robustness

- [x] **95** — Photo upload size limit: `POST /questions/:questionId/photo` accepts arbitrary `photoData` strings with no size cap. A client could upload a very large base64 image, exhausting memory in the in-process store or filling the DB column. Changes needed: (1) `functions/questions.js` — define `MAX_PHOTO_BYTES = 512_000` (500 KB decoded ≈ 683 KB base64); in the photo upload handler, after extracting `photoData`, check `typeof photoData === 'string' && photoData.length > Math.ceil(MAX_PHOTO_BYTES * 4 / 3)`; if so, return `{ status: 413, body: { error: 'Photo exceeds 500 KB limit' } }`. (2) `db/schema.sql` — add a comment on the `photo_data` column documenting the 500 KB cap (no schema change needed). Tests: `functions/questions.test.js` — 3 tests: (a) `photoData` exactly at the limit is accepted (201); (b) `photoData` one byte over the limit returns 413; (c) `photoData` well under the limit is accepted.

### Phase 40 — Reconnect State Recovery

- [x] **96** — Reconnect game state sync: Task 56 implemented WS reconnection with exponential backoff and re-sends `join_game` on reconnect. However, the server only adds the player back to game state and broadcasts `joined_game`; it sends no snapshot of the current game state. A reconnecting player misses the current phase, pending question, zones, curse status, and end-game active flag until the next periodic broadcast. Changes needed: (1) `server/wsHandler.js` — in `_handleJoinGame`, after sending `joined_game`, build and send a `game_state_sync` message directly to that player's socket using `this._send(ws, ...)`. The payload: `{ type: 'game_state_sync', gameId, phase: gameLoopManager.getPhase(gameId), zones: gameStateManager.getGameZones(gameId), endGameActive: server._endGameTimers?.has(gameId) ?? false }`. Accept the game loop manager reference via the `createServer` opts as `gameLoopManager` (already available in server scope; pass to WsHandler constructor). (2) `src/components/GameMap.jsx` — handle `game_state_sync` event: set `phase`, update `zones` state from `msg.zones`, set `endGameActive` from `msg.endGameActive`. Do not reset `locationTrail` or other accumulated state. Tests: `server/wsHandler.test.js` (join_game sends game_state_sync to connecting player with phase/zones/endGameActive; second player joining same game also gets state_sync; absent gameLoopManager omits phase field gracefully); `src/components/GameMap.test.jsx` (game_state_sync sets phase; game_state_sync updates zones; game_state_sync sets endGameActive; existing locationTrail not cleared on state_sync).

### Phase 35 — Seeker Information Completeness

- [x] **91** — Computed hints in seeker question history: The hint-generating functions in `AnswerPanel.jsx` (`tentacleHint`, `measuringHint`, `matchingHint`, `transitHint`, `thermometerHint`) are only shown in the hider's answer panel, but the same computed data is returned in the question history fetched by `QuestionPanel.jsx`. Seekers cannot see the server-computed factual data (e.g., "Thermometer: warmer — moved closer", "Tentacle: outside radius — 3.5 km away") in their question history, even though this data is available on the question objects. Changes needed: (1) Extract the five hint functions from `AnswerPanel.jsx` into a new `src/components/questionHints.js` module and import them in both `AnswerPanel.jsx` and `QuestionPanel.jsx`. (2) `src/components/QuestionPanel.jsx` — in the question history list, for each answered question (`q.answer` is present), render the category-specific hint below the answer text: for `thermometer` render `<p data-testid="question-thermometer-hint">`; for `tentacle` render `<p data-testid="question-tentacle-hint">`; for `measuring` render `<p data-testid="question-measuring-hint">`; for `transit` render `<p data-testid="question-transit-hint">`; for `matching` render `<p data-testid="question-matching-hint">`. (3) `src/components/QA.test.jsx` — 6 tests: (a) thermometer answered question in history shows thermometer hint; (b) tentacle answered question shows tentacle hint; (c) measuring answered question shows measuring hint; (d) transit answered question shows transit hint; (e) matching answered question shows matching hint; (f) unanswered question does not render any computed hint.

### Phase 34 — Photo Display

- [x] **90** — Photo display in question history: Task 51 added `POST /questions/:questionId/photo` for hiders to upload photos and `GET /questions/:questionId/photo` to retrieve them, and `fetchQuestionPhoto` exists in `src/api.js`, but `QuestionPanel` never fetches or displays these photos — seekers have no way to see the hider's photo answer in the question history. Changes needed: (1) `src/components/QuestionPanel.jsx` — import `fetchQuestionPhoto`; add `photoCache` state (map of questionId → `'loading' | string | null`); add a `fetchedPhotosRef` ref (Set) to avoid duplicate fetches; add a `useEffect` watching `history` that, for every photo question with an answer not yet in the ref, marks the cache entry as `'loading'`, adds the id to the ref, then calls `fetchQuestionPhoto` — on success stores `data.photoData` (or `null` if absent), on failure stores `null`; in the history list, for `category === 'photo'` questions that have an answer, render below the answer text: if `photoCache[q.questionId] === 'loading'` → `<span>Loading photo…</span>`; if the cached value is a non-empty string → `<img data-testid="question-photo-img" src={cached} alt="Hider photo" />`; otherwise → `<span data-testid="no-photo">No photo attached</span>`. (2) `src/components/QA.test.jsx` — 4 tests: (a) photo question with answer fetches photo and renders `<img data-testid="question-photo-img">`; (b) photo question without answer does not render img and does not call fetchQuestionPhoto; (c) non-photo question with answer does not render img; (d) fetchQuestionPhoto rejection renders `<span data-testid="no-photo">`.

### Phase 33 — Matching Question Mechanics

- [x] **88** — Matching question server-side nearest-feature comparison: `RULES.md` §Asking Questions category 1 (Matching) asks "Is your nearest [landmark] the same as mine?" Currently `matching` is just a category name with no server-side computation. When a seeker submits a matching question they should include `matchingFeatureType` in the POST body (one of: `airport`, `train_station`, `bus_station`, `ferry_terminal`, `university`, `hospital`); the server looks up the nearest feature of that type to both the hider's and the seeker's current GPS positions using the OSM Overpass API, then compares them by OSM node ID to determine if they match. Changes needed: (1) `db/schema.sql` — add four idempotent `ALTER TABLE questions ADD COLUMN IF NOT EXISTS` statements: `matching_feature_type TEXT`, `matching_hider_feature_name TEXT`, `matching_seeker_feature_name TEXT`, `matching_features_match BOOLEAN`. (2) `db/gameStore.js` — all three query functions (`dbCreateQuestion`, `dbGetQuestionsForPlayer`, `dbGetQuestionsForGame`) include these four columns. (3) `server/index.js` — add `GET /internal/games/:gameId/matching?seekerId=` HTTP endpoint protected by ADMIN_API_KEY Bearer check; returns `{ hiderLat: number|null, hiderLon: number|null, seekerLat: number|null, seekerLon: number|null }` from `gameStateManager`; 404 when game not found. (4) `functions/questions.js` — define `VALID_FEATURE_TYPES` and `FEATURE_TYPE_QUERIES` (one Overpass clause per type) and `FEATURE_RADIUS_M` (airport 50 km, others 5 km); add `fetchNearestFeature(lat, lon, featureType, fetchFn)` returning `{ id, name }|null`; add `fetchMatchingData({ gameId, seekerId, featureType }, gameServerUrl, adminApiKey, fetchFn)` (same fire-and-forget-on-failure pattern as `fetchTransitData`) that calls `/internal/games/:gameId/matching` to get both positions, calls `fetchNearestFeature` for each, compares OSM node IDs; when `category === 'matching'` and `matchingFeatureType` is provided, calls it and stores `matchingFeatureType`, `matchingHiderFeatureName`, `matchingSeekerFeatureName`, `matchingFeaturesMatch` in DB and in-process store; returns matching fields in all `GET /questions` responses. Tests: `server/server.test.js` (matching endpoint returns `{ hiderLat, hiderLon, seekerLat, seekerLon }`; 401 without token; 404 for unknown game; null positions when players have no location); `functions/questions.test.js` (matching question fetches positions then Overpass twice, stores feature names and match status; match vs. no-match by node ID; non-matching category skips fetch; missing positions return nulls; Overpass failure returns nulls; pool path passes matching columns to INSERT); `db/gameStore.test.js` (dbCreateQuestion accepts matching columns; dbGetQuestionsForPlayer returns them; dbGetQuestionsForGame returns them).

- [x] **89** — Matching question frontend UI: Task 88 stores `matchingFeatureType`, `matchingHiderFeatureName`, `matchingSeekerFeatureName`, and `matchingFeaturesMatch` in the DB and returns them via `GET /questions?playerId=`, but the AnswerPanel shows nothing about the feature comparison. Changes needed: (1) `src/components/AnswerPanel.jsx` — for questions with `category === 'matching'`, display a hint element `<p data-testid="matching-hint">` below the question text; derive text: `featuresMatch === true` → `"Matching hint: same ${featureType} — both nearest to ${hiderFeatureName}"`; `featuresMatch === false` → `"Matching hint: different ${featureType} — hider: ${hiderFeatureName}, seeker: ${seekerFeatureName}"`; either null → `"Matching hint: unknown — position unavailable"`. Do not show the hint for non-matching categories. (2) `src/components/QuestionPanel.jsx` — when `category === 'matching'`, show a labelled select input below the question text field for "Feature type" with options for each valid feature type (`airport`, `train_station`, `bus_station`, `ferry_terminal`, `university`, `hospital`); pass as `matchingFeatureType` in the `submitQuestion` API call body (omit when category is not matching). (3) `src/components/QA.test.jsx` — 6 tests: (a) matching with `featuresMatch=true` renders "same [featureType]" hint with hider name; (b) `featuresMatch=false` renders "different [featureType]" hint with both names; (c) null match renders "unknown" hint; (d) non-matching question does not render matching hint; (e) matching category renders feature type select in QuestionPanel; (f) matching submit includes matchingFeatureType in API call.

### Phase 32 — Transit Question Mechanics

- [x] **86** — Transit question server-side nearest-station lookup: `RULES.md` §Asking Questions category 3 (Transit) asks "Is your nearest transit station on my route?" Currently `transit` is just a category name with no server-side computation. When a seeker submits a transit question the server should look up the nearest transit station to the hider's current GPS position using the OSM Overpass API and store it so the hider's AnswerPanel can show a factual hint. Changes needed: (1) `db/schema.sql` — add four idempotent `ALTER TABLE questions ADD COLUMN IF NOT EXISTS` statements: `transit_nearest_station_name TEXT`, `transit_nearest_station_lat FLOAT`, `transit_nearest_station_lon FLOAT`, `transit_nearest_station_distance_km FLOAT`. (2) `db/gameStore.js` — all three query functions (`dbCreateQuestion`, `dbGetQuestionsForPlayer`, `dbGetQuestionsForGame`) include these four columns. (3) `server/index.js` — add `GET /internal/games/:gameId/hider-position` HTTP endpoint protected by ADMIN_API_KEY Bearer check; returns `{ lat: number|null, lon: number|null }` from `gameStateManager`; 404 when game not found. (4) `functions/questions.js` — add `fetchTransitData({ gameId }, gameServerUrl, adminApiKey, fetchFn)` (same fire-and-forget-on-failure pattern as `fetchMeasuringData`); calls `/internal/games/:gameId/hider-position` to get hider's lat/lon, then if available queries Overpass API `around:2000` to find nearby transit stations, picks nearest by haversine distance; when `category === 'transit'`, calls it and stores `transitNearestStationName`, `transitNearestStationLat`, `transitNearestStationLon`, `transitNearestStationDistanceKm` in DB and in-process store; returns transit fields in all `GET /questions` responses. Tests: `server/server.test.js` (hider-position returns `{ lat, lon }`; 401 without token; 404 for unknown game; null lat/lon when hider has no position); `functions/questions.test.js` (transit question fetches hider-position then Overpass, stores nearest station; non-transit skips fetch; missing hider position returns nulls; Overpass failure returns nulls); `db/gameStore.test.js` (dbCreateQuestion accepts transit columns; dbGetQuestionsForPlayer returns them; dbGetQuestionsForGame returns them).

- [x] **87** — Transit question frontend UI: Task 86 stores `transitNearestStationName`, `transitNearestStationLat`, `transitNearestStationLon`, and `transitNearestStationDistanceKm` in the DB and returns them via `GET /questions?playerId=`, but the AnswerPanel shows nothing about the nearest station. Changes needed: (1) `src/components/AnswerPanel.jsx` — for questions with `category === 'transit'`, display a hint element `<p data-testid="transit-hint">` below the question text; derive text: `nearestStationName != null` → `"Transit hint: nearest station is ${nearestStationName} — ${nearestStationDistanceKm.toFixed(2)} km away"`; name null → `"Transit hint: unknown — position unavailable"`. Do not show the hint for non-transit categories. (2) `src/components/QA.test.jsx` — 3 tests: (a) transit question with station data renders "nearest station is [name]" hint with distance; (b) null station name renders "unknown" hint; (c) non-transit question does not render transit hint.

### Phase 31 — Measuring Question Mechanics

- [x] **84** — Measuring question server-side distance comparison: `RULES.md` §Asking Questions category 2 (Measuring) asks "Are they closer/further to something than Seekers?" Currently `measuring` is just a category name with no server-side computation. When a seeker submits a measuring question they should include `measuringTargetLat` and `measuringTargetLon` in the POST body; the server computes both the hider's distance and the seeker's distance to the target landmark, determines which is closer, and stores the result so the hider's AnswerPanel can show a factual hint. Changes needed: (1) `db/schema.sql` — add five idempotent `ALTER TABLE questions ADD COLUMN IF NOT EXISTS` statements: `measuring_target_lat FLOAT`, `measuring_target_lon FLOAT`, `measuring_hider_distance_km FLOAT`, `measuring_seeker_distance_km FLOAT`, `measuring_hider_is_closer BOOLEAN`. (2) `db/gameStore.js` — `dbCreateQuestion` accepts `measuringTargetLat`, `measuringTargetLon`, `measuringHiderDistanceKm`, `measuringSeekerDistanceKm`, `measuringHiderIsCloser`; all three query functions (`dbCreateQuestion`, `dbGetQuestionsForPlayer`, `dbGetQuestionsForGame`) include these five columns. (3) `server/captureDetector.js` — add and export `calculateMeasuring(hiderLat, hiderLon, seekerLat, seekerLon, targetLat, targetLon)` returning `{ hiderDistanceKm: number, seekerDistanceKm: number, hiderIsCloser: boolean }` using `haversineDistance`; return `{ hiderDistanceKm: null, seekerDistanceKm: null, hiderIsCloser: null }` when hider position, seeker position, or target params are missing. (4) `server/index.js` — add `GET /internal/games/:gameId/measuring?seekerId=&targetLat=&targetLon=` HTTP endpoint protected by ADMIN_API_KEY Bearer check; looks up hider position and seeker position via `gameStateManager`, calls `calculateMeasuring`, returns `{ hiderDistanceKm, seekerDistanceKm, hiderIsCloser }`. (5) `functions/questions.js` — add `fetchMeasuringData({ gameId, seekerId, targetLat, targetLon }, ...)` (same fire-and-forget-on-failure pattern as `fetchTentacleData`); when `category === 'measuring'`, extract params from body (including `seekerId` from the question's `asker_id`) and call it; store results in DB and in-process store; return measuring fields in all `GET /questions` responses. Tests: `server/captureDetector.test.js` (calculateMeasuring: hider closer; seeker closer; equal distance → hiderIsCloser false; missing hider returns nulls; missing seeker returns nulls; missing target returns nulls); `server/server.test.js` (measuring endpoint returns result; 401 without token; 404 unknown game; targetLat/targetLon/seekerId params parsed correctly; missing seeker position returns nulls); `functions/questions.test.js` (measuring question fetches endpoint and stores results; non-measuring skips fetch; missing target params return nulls gracefully); `db/gameStore.test.js` (dbCreateQuestion accepts measuring columns; dbGetQuestionsForPlayer returns them; dbGetQuestionsForGame returns them).

- [x] **85** — Measuring question frontend UI: Task 84 stores `measuringHiderDistanceKm`, `measuringSeekerDistanceKm`, and `measuringHiderIsCloser` in the DB and returns them via `GET /questions?playerId=`, but the AnswerPanel shows nothing about the distance comparison. Changes needed: (1) `src/components/AnswerPanel.jsx` — for questions with `category === 'measuring'`, display a hint element `<p data-testid="measuring-hint">` below the question text; derive text: `hiderIsCloser === true` → `"Measuring hint: hider is closer — hider ${hiderDistanceKm.toFixed(2)} km, seeker ${seekerDistanceKm.toFixed(2)} km"`; `hiderIsCloser === false` → `"Measuring hint: seeker is closer — hider ${hiderDistanceKm.toFixed(2)} km, seeker ${seekerDistanceKm.toFixed(2)} km"`; either null → `"Measuring hint: unknown — position unavailable"`. Do not show the hint for non-measuring categories. (2) `src/components/QuestionPanel.jsx` — when `category === 'measuring'`, show two extra labelled inputs below the question text field: "Target latitude" (number, step 0.000001), "Target longitude" (number, step 0.000001); pass these as `measuringTargetLat`, `measuringTargetLon` in the `submitQuestion` API call body (omit when category is not measuring). (3) `src/components/QA.test.jsx` — 6 tests: (a) measuring with `hiderIsCloser=true` renders "hider is closer" hint with distances; (b) `hiderIsCloser=false` renders "seeker is closer" hint; (c) both null renders "unknown" hint; (d) non-measuring question does not render measuring hint; (e) measuring category renders lat/lon inputs in QuestionPanel; (f) measuring submit includes measuring params in API call.

### Phase 30 — Tentacle Question Mechanics

- [x] **82** — Tentacle question server-side radius check: `RULES.md` §Asking Questions category 6 (Tentacle) asks "Ask about nearby locations (e.g., within X km)". Currently `tentacle` is just a category name with no server-side computation. When a seeker submits a tentacle question they should include `tentacleTargetLat`, `tentacleTargetLon`, and `tentacleRadiusKm` in the POST body; the server computes whether the hider is currently within that radius and stores the result so the hider's AnswerPanel can show a factual hint. Changes needed: (1) `db/schema.sql` — add five idempotent `ALTER TABLE questions ADD COLUMN IF NOT EXISTS` statements: `tentacle_target_lat FLOAT`, `tentacle_target_lon FLOAT`, `tentacle_radius_km FLOAT`, `tentacle_distance_km FLOAT`, `tentacle_within_radius BOOLEAN`. (2) `db/gameStore.js` — `dbCreateQuestion` accepts `tentacleTargetLat`, `tentacleTargetLon`, `tentacleRadiusKm`, `tentacleDistanceKm`, `tentacleWithinRadius`; all three query functions (`dbCreateQuestion`, `dbGetQuestionsForPlayer`, `dbGetQuestionsForGame`) include these five columns. (3) `server/captureDetector.js` — add and export `calculateTentacle(hiderLat, hiderLon, targetLat, targetLon, radiusKm)` returning `{ withinRadius: boolean, distanceKm: number }` using `haversineDistance`; return `{ withinRadius: null, distanceKm: null }` when hider position or target params are missing. (4) `server/index.js` — add `GET /internal/games/:gameId/tentacle?targetLat=&targetLon=&radiusKm=` HTTP endpoint protected by ADMIN_API_KEY Bearer check; looks up hider position via `gameStateManager`, calls `calculateTentacle`, returns `{ withinRadius, distanceKm }`. (5) `functions/questions.js` — add `fetchTentacleData({ gameId, targetLat, targetLon, radiusKm }, ...)` (same fire-and-forget-on-failure pattern as `fetchThermometerData`); when `category === 'tentacle'`, extract params from body and call it; store `distanceKm` and `withinRadius` in DB and in-process store; return tentacle fields in all `GET /questions` responses. Tests: `server/captureDetector.test.js` (calculateTentacle: within radius; outside radius; missing hider; missing target returns nulls); `server/server.test.js` (tentacle endpoint returns result; 401 without token; 404 unknown game; radiusKm/targetLat/targetLon params parsed correctly); `functions/questions.test.js` (tentacle question fetches endpoint and stores distanceKm + withinRadius; non-tentacle skips fetch; missing params return nulls gracefully); `db/gameStore.test.js` (dbCreateQuestion accepts tentacle columns; dbGetQuestionsForPlayer returns them; dbGetQuestionsForGame returns them).

- [x] **83** — Tentacle question frontend UI: Task 82 stores `tentacleDistanceKm` and `tentacleWithinRadius` in the DB and returns them via `GET /questions?playerId=`, but the AnswerPanel shows nothing about tentacle proximity. Changes needed: (1) `src/components/AnswerPanel.jsx` — for questions with `category === 'tentacle'`, display a hint element `<p data-testid="tentacle-hint">` below the question text; derive text: `withinRadius === true` → `"Tentacle hint: within radius — ${distanceKm.toFixed(2)} km away"`; `withinRadius === false` → `"Tentacle hint: outside radius — ${distanceKm.toFixed(2)} km away"`; either null → `"Tentacle hint: unknown — position unavailable"`. Do not show the hint for non-tentacle categories. (2) `src/components/QuestionPanel.jsx` — when `category === 'tentacle'`, show three extra labelled inputs below the question text field: "Target latitude" (number, step 0.000001), "Target longitude" (number, step 0.000001), "Radius (km)" (number, min 0.1, step 0.1); pass these as `tentacleTargetLat`, `tentacleTargetLon`, `tentacleRadiusKm` in the `submitQuestion` API call body (omit when category is not tentacle). (3) `src/components/QA.test.jsx` — 6 tests: (a) tentacle with `withinRadius=true` renders "within radius" hint; (b) `withinRadius=false` renders "outside radius" hint; (c) both null renders "unknown" hint; (d) non-tentacle question does not render tentacle hint; (e) tentacle category renders lat/lon/radius inputs in QuestionPanel; (f) tentacle submit includes tentacle params in API call.

### Phase 29 — Thermometer UX

- [x] **81** — Thermometer hint display in AnswerPanel: Task 80 stores `thermometerCurrentDistanceM` and `thermometerPreviousDistanceM` in the DB and returns them via `GET /questions?playerId=`, but `src/components/AnswerPanel.jsx` ignores these fields. The hider cannot know whether they moved warmer/colder/same relative to the seeker when the question was asked. Changes needed: (1) `src/components/AnswerPanel.jsx` — for questions with `category === 'thermometer'`, derive the result from the two distance fields: `current < previous` → `'warmer'`; `current > previous` → `'colder'`; `current === previous` → `'same'`; either `null` → `'unknown'`. Display a hint element (e.g. `<p data-testid="thermometer-hint">Thermometer hint: warmer — you moved closer</p>`) below the question text; use role-appropriate descriptions ("warmer — you moved closer", "colder — you moved further away", "same — no distance change", "unknown — position unavailable"). Do not show the hint for non-thermometer categories. (2) `src/components/QA.test.jsx` — 5 tests: (a) thermometer question with `currentDistanceM < previousDistanceM` renders "warmer" hint; (b) `current > previous` renders "colder" hint; (c) `current === previous` renders "same" hint; (d) both null renders "unknown" hint; (e) non-thermometer question does not render a thermometer hint element.

### Phase 28 — Seeker Proximity UX

- [x] **79** — Spot rejection distance feedback: when a seeker taps "I See the Hider!" but is too far away, `server/wsHandler.js` already sends `spot_rejected` with `distanceM` and `spotRadiusM`, but `src/components/GameMap.jsx` ignores these fields and shows only the generic "You are not close enough to the hider yet." Changes needed: (1) `src/components/GameMap.jsx` — add `spotDistance` state `{ distanceM: null, spotRadiusM: null }`; in the `spot_rejected` WS handler, populate it from `msg.distanceM` and `msg.spotRadiusM`; reset to `{ distanceM: null, spotRadiusM: null }` when a new spot attempt starts (when `setSpotResult('pending')`); in the rejection message span, show `"You are ${Math.round(distanceM)} m away; need to be within ${spotRadiusM} m"` when `spotDistance.distanceM != null`, otherwise fall back to the existing generic text. Tests: `src/components/GameMap.test.jsx` — (a) update existing spot_rejected test to assert distance-aware text "You are 80 m away; need to be within 30 m" (fixture already sends `distanceM: 80, spotRadiusM: 30`); (b) new test: spot_rejected with `distanceM: null` shows generic fallback "You are not close enough to the hider yet."; (c) new test: clicking the button again after rejection resets spotDistance so a stale distance is not shown while pending.

- [x] **80** — Thermometer question server-side position tracking: `RULES.md` §Asking Questions category 5 requires thermometer questions to tell seekers whether they are "warmer" (closer to the hider) or "colder" (farther). Currently the server accepts `thermometer` as a valid question category but provides no position-delta calculation. Changes needed: (1) `server/gameState.js` — in `updatePlayerLocation`, before writing the new lat/lon, save the old position as `previousLocation` in the player entry; add `getPreviousPlayerLocation(gameId, playerId)` returning `{ lat, lon } | null`. (2) `server/captureDetector.js` — add and export `calculateThermometer(gameState, seekerId, hiderLat, hiderLon)` returning `{ result: 'warmer' | 'colder' | 'same', currentDistanceM, previousDistanceM }` using `haversineDistance`; return `{ result: 'unknown' }` when either position is missing. (3) `server/index.js` — add `GET /internal/games/:gameId/thermometer?seekerId=` HTTP endpoint, protected by `ADMIN_API_KEY` Bearer check, that looks up hider position via `gameStateManager` and calls `calculateThermometer`; returns JSON `{ result, currentDistanceM, previousDistanceM }`. (4) `functions/questions.js` — when a question with `category: 'thermometer'` is submitted, fire-and-forget a fetch to the managed server's thermometer endpoint and store the numeric `currentDistanceM` and `previousDistanceM` in two new nullable columns on the `questions` table; include these fields when returning the question to the hider via `GET /questions` so the frontend AnswerPanel can pre-fill or hint the correct answer. (5) `db/schema.sql` — add idempotent `ALTER TABLE questions ADD COLUMN IF NOT EXISTS thermometer_current_distance_m FLOAT; ALTER TABLE questions ADD COLUMN IF NOT EXISTS thermometer_previous_distance_m FLOAT;`. Tests: `server/captureDetector.test.js` (calculateThermometer warmer/colder/same/unknown); `server/server.test.js` (thermometer endpoint returns result; 401 without token; 404 on unknown game); `functions/questions.test.js` (thermometer question fetches endpoint and stores distances; non-thermometer categories skip fetch); `db/gameStore.test.js` (dbCreateQuestion accepts thermometer columns; dbGetQuestion returns them).

### Phase 27 — End Game Correctness

- [x] **78** — Hider movement-locked server notification and End Game state cleanup: Two related gaps: (1) When the hider sends `location_update` during End Game, `server/wsHandler.js` silently discards it but never tells the hider why. Send `{ type: 'movement_locked', code: 'END_GAME_ACTIVE', message: 'You cannot move during End Game' }` to the hider's socket before returning, so the client receives explicit feedback. (2) `src/components/GameMap.jsx` — `endGameActive` state and `endGameActiveRef` are set to `true` on `end_game_started` but never reset to `false`, so the End Game banner persists even after the game transitions to `finished`. Fix: in the `phase_change` handler, when `msg.phase === 'finished'` or `msg.phase === 'hiding'` set `endGameActive(false)` and `endGameActiveRef.current = false`. Also handle the `movement_locked` message with `code: 'END_GAME_ACTIVE'` by showing a brief banner (or rely on the existing End Game banner already shown). Tests: `server/wsHandler.test.js` (hider location_update during end_game sends movement_locked to hider; seeker during end_game is unaffected — no movement_locked); `src/components/GameMap.test.jsx` (movement_locked with END_GAME_ACTIVE renders banner; endGameActive resets to false on phase_change → finished; endGameActive resets to false on phase_change → hiding).

### Phase 24 — Rules Enforcement

- [x] **77** — Hider out-of-zone warning during seeking phase: `RULES.md` §Hiding Rules rule 4 states "At end of hiding period, Hider must remain within their hiding zone." Nothing currently enforces or signals this. Changes needed: (1) `server/gameState.js` — add `getGameStatus(gameId)` returning `this._games.get(gameId)?.status ?? null` for efficient phase checks. (2) `server/wsHandler.js` — in `_handleLocationUpdate`, after updating and echoing the hider's position, if the game status is `'seeking'` and the game has zones, check whether the hider's lat/lon is inside any zone (`haversineDistance` from `captureDetector.js`). If outside all zones, send `{ type: 'zone_warning', code: 'HIDER_OUT_OF_ZONE', message: 'You are outside your hiding zone' }` to the hider's own socket, and broadcast `{ type: 'hider_out_of_zone', gameId }` to all players in the game. (3) `src/components/GameMap.jsx` — add `outOfZone` state (boolean); handle `zone_warning` with `code: 'HIDER_OUT_OF_ZONE'` by setting `outOfZone = true`; reset to `false` on `phase_change`; render a warning banner to the hider when `outOfZone` is true. Handle `hider_out_of_zone` by setting a `hiderOutOfZone` boolean state and rendering a seeker notification. Tests: `server/wsHandler.test.js` (hider in zone during seeking → no warning; hider outside zone during seeking → zone_warning to hider + hider_out_of_zone broadcast; wrong phase → no warning; seeker → no warning; no zones → no warning); `src/components/GameMap.test.jsx` (zone_warning renders hider banner; hider_out_of_zone renders seeker notification; phase_change resets hider banner).

---

## Completed Tasks Log

| # | Date | Task | Files | Notes |
|---|------|------|-------|-------|
| 168 | 2026-03-22 | Card-drawn toast notification for hider | src/components/gameUtils.js, src/components/CardPanel.jsx, src/components/GameMap.jsx, src/components/GameMap.test.jsx, spec/TASKS.md | CARD_LABELS + CARD_DESCRIPTIONS extracted to gameUtils (no duplication); CardPanel imports from gameUtils; GameMap: drawnCardToast state + setDrawnCardToast in card_drawn handler + auto-dismiss useEffect (CARD_TOAST_DURATION_MS=4000) + infoBanner toast render for hider; 4 new GameMap tests; 1754 tests pass, build clean |
| 165 | 2026-03-22 | Live hider elapsed-time display | src/components/gameUtils.js, src/components/ResultsScreen.jsx, src/components/GameMap.jsx, src/components/GameMap.module.css, src/components/gameUtils.test.js, src/components/GameMap.test.jsx, spec/TASKS.md | formatDuration extracted from ResultsScreen to gameUtils (no duplication); GameMap imports formatDuration; elapsed timer derived inline from hidingStartedAtRef + bonusSecondsRef using existing timerTick re-renders (no new interval); .elapsedTimer CSS class with JetBrains Mono font; shown for hider only during hiding/seeking phases after phase_change sets the ref; 4 new gameUtils tests + 4 new GameMap tests; 1739 tests pass, build clean |
| 164 | 2026-03-22 | ZoneSelector station search filter | src/components/ZoneSelector.jsx, src/components/ZoneSelector.module.css, src/components/ZoneSelector.test.jsx, spec/TASKS.md | SEARCH_PLACEHOLDER constant; searchQuery state; filteredZones computed from trimmed query; search input with aria-label above zone list; "No stations match your search." message when no results; search input hidden after lock; 5 new tests; 1731 tests pass, build clean |
| 163 | 2026-03-22 | Zone privacy: zone_locked private to hider; seeker sees zone on end_game_started | server/wsHandler.js, server/index.js, src/components/GameMap.jsx, server/server.test.js, src/components/GameMap.test.jsx, spec/TASKS.md | Added sendToPlayer() to WsHandler; zone_locked notify sends only to hider (lockedBy); end_game_started broadcast includes zone; GameMap setLockedZone on end_game_started for seekers; 6 new tests (3 server, 3 frontend); 1726 tests pass, build clean |
| 162 | 2026-03-21 | Player names on map markers | src/components/GameMap.jsx, server/wsHandler.js, server/wsHandler.test.js, server/server.test.js, src/components/GameMap.test.jsx, spec/TASKS.md | join_game now sends name; _playerNames Map in WsHandler stores it; player_joined and game_state_sync broadcast name; playerNamesRef in GameMap populated from both events; marker tooltips show name instead of playerId; 3 new GameMap tests, 3 new wsHandler tests, 2 updated server.test.js/wsHandler.test.js assertions; 1720 tests pass, build clean |
| 161 | 2026-03-21 | Gate spot_hider on End Game active + seeker off transit | server/captureDetector.js, server/wsHandler.js, src/components/GameMap.jsx, server/captureDetector.test.js, server/wsHandler.test.js, src/components/GameMap.test.jsx, spec/TASKS.md | checkSpot returns on_transit reason when spotter.onTransit; _handleSpotHider pre-check for isEndGameActive; spot button hidden unless endGameActive; disabled with SPOT_ON_TRANSIT_LABEL when myOnTransit; 8 new tests; 1714 tests pass, build clean |
| 157 | 2026-03-21 | Disable answer submission after question deadline expires in client UI | src/components/AnswerPanel.jsx, src/components/AnswerPanel.module.css, src/components/QA.test.jsx, spec/TASKS.md | Converted pending.map implicit-return to block-body arrow so isExpired = expiryCounts[q.questionId] === '0:00' can be derived cleanly; Submit button disabled+aria-disabled when isExpired; label priority: Sending… > Expired > Submit answer; .expiredBtn CSS (opacity 0.5, cursor not-allowed); 5 new tests; 1685 tests pass, build clean |
| 153 | 2026-03-21 | Fix SERVER_URL to use ENV.wsUrl in Lobby.jsx | src/components/Lobby.jsx, src/components/Lobby.test.jsx, spec/TASKS.md | Replaced VITE_GAME_SERVER_URL (undefined env var) with ENV.wsUrl (single source of truth in config/env.js); GameMap mock changed to vi.fn() to allow prop inspection; added test asserting serverUrl prop matches ENV.wsUrl; 1646 tests pass, build clean |
| 152 | 2026-03-20 | Architecture review cleanup (task 150 periodic review) | src/components/QuestionPanel.jsx, spec/TASKS.md | Added COUNTDOWN_TICK_MS=1_000 constant replacing magic 1000 literal; replaced curseCountdown inline IIFE with formatCountdown(curseEndsAt) reusing shared gameUtils.js helper; 1645 tests pass, build clean |
| 147 | 2026-03-20 | Copy invite link to clipboard in WaitingRoom | src/components/WaitingRoom.jsx, src/components/WaitingRoom.module.css, src/components/WaitingRoom.test.jsx, spec/TASKS.md | CLIPBOARD_RESET_MS=2_000 constant; copied state + copyTimerRef; handleCopyLink async fn calls navigator.clipboard.writeText, sets copied=true, resets after 2s; cleanup useEffect clears timer on unmount; Copy Link button (aria-label, 44px min touch target, CSS token colours); .inviteRow flex layout; 5 new tests; 1638 tests pass, build clean |
| 146 | 2026-03-20 | Lazy-load non-critical views | src/components/Lobby.jsx, src/components/GameMap.jsx, src/components/Lobby.test.jsx | Leaderboard + AdminDashboard lazy-loaded in Lobby.jsx; CardPanel + ResultsScreen lazy-loaded in GameMap.jsx; each wrapped in Suspense fallback=null; admin dashboard test updated to findByLabelText (async); build emits 4 separate JS chunks; main bundle shrinks from 353 kB to 347 kB; 1633 tests pass, build clean |
| 145 | 2026-03-20 | Manual bounds fields sync preview map | src/components/GameForm.jsx, GameForm.test.jsx | setBound now computes center+radius from midpoint/half-shorter-dimension when all four fields are valid; preview map and Zone radius output update on manual field edits; 3 new tests; 1633 tests pass, build clean |
| 144 | 2026-03-20 | Draggable zone radius resize | src/components/gameUtils.js, gameUtils.test.js (new), GameForm.jsx, GameForm.module.css, Lobby.test.jsx | haversineKm + lonDeltaDeg added to gameUtils.js; resize handle Marker at east edge of zone circle; drag/dragend handlers update radiusKm + bounds; <output> shows "Zone radius: X.X km"; keyboard={false} keeps handle out of tab order; 6 new tests (a–f); 1630 tests pass, build clean |
| 142 | 2026-03-20 | Geocoding search in GameForm (Nominatim) | src/components/GameForm.jsx, GameForm.module.css, gameUtils.js, GameForm.test.jsx | Changed <li> to <button role="option"> for reliable test interaction; scoped getAllByRole with within(); removed fake timers (userEvent v14 deadlock); 11 tests; 1621 tests pass, build clean |
| 138 | 2026-03-20 | React Error Boundary | src/components/ErrorBoundary.jsx (new), src/components/ErrorBoundary.module.css (new), src/components/ErrorBoundary.test.jsx (new), src/App.jsx | Class-based Error Boundary wrapping Lobby in App.jsx; getDerivedStateFromError + componentDidCatch; fallback shows "Something went wrong" + Reload button; onError callback prop for tests; 4 tests; 1600 tests pass, build clean |
| 119 | 2026-03-20 | Accessibility hardening pass | src/components/GameMap.jsx, src/components/ZoneSelector.jsx, src/components/ZoneSelector.module.css, src/components/Lobby.jsx, src/components/CardPanel.test.jsx, src/components/ZoneSelector.test.jsx | Added type="button" to spot button, transit toggle, zone list buttons, confirm/cancel in ZoneSelector, and Lobby leaderboard toggle; added aria-live="polite" status region to ZoneSelector for screen reader announcements; CardPanel aria-label already correct; no outline:none violations found; 3 new tests (unique aria-labels, live region update, live region clear); 1554 tests pass, build clean |
| 118 | 2026-03-20 | PWA icons and manifest refresh | public/icon-192.svg, public/icon-512.svg, public/manifest.json, src/pwa.test.js | Replaced placeholder icons with JetLag brand design: dark navy circle, five concentric sunset semicircle arcs (bottom half), airplane silhouette; manifest theme_color and background_color updated to #1B2A3A; 11 new pwa.test.js tests (manifest fields, SVG validity, viewBox, brand colour); 1551 tests pass; build clean |
| 115 | 2026-03-19 | CardPanel and Q&A styles | src/components/CardPanel.module.css (new), src/components/CardPanel.jsx, src/components/QuestionPanel.module.css (new), src/components/QuestionPanel.jsx, src/components/AnswerPanel.module.css (new), src/components/AnswerPanel.jsx | CardPanel: .panel/.hand/.card/.cardType/.playBtn/.confirmation CSS module; removed CARD_COLORS hex dict; refactored to li.card + span.cardType + small + button.playBtn; imported Alert for errors. QuestionPanel: .history/.historyItem/.category/.hint applied. AnswerPanel: .pendingQuestion/.hintText applied; 1541 tests pass, build clean |
| 109 | 2026-03-19 | Panel utility class and section spacing | src/index.css, src/components/WaitingRoom.jsx, src/components/PlayerForm.jsx, src/components/GameForm.jsx | Added .panel utility (surface bg, border, 6px radius, padding, box-shadow) and .section-gap utility (flex column, 1.5rem gap) to index.css; applied className="panel" to root elements of WaitingRoom, PlayerForm, GameForm; 1534 tests pass, build clean |
| 108 | 2026-03-19 | Global input and select styles | src/index.css | Added global styles for input/select/textarea with surface bg, border, radius, padding, color, font, 100% width; :focus outline accent; ::placeholder secondary colour; radio/checkbox width:auto with accent-color; pure CSS — 1534 tests pass, build clean |
| 107 | 2026-03-19 | Button component | src/components/Button.jsx (new), src/components/Button.module.css (new), src/components/Button.test.jsx (new) | Created Button with primary/ghost/danger variants; 44px min-height; press animation scale(0.97); focus-visible ring; disabled opacity 0.45; 11 tests pass |
| 106 | 2026-03-19 | Page layout shell | src/App.module.css (new), src/App.jsx, src/App.test.jsx | Created App.module.css with .root max-width 480px centred layout and 600px breakpoint; wrapped Lobby in styled div; updated App.test.jsx to mock Lobby and assert heading presence; 1523 tests pass, build clean |
| 105 | 2026-03-19 | Typography scale | src/index.css | Added h1/h2/h3 Oswald headings, body line-height 1.5, code/pre/kbd JetBrains Mono, p margin, a colour and hover; pure CSS — 1523 tests pass, build clean |
| 104 | 2026-03-19 | CSS token foundation and global base styles | src/index.css (new), index.html, src/main.jsx | Created src/index.css with all 15 colour tokens from DESIGN.md §22, box-sizing reset, body base styles; updated index.html with theme-color #1B2A3A and Google Fonts preconnect/stylesheet links; added import './index.css' as first import in main.jsx; 1523 tests pass, build clean |
| 131 | 2026-03-19 | Game zones single-zone model | server/gameState.js, server/index.js, server/gameState.test.js, server/server.test.js, functions/gameZone.js | Renamed zones[] array to zone (singular) in game entry; replaced setGameZones with setGameZone(gameId, zone) accepting a single object; getGameZones returns [zone] or [] for backward compat; endpoint updated to extract zones[0]; false-zone decoy code adds explicit comment; updated 12 setGameZones call sites in server.test.js; 5 new tests; 1523 tests pass |
| 130 | 2026-03-19 | Game start silent failure fix | functions/games.js, functions/games.test.js | Changed notifyGameStart from fire-and-forget to async awaited call; handleStartGame wraps in try/catch returning 503 on network failure or non-2xx response; updated 6 existing test mocks to { ok: true }; added 3 new tests (503 on throw, 503 on non-2xx, 204 on success); 1518 tests pass |
| 129 | 2026-03-19 | Photo upload client-side size pre-check | src/components/AnswerPanel.jsx, src/components/QA.test.jsx | Added MAX_PHOTO_BYTES=500_000 constant; handlePhotoChange returns with error "Photo must be under 500 KB" before FileReader when file.size exceeds limit; 2 new tests (small file proceeds, large file blocks and upload skipped); 1516 tests pass |
| 126 | 2026-03-19 | End Game timeout configurable via env | config/env.js, server/index.js, .env.example, server/server.test.js | Added number() helper to env.js; ENV.endGameTimeoutMs reads END_GAME_TIMEOUT_MS (default 600_000); removed DEFAULT_END_GAME_TIMEOUT_MS constant; added ENV import to server/index.js; .env.example documents new var; 1 new test (custom timeout used for End Game timer); 1514 tests pass |
| 124 | 2026-03-19 | SCALE_DURATION_RANGES deduplication | config/gameRules.js (new), functions/games.js, server/index.js | Extracted SCALE_DURATION_RANGES and SCALE_DURATIONS to config/gameRules.js; both files import from single source; re-export from each module preserves existing test imports; 1513 tests pass |
| 125 | 2026-03-19 | DB performance indices | db/schema.sql, db/db.js, db/db.test.js | Added 5 idempotent CREATE INDEX IF NOT EXISTS statements for questions, cards, scores, game_players; 5 new SCHEMA_SQL tests; 1513 tests pass |
| 123 | 2026-03-19 | API fetch timeout | src/api.js, src/api.test.js | Added FETCH_TIMEOUT_MS=10_000, fetchWithTimeout helper using AbortController; replaced all 12 fetch() calls; 2 new tests (timeout throws, success clears timer); 1508 tests pass |
| 122 | 2026-03-19 | QuestionPanel team filter fix | src/components/QuestionPanel.jsx, src/components/GameMap.jsx, src/components/QA.test.jsx | Added teamId prop to QuestionPanel; replaced game.teamId with teamId prop; GameMap passes myTeam as teamId; updated 2 existing tests and added 1 new test; 1506 tests pass |
| 121 | 2026-03-19 | Leaderboard bonus seconds | src/components/Leaderboard.jsx, src/components/Leaderboard.test.jsx | Added fmtBonus helper; Score column shows scoreSeconds+bonusSeconds total; Bonus column shows +Xm when bonusSeconds>0; sorted client-side by total descending; updated column headers and MM:SS test; added 3 new tests; 1505 tests pass |
| 120 | 2026-03-19 | phase_change property name mismatch | src/components/GameMap.jsx, src/components/GameMap.test.jsx | Fixed setPhase(msg.phase) → setPhase(msg.newPhase); removed redundant || msg.phase guards on hiding/finished branches; updated 10 existing tests to send newPhase instead of phase; added 4 new tests (newPhase reflected in phase display, ZoneSelector after hiding transition, spot-hider button after seeking transition, results overlay after finished transition); 1502 tests pass; build clean |
| 103 | 2026-03-19 | Non-host player game start detection | src/components/WaitingRoom.jsx, src/components/Lobby.jsx, src/components/WaitingRoom.test.jsx, src/components/Lobby.test.jsx | WaitingRoom polls lookupGame every 3 s when onStart absent; onGameStarted fires on status !== 'waiting'; Lobby passes onGameStarted to all players; handleStart also calls onGameStarted; 6 new tests; 1498 tests pass |
| 102 | 2026-03-18 | Host player tracking | db/schema.sql, db/gameStore.js, functions/games.js, src/api.js, src/components/GameForm.jsx, src/components/Lobby.jsx + tests | host_player_id column (idempotent migration); dbCreateGame/dbGetGame updated; handleCreateGame reads playerId; createGame stores hostPlayerId in-process; api.js sends playerId; GameForm passes player.playerId; Lobby gates onStart to host only; 7 new tests; 1492 tests pass |
| 101 | 2026-03-18 | Hider zone requirement before game start | functions/games.js, server/index.js, db/gameStore.test.js, functions/games.test.js, server/server.test.js | dbGetGameZone already present; handleStartGame checks zone after player counts; server start endpoint checks getGameZones().length > 0; seedPlayers updated with zone; 5 new tests; 1484 tests pass |
| 100 | 2026-03-18 | Stale game cleanup | db/gameStore.js, functions/games.js, functions/router.js, db/gameStore.test.js, functions/games.test.js | dbCleanupStaleGames DELETE waiting games by age; cleanupStaleGames handler with admin-auth; POST /games/cleanup route; 13 new tests; 1479 tests pass |
| 99 | 2026-03-18 | Container health check endpoint | server/index.js, Dockerfile, server/server.test.js | GET /health returns { status, uptimeMs, activeGames, connections }; HEALTHCHECK instruction in Dockerfile; no auth required; 5 new tests; 1466 tests pass |
| 98 | 2026-03-18 | Minimum player count validation before game start | db/gameStore.js, server/gameState.js, server/index.js, functions/games.js, src/api.js + tests | dbGetGamePlayerCounts added; getPlayerCounts on GameStateManager; server start endpoint returns 400 insufficient_players; handleStartGame async with DB check; api.js parses error body; 13 new tests; 1461 tests pass |
| 97 | 2026-03-18 | Expired question answer rejection | db/gameStore.js, functions/questions.js, db/gameStore.test.js, functions/questions.test.js | dbSubmitAnswer checks status and returns { questionExpired: true } for non-pending questions; submitAnswer returns 409 question_expired for DB and in-process paths; _setQuestionStatus test helper; 7 new tests; 1448 tests pass |
| 96 | 2026-03-18 | Reconnect game state sync | server/wsHandler.js, server/index.js, src/components/GameMap.jsx + tests | WsHandler accepts gameLoopManager param; _handleJoinGame sends game_state_sync after joined_game with phase/zones/endGameActive; GameMap handles game_state_sync updating phase/syncedZones/endGameActive without clearing locationTrail; 10 new tests; 1441 tests pass |
| 95 | 2026-03-18 | Photo upload size limit | functions/questions.js, db/schema.sql, functions/questions.test.js | MAX_PHOTO_BYTES=512_000 constant; uploadQuestionPhoto returns 413 when base64 length exceeds ceil(512000*4/3); schema comment updated; 3 new tests; 1431 tests pass |
| 94 | 2026-03-18 | Player location bounds validation | server/gameState.js, server/wsHandler.js, src/components/GameMap.jsx + tests | setGameBounds/getGameBounds; join_game stores bounds; location_update outside bounds sends location_rejected/OUT_OF_BOUNDS; GameMap shows dismissible banner; 16 new tests; 1428 tests pass |
| 93 | 2026-03-18 | Two-team question history scoping | db/gameStore.js, functions/questions.js, src/api.js, src/components/QuestionPanel.jsx, db/gameStore.test.js, functions/questions.test.js, src/components/QA.test.jsx | dbGetQuestionsForGame accepts optional teamId with game_players JOIN; listQuestions passes teamId to DB path and filters via _teamMemberships in-process; QuestionPanel passes game.teamId; api.js forwards teamId; 9 new tests; 1412 tests pass |
| 92 | 2026-03-18 | Card draw WS notification to hider | functions/questions.js, src/components/GameMap.jsx, functions/questions.test.js, src/components/GameMap.test.jsx | card_drawn notify fires after draw (both paths); GameMap increments cardRefresh on card_drawn for own playerId; CardPanel uses dedicated cardRefresh trigger; 4 new tests; 1403 tests pass |
| 91 | 2026-03-18 | Computed hints in seeker question history | src/components/questionHints.js, src/components/AnswerPanel.jsx, src/components/QuestionPanel.jsx, src/components/QA.test.jsx | 5 hint functions extracted to questionHints.js; AnswerPanel imports from shared module; QuestionPanel history renders thermometer/tentacle/measuring/transit/matching hints for answered questions only; 6 new tests; 1399 tests pass |
| 90 | 2026-03-18 | Photo display in question history | src/components/QuestionPanel.jsx, src/components/QA.test.jsx | fetchQuestionPhoto called for answered photo questions in history; fetchedPhotosRef prevents duplicate fetches; photoCache state drives img/loading/no-photo rendering; 4 new tests; 1393 tests pass |
| 89 | 2026-03-18 | Matching question frontend UI | src/components/AnswerPanel.jsx, src/components/QuestionPanel.jsx, src/components/QA.test.jsx | matchingHint() derives same/different/unknown from stored fields; hint shown below question text for matching category; QuestionPanel shows feature type select when category=matching; matchingFeatureType passed in submitQuestion call; 6 new tests; 1389 tests pass |
| 88 | 2026-03-18 | Matching question server-side nearest-feature comparison | db/schema.sql, db/gameStore.js, server/index.js, functions/questions.js + tests | fetchNearestFeature() queries Overpass per feature type; fetchMatchingData() compares hider/seeker nearest features by OSM node ID; GET /internal/games/:gameId/matching endpoint with Bearer auth; 4 matching columns on questions table (idempotent); in-process and DB paths; 13 new tests; 1383 tests pass |
| 128 | 2026-03-19 | Log silent failures for hint fetches and fire-and-forget notifies | functions/questions.js, functions/cards.js, functions/games.js | Added console.error with [questions]/[cards]/[games] prefix to all hint-fetch catch blocks (tentacle, measuring, transit, matching, thermometer) and all fire-and-forget notify catches; 1514 tests pass |
| 87 | 2026-03-18 | Transit question frontend UI | src/components/AnswerPanel.jsx, src/components/QA.test.jsx | transitHint() derives nearest-station/unknown from stored fields; hint shown below question text for transit category; 3 new tests; 1370 tests pass |
| 86 | 2026-03-18 | Transit question server-side nearest-station lookup | db/schema.sql, db/gameStore.js, server/index.js, functions/questions.js + tests | fetchTransitData() calls hider-position endpoint then Overpass API; 4 transit columns on questions table (idempotent); GET /internal/games/:gameId/hider-position endpoint with Bearer auth; in-process and DB paths; 11 new tests; 1367 tests pass |
| 85 | 2026-03-18 | Measuring question frontend UI | src/components/AnswerPanel.jsx, src/components/QuestionPanel.jsx, src/components/QA.test.jsx | measuringHint() derives hider-closer/seeker-closer/unknown from stored fields; hint shown below question text for measuring category; QuestionPanel shows lat/lon inputs when category=measuring; measuring params passed in submitQuestion call; 6 new tests; 1356 tests pass |
| 84 | 2026-03-18 | Measuring question server-side distance comparison | server/captureDetector.js, db/schema.sql, db/gameStore.js, server/index.js, functions/questions.js + tests | calculateMeasuring pure function; 5 measuring columns on questions table (idempotent); GET /internal/games/:gameId/measuring endpoint with Bearer auth; fetchMeasuringData in questions.js; in-process and DB paths; 28 new tests; 1350 tests pass |
| 83 | 2026-03-18 | Tentacle question frontend UI | src/components/AnswerPanel.jsx, src/components/QuestionPanel.jsx, src/components/QA.test.jsx | tentacleHint() derives within/outside/unknown from stored fields; hint shown below question text for tentacle category; QuestionPanel shows lat/lon/radius inputs when category=tentacle; tentacle params passed in submitQuestion call; 6 new tests; 1322 tests pass |
| 82 | 2026-03-18 | Tentacle question server-side radius check | server/captureDetector.js, db/schema.sql, db/gameStore.js, server/index.js, functions/questions.js + tests | calculateTentacle pure function; 5 tentacle columns on questions table (idempotent); GET /internal/games/:gameId/tentacle endpoint with Bearer auth; fetchTentacleData in questions.js; in-process and DB paths; 26 new tests; 1316 tests pass |
| 81 | 2026-03-18 | Thermometer hint display in AnswerPanel | src/components/AnswerPanel.jsx, src/components/QA.test.jsx | thermometerHint() derives warmer/colder/same/unknown from stored distance fields; hint shown below question text for thermometer category; 5 new tests; 1290 tests pass |
| 80 | 2026-03-18 | Thermometer server-side position tracking | server/gameState.js, server/captureDetector.js, server/index.js, functions/questions.js, db/schema.sql, db/gameStore.js + tests | previousLocation in gameState; calculateThermometer (warmer/colder/same/unknown); /internal/games/:gameId/thermometer endpoint; questions.js fetches and stores distances; schema adds two nullable FLOAT columns; 1285 tests pass |
| 79 | 2026-03-13 | Spot rejection distance feedback | src/components/GameMap.jsx, src/components/GameMap.test.jsx | spotDistance state; rejection msg shows "You are Xm away; need to be within Ym"; fallback for null distance; stale distance cleared on re-attempt; 1256 tests pass |
| 78 | 2026-03-13 | Hider movement-locked notification + End Game state fix | server/wsHandler.js, src/components/GameMap.jsx, server/wsHandler.test.js, src/components/GameMap.test.jsx | movement_locked sent to hider on End Game block; endGameActive reset on phase → finished/hiding; movementLocked banner; 1254 tests pass |
| 77 | 2026-03-13 | Hider out-of-zone warning | server/gameState.js, server/wsHandler.js, src/components/GameMap.jsx, server/wsHandler.test.js, src/components/GameMap.test.jsx | getGameStatus added; zone_warning + hider_out_of_zone events; banners with phase reset; 1249 tests pass |
| 136 | 2026-03-20 | Card draw probabilities per question category | config/gameRules.js, functions/cards.js, db/gameStore.js, functions/questions.js, functions/cards.test.js | CARD_DRAW_WEIGHTS + CARD_DRAW_WEIGHTS_DEFAULT in gameRules.js; weightedCardType() in cards.js; randomCardDescriptor accepts optional questionCategory; drawCardInProcess accepts questionCategory; dbSubmitAnswer returns category; submitAnswer passes category in both DB and in-process paths; 1584 tests pass |
| 1 | 2026-03-05 | Initialize project repository | README.md, LICENSE, src/, functions/, docs/, config/ | Git initialized; MIT license; placeholder dirs with .gitkeep |
| 2 | 2026-03-05 | Set up build and deployment tools | package.json, vite.config.js, index.html, src/main.jsx, src/App.jsx, src/App.test.jsx, .gitignore, .github/workflows/ci.yml | React+Vite+Vitest; build and 2 tests pass; CI placeholder added |
| 4 | 2026-03-05 | Define environment configuration | .env.example, config/env.js, config/env.test.js | Typed ENV config module; 16 new tests; build clean; no secrets committed |
| 5 | 2026-03-05 | Set up managed server instances | server/index.js, server/gameLoop.js, server/wsHandler.js, server/server.test.js | HTTP+WebSocket server; GameLoop auto-start/stop on player lifecycle; 22 new tests; build clean |
| 6 | 2026-03-05 | Implement serverless endpoints | functions/players.js, functions/games.js, functions/scores.js, functions/router.js, functions/functions.test.js | Pure handler functions for player registration, game queries, score submission; 30 new tests; build clean |
| 7 | 2026-03-05 | Establish hybrid architecture documentation | docs/ARCHITECTURE.md | Documents serverless vs managed tiers, lifecycle, message flows, config, and decision rationale; build clean; 70 tests pass |
| 8 | 2026-03-05 | Choose database for game state | docs/DATABASE.md, config/env.js, config/env.test.js | Chose Neon serverless Postgres ($0 idle, autoscales to zero); documented rationale; DATABASE_URL config added; 72 tests pass |
| 9 | 2026-03-05 | Implement database schema | db/schema.sql, db/db.js, db/db.test.js, package.json | Tables: players, games, game_players, scores; pg driver installed; createPool + createTables module; 23 new tests; 95 total pass; build clean |
| 10 | 2026-03-05 | Add serverless functions for DB read/write | db/gameStore.js, db/gameStore.test.js, functions/players.js, functions/games.js, functions/scores.js | DB CRUD layer (dbCreatePlayer, dbGetPlayer, dbCreateGame, dbGetGame, dbUpdateGameStatus, dbJoinGame, dbSubmitScore, dbGetGameScores); handlers accept optional pool; 32 new tests; 127 total pass; build clean |
| 11 | 2026-03-05 | Add automated tests for database interactions | db/lifecycle.test.js | Lifecycle tests: player create→get, game full lifecycle (join/status transitions), score submit→retrieve/upsert, full round workflow, error propagation, SQL structure verification; 27 new tests; 154 total pass; build clean |
| 12 | 2026-03-05 | Implement WebSocket server for real-time game state updates | server/gameState.js, server/wsHandler.js, server/index.js, server/gameState.test.js, server/server.test.js | GameStateManager (in-memory per-game state); WsHandler extended with join_game, leave_game, location_update, request_state handlers; broadcastToGame routing; 36 new tests; 190 total pass; build clean |
| 13 | 2026-03-05 | Add serverless endpoints to initiate/terminate WebSocket sessions | functions/sessions.js, functions/sessions.test.js, functions/router.js | POST /sessions (initiateSession) and DELETE /sessions/:sessionId (terminateSession); in-process Map store with injectable store for testing; router updated; 14 new tests; 204 total pass; build clean |
| 14 | 2026-03-05 | Implement basic heartbeat/ping system to keep connections alive and detect disconnects | server/heartbeat.js, server/heartbeat.test.js, server/index.js, server/server.test.js | HeartbeatManager using native WS ping/pong frames; track(), start(), stop(); auto-terminates unresponsive clients; wired into createServer with heartbeatInterval option; 14 new tests; 218 total pass; build clean |
| 15 | 2026-03-05 | Write tests for connection reliability and message delivery | server/connection.test.js | 8 describe blocks covering reconnection, closed-client safety, multi-game cleanup, message ordering, broadcast isolation, GSM-less degradation, concurrent players, heartbeat integration; 23 new tests; 241 total pass; build clean |
| 16 | 2026-03-05 | Implement skeleton game loop logic on managed servers | server/gameLoopManager.js, server/index.js | GameLoopManager: per-game phase lifecycle (waiting→hiding→seeking→finished); auto-advances on timers; onPhaseChange/onTick hooks; wired into createServer with phase broadcast + GSM sync; 31 new tests; 272 total pass; build clean |
| 17 | 2026-03-05 | Add state update dispatcher | server/stateDispatcher.js, server/stateDispatcher.test.js, server/index.js | StateDispatcher: phase-keyed task registry ('*' global + per-phase); async concurrent dispatch; isolated error handling; onDispatch callback; wired to gameLoopManager.onTick; 23 new tests; 295 total pass; build clean |
| 18 | 2026-03-05 | Implement start/stop hooks for game loop to spin down managed servers when idle | server/gameLoopManager.js, server/index.js, server/gameLoopManager.test.js, server/server.test.js | onActive/onIdle callbacks on GameLoopManager; fire on 0→1 and 1→0 active game transitions; server.onActive(fn)/server.onIdle(fn) registration API; 12 new tests; 307 total pass; build clean |
| 19 | 2026-03-05 | Create logging mechanism to track loop iterations, errors, and performance metrics | server/logger.js, server/logger.test.js, server/gameLoopManager.js, server/stateDispatcher.js, server/index.js, server/server.test.js | Logger class with level filtering and injectable sink; nullLogger no-op; startTimer perf helper; wired into GameLoopManager (tick/phase/start/stop) and StateDispatcher (task errors + dispatch perf); server lifecycle logs (start/stop/active/idle); 23 new tests; 330 total pass; build clean |
| 20 | 2026-03-05 | Implement serverless API endpoints for retrieving live game state | functions/liveState.js, functions/liveState.test.js, functions/router.js, server/index.js, server/server.test.js | GET /live/:gameId serverless handler (gsm or serverUrl proxy); GET /internal/state/:gameId HTTP endpoint on managed server; router updated with new route and async-aware dispatch; 15 new tests; 345 total pass; build clean |
| 21 | 2026-03-05 | Build basic admin dashboard to view active sessions, connected players, and loop health | functions/admin.js, functions/admin.test.js, functions/router.js, server/index.js, server/server.test.js | GET /admin serverless handler (in-process or serverUrl proxy); GET /internal/admin HTTP endpoint on managed server returns connectedPlayers, activeGameCount, uptimeMs, games[]; router updated; 18 new tests; 363 total pass; build clean |
| 22 | 2026-03-05 | Add authentication/authorization for admin access | functions/auth.js, functions/auth.test.js, functions/admin.js, functions/admin.test.js, functions/router.js, config/env.js, .env.example | Bearer token auth via Authorization header; constant-time comparison (timingSafeEqual); ADMIN_API_KEY env var; 401 for missing/wrong token, 503 if unconfigured; router passes headers to handlers; 23 new tests; 386 total pass; build clean |
| 23 | 2026-03-05 | Add rate limiting and error handling for API endpoints | functions/rateLimiter.js, functions/rateLimiter.test.js, functions/router.test.js, functions/router.js | Fixed-window rate limiter (100 req/60s per client IP); X-Forwarded-For extraction; 429 with Retry-After + X-RateLimit-Remaining headers; try/catch in router → 500 on handler throw; injectable limiter for test isolation; 31 new tests; 417 total pass; build clean |
| 24 | 2026-03-09 | Integrate logging/monitoring services | server/monitoring.js, server/monitoring.test.js, api/*.js, vercel.json, .env.example, .github/workflows/ci.yml | MetricsCollector (loop iterations, connections, DB reads/writes, errors); createMonitoringSink (stdout JSON-line for CloudWatch/Datadog, HTTP sink for Datadog Logs API); Vercel serverless adapters in api/; CI Vercel deploy step; GAME_SERVER_URL env var; 26 new tests; 443 total pass; build clean |
| 25 | 2026-03-09 | Track metrics: active connections, loop iterations/min, DB reads/writes | server/monitoring.js, server/monitoring.test.js, server/index.js, server/server.test.js, db/gameStore.js, db/gameStore.test.js | RateTracker (sliding-window per-minute rate); MetricsCollector wired into createServer (ACTIVE_CONNECTIONS on WS connect/close, LOOP_ITERATIONS + RateTracker on tick, ERRORS on WS error); /internal/admin includes metrics snapshot + loopIterationsPerMinute; createInstrumentedStore wraps all DB fns to auto-increment DB_READS/DB_WRITES/ERRORS; 23 new tests; 466 total pass; build clean |
| 26 | 2026-03-09 | Implement alerting for failure scenarios | server/alerting.js, server/alerting.test.js, server/index.js, server/server.test.js, config/env.js, config/env.test.js, .env.example | AlertManager (SERVER_CRASH, DB_ERROR, CONNECTION_DROP, ERROR_RATE_HIGH, LOOP_STALL); fire-and-forget HTTP webhook; logger + onAlert callback; watchProcess() for uncaughtException/unhandledRejection; nullAlertManager no-op; ALERT_WEBHOOK_URL + ALERT_ERROR_THRESHOLD env vars; wired into createServer (onTick checkMetrics + ws error alert); 39 new tests; 505 total pass; build clean |
| 27 | 2026-03-09 | Add unit tests for serverless functions and managed game loop logic | server/wsHandler.test.js | Dedicated WsHandler unit tests: handleConnection, broadcast, broadcastToGame, getGamePlayerCount, message routing (join_game, leave_game, location_update, request_state, unknown type, invalid JSON), disconnect; mock ws objects; 47 new tests; 552 total pass; build clean |
| 28 | 2026-03-09 | Add integration tests to simulate multiple players connecting, updating state, and disconnecting | server/integration.test.js | Real HTTP+WebSocket server; buffered message queue; 10 describe blocks: handshake, multi-player join, location updates, WS state request, HTTP state endpoint, disconnect notifications, count tracking, broadcast isolation, admin endpoint, full lifecycle; 19 new tests; 571 total pass; build clean |
| 29 | 2026-03-09 | Set up CI/CD pipeline to run tests and deploy both serverless and managed components | .github/workflows/ci.yml, server/start.js, Dockerfile, package.json | Multi-job pipeline: test → deploy-serverless (Vercel) + deploy-server (Docker → GHCR + webhook); concurrency cancel-in-progress; Docker layer cache via GHA; server/start.js container entrypoint with onIdle shutdown; npm start script; 571 tests pass; build clean |
| 30 | 2026-03-09 | Add staging environment to validate system behavior before production deployment | .github/workflows/ci.yml, scripts/smoke.js, scripts/smoke.test.js, .env.staging.example, package.json | 6-job CI pipeline: test → deploy-staging-serverless + deploy-staging-server → smoke-test → deploy-serverless + deploy-server; Vercel preview URL captured as job output; Docker :staging tag separate from :latest; smoke.js checks SPA 200 / admin 401 / unknown-route 404 / optional game-server; 11 new tests; 582 total pass; build clean |
| 31 | 2026-03-09 | Implement auto-scaling for managed servers based on activity | server/autoScaler.js, server/autoScaler.test.js, server/index.js, server/server.test.js, config/env.js, config/env.test.js, .env.example | AutoScaler class: UP/DOWN thresholds on activeGames + activeConnections; cooldown hysteresis; fire-and-forget webhook; onScale callback; nullAutoScaler no-op; wired into createServer onTick via nullAutoScaler default; env vars SCALE_WEBHOOK_URL/SCALE_UP_GAMES/SCALE_UP_CONNECTIONS/SCALE_DOWN_GAMES/SCALE_DOWN_CONNECTIONS/SCALE_COOLDOWN_MS; 36 new tests; 618 total pass; build clean |
| 33 | 2026-03-10 | Implement full shutdown option to reduce idle costs to zero | server/shutdown.js, server/shutdown.test.js, server/start.js, .env.example | ShutdownManager: idle-triggered shutdown with configurable IDLE_SHUTDOWN_DELAY_MS grace period; SIGTERM/SIGINT signal handlers; async cleanup hooks; re-entrancy guard; onActive() cancels pending idle countdown; 16 new tests; 642 total pass; build clean |
| 34 | 2026-03-10 | Document cost-saving strategies in DESIGN.md | spec/DESIGN.md | Added section 16: 8 numbered strategies covering static frontend, serverless functions, serverless Postgres, on-demand container + shutdown, auto-scaling, OSM maps, throttled location, and metrics-driven alerting; cost decision reference table included |
| 39 | 2026-03-10 | Wire Vercel API adapters to real Postgres | api/players.js, api/games/[id].js, api/scores.js, api/api.test.js | Lazy pool singleton (createPool + createTables on cold start) in each adapter; null fallback when DATABASE_URL unset; try/catch → 500; 13 new tests; 655 total pass; build clean |
| 35 | 2026-03-10 | Finalize DESIGN.md with architecture diagrams | spec/DESIGN.md | Added sections 17–21: detailed component diagram, 3 request-flow diagrams (game setup / real-time gameplay / end-game), game state machine, key file reference table, deployment topology; 655 tests pass; build clean |
| 36 | 2026-03-10 | Update README.md with setup, API endpoints, and deployment | README.md | Full rewrite: prerequisites, local setup, env var table, all REST + WS API endpoints, project structure, Vercel + Docker + CI/CD deployment, GitHub secrets reference; 655 tests pass; build clean |
| 38 | 2026-03-10 | Write onboarding guide for new developers | docs/ONBOARDING.md | 13-section guide: prerequisites, first-time setup, dev commands, project map, 90-second architecture, testing conventions, step-by-step for adding endpoints and WS messages, env var rules, RALPH loop, deployment checklist, help reference; 655 tests pass; build clean |
| 40 | 2026-03-10 | Implement zone calculation service | functions/zones.js, functions/zones.test.js, api/zones.js, functions/router.js, functions/router.test.js | GET /zones?bounds=&scale= fetches transit stations from OSM Overpass API and returns hiding zones (500 m small/medium, 1 km large); injectable fetch for test isolation; router updated with query param parsing; 32 new tests; 687 total pass; build clean |
| 41 | 2026-03-10 | Implement question system API | db/schema.sql, db/gameStore.js, functions/questions.js, functions/questions.test.js, functions/router.js, api/questions.js, api/answers/[questionId].js, server/index.js | POST /questions, GET /questions?playerId=, POST /answers/:questionId; questions+answers tables; DB store functions; fire-and-forget WS notify via POST /internal/notify on managed server; injectable fetch for test isolation; 35 new tests; 722 total pass; build clean |
| 42 | 2026-03-10 | Implement challenge card system | db/schema.sql, db/gameStore.js, functions/cards.js, functions/cards.test.js, functions/questions.js, functions/router.js, api/cards.js, api/cards/[cardId]/play.js | Hider Deck (time_bonus/powerup/curse); cards table; dbDrawCard/dbGetPlayerHand/dbPlayCard; GET /cards?gameId=&playerId= returns hand (max 6); POST /cards/:cardId/play applies effect; card draw triggered on answer submit (in-process + DB); 29 new tests; 751 total pass; build clean |
| 43 | 2026-03-10 | Implement proximity / capture detection | server/captureDetector.js, server/captureDetector.test.js, server/gameState.js, server/index.js | haversineDistance + checkCapture pure functions; GameStateManager.setGameZones/getGameZones; POST /internal/games/:gameId/zones endpoint; seeking-phase StateDispatcher task checks capture each tick; broadcasts capture event; optional store for DB status+score persistence; re-entrancy guard; 20 new tests; 771 total pass; build clean |
| 44 | 2026-03-10 | Build frontend game lobby | src/api.js, src/components/PlayerForm.jsx, src/components/GameForm.jsx, src/components/WaitingRoom.jsx, src/components/Lobby.jsx, src/components/Lobby.test.jsx, src/App.jsx, functions/games.js, api/games/index.js | 3-step lobby flow (register → create/join → waiting room); PlayerForm calls POST /api/players; GameForm create tab has scale selector + 4-field bounds picker; join tab validates via GET /api/games/:id; handleCreateGame HTTP handler + Vercel adapter api/games/index.js; 32 new tests; 803 total pass; build clean |
| 45 | 2026-03-10 | Build frontend map view | src/components/GameMap.jsx, src/components/GameMap.test.jsx, src/components/Lobby.jsx, src/components/WaitingRoom.jsx, package.json | Leaflet+OSM map; game bounds rectangle; hiding zone circles; live player markers; WS connection (player_location/game_state/phase_change/capture); GPS polling throttled to 10 s; WaitingRoom gets Start Game button; Lobby transitions to GameMap in playing state; 18 new tests; 813 total pass; build clean |
| 46 | 2026-03-10 | Build frontend question/answer UI | src/api.js, src/components/QuestionPanel.jsx, src/components/AnswerPanel.jsx, src/components/GameMap.jsx, src/components/QA.test.jsx, src/components/GameMap.test.jsx | Seeker QuestionPanel (category selector, targetId field, optimistic question list); Hider AnswerPanel (fetches inbox, per-question answer form, answered-count feedback); GameMap handles question_answered WS event via qaRefresh counter; 20 new tests; 833 total pass; build clean |
| 47 | 2026-03-10 | Build frontend hider card panel | src/api.js, src/components/CardPanel.jsx, src/components/CardPanel.test.jsx, src/components/GameMap.jsx, src/components/GameMap.test.jsx | fetchCards/playCardApi API fns; CardPanel (hand display max 6, tap-to-play, effect confirmation, load/play errors, refreshTrigger); wired into GameMap for hiders below AnswerPanel; 15 new tests; 848 total pass; build clean |
| 48 | 2026-03-11 | Fix Vercel 12-function limit | vercel.json, spec/DESIGN.md | Individual api/ adapters already deleted (commit feddf74); updated vercel.json functions glob from api/**/*.js to api/[...path].js; updated DESIGN.md sections 16, 17, 18, 20 to remove stale adapter file references; 848 tests pass; build clean |
| 49 | 2026-03-11 | Hider zone selection | db/schema.sql, db/gameStore.js, functions/gameZone.js, functions/gameZone.test.js, functions/router.js, src/api.js, src/components/ZoneSelector.jsx, src/components/ZoneSelector.test.jsx, src/components/GameMap.jsx, src/components/GameMap.test.jsx | game_zones table; dbSetGameZone/dbGetGameZone; POST /games/:gameId/zone handler; ZoneSelector component (tap-to-select + confirm dialog); GameMap shows ZoneSelector for hiders in hiding phase + handles zone_locked WS event; 880 tests pass; build clean |
| 50 | 2026-03-11 | Question timing enforcement | db/schema.sql, db/gameStore.js, db/gameStore.test.js, functions/questions.js, functions/questions.test.js, server/index.js, server/server.test.js | expires_at column + 'expired' status on questions; dbCreateQuestion enforces one-pending-at-a-time (409 conflict); dbExpireStaleQuestions marks overdue questions; seeking-phase StateDispatcher task expires questions + broadcasts question_expired WS event; 19 new tests; 899 total pass; build clean |
| 52 | 2026-03-11 | Game timer display | server/index.js, functions/questions.js, src/components/GameMap.jsx, server/server.test.js, src/components/GameMap.test.jsx, functions/questions.test.js | timer_sync WS broadcast on phase change + 30 s periodic; question_pending WS notify from submitQuestion; GameMap countdown banner (MM:SS); 16 new tests; 938 total pass; build clean |
| 51 | 2026-03-11 | Photo question support | db/schema.sql, db/gameStore.js, functions/questions.js, functions/questions.test.js, functions/router.js, src/api.js, src/components/AnswerPanel.jsx, src/components/QA.test.jsx | question_photos table; dbSaveQuestionPhoto/dbGetQuestionPhoto; POST+GET /questions/:questionId/photo handlers with in-process store; router updated; AnswerPanel shows file-input for photo questions, reads as base64 via FileReader, uploads before text answer; 23 new tests; 922 total pass; build clean |
| 53 | 2026-03-11 | Post-game results screen | db/schema.sql, db/gameStore.js, db/gameStore.test.js, db/lifecycle.test.js, functions/scores.js, src/api.js, src/components/ResultsScreen.jsx, src/components/ResultsScreen.test.jsx, src/components/CardPanel.jsx, src/components/GameMap.jsx, src/components/Lobby.jsx | bonus_seconds column on scores; ResultsScreen full-screen overlay (winner, elapsed, bonus, final score, Play Again); CardPanel onTimeBonusPlayed callback; GameMap tracks hidingStartedAt + captureWinnerRef; submitScore API fn; Lobby Play Again resets game/playing but keeps player; 15 new tests; 953 total pass; build clean |
| 54 | 2026-03-11 | Leaderboard | db/gameStore.js, functions/scores.js, functions/scores.test.js, functions/router.js, src/api.js, src/components/Leaderboard.jsx, src/components/Leaderboard.test.jsx, src/components/Lobby.jsx | dbGetLeaderboard JOIN query (scores+players+games); GET /scores endpoint with limit+gameId query params; fetchLeaderboard API fn; Leaderboard component (rank, player, scale, MM:SS hiding time); Leaderboard toggle button in Lobby; 24 new tests; 977 total pass; build clean |
| 55 | 2026-03-11 | Two-teams seeker variant | db/schema.sql, db/gameStore.js, db/gameStore.test.js, db/lifecycle.test.js, functions/games.js, server/gameState.js, server/wsHandler.js, server/wsHandler.test.js, server/captureDetector.js, server/captureDetector.test.js, server/index.js, server/gameState.test.js, server/server.test.js, src/api.js, src/components/GameForm.jsx, src/components/WaitingRoom.jsx, src/components/GameMap.jsx, src/components/Lobby.jsx | seeker_teams on games (0=off, 2=two teams); team on game_players (auto-balanced A/B for seekers); team-scoped pending question check; WsHandler team auto-assignment + team-scoped location broadcasts; checkCapture two-team mode (first team all in zone wins); GameMap join_game on open + team-colored markers; GameForm seeker_teams selector; WaitingRoom team display; 15 new tests; 992 total pass; build clean |
| 56 | 2026-03-11 | WebSocket reconnection | server/wsHandler.js, server/wsHandler.test.js, server/index.js, server/server.test.js, server/connection.test.js, server/integration.test.js, src/components/GameMap.jsx, src/components/GameMap.test.jsx | WsHandler 30 s grace period on disconnect; broadcasts player_disconnected immediately then player_left after expiry; cancels timer on rejoin; sends game_state to reconnecting player; broadcasts player_reconnected; reconnectGraceMs createServer param; GameMap exponential backoff (1 s→30 s, 6 attempts); Reconnecting… banner via wsStatus; 12 new tests; 1004 total pass; build clean |
| 57 | 2026-03-11 | Game invite URL | src/components/WaitingRoom.jsx, src/components/GameForm.jsx, src/components/Lobby.jsx, src/components/Lobby.test.jsx | WaitingRoom shows invite link (?gameId=xxx); GameForm accepts initialTab/initialGameId props; Lobby reads ?gameId from URL and activates join tab with pre-filled ID; 3 new tests; 1007 total pass; build clean |
| 58 | 2026-03-11 | Progressive Web App | public/manifest.json, public/sw.js, public/icon-192.svg, public/icon-512.svg, index.html, src/registerSW.js, src/main.jsx, src/pwa.test.js | manifest.json (name, icons, theme colour, standalone); minimal SW (install cache app shell, runtime cache assets, navigate fallback); registerServiceWorker module registered in main.jsx; 5 new tests; 1012 total pass; build clean |
| 59 | 2026-03-11 | Question history | db/gameStore.js, functions/questions.js, functions/questions.test.js, src/api.js, src/components/QuestionPanel.jsx, src/components/AnswerPanel.jsx, src/components/GameMap.jsx, src/components/QA.test.jsx | dbGetQuestionsForGame (LEFT JOIN questions+answers); listQuestions extended to accept gameId (returns full Q&A history) or playerId (hider inbox); listQuestions API fn changed to object param; QuestionPanel fetches history on mount + qaRefresh, displays Q&A pairs with answer text; GameMap passes qaRefresh to QuestionPanel; 10 new tests; 1022 total pass; build clean |
| 60 | 2026-03-11 | Measuring and transit question categories | functions/questions.js, functions/questions.test.js, src/components/QuestionPanel.jsx, src/components/QA.test.jsx, spec/TASKS.md | Added 'measuring' and 'transit' to VALID_CATEGORIES (backend) and CATEGORIES array (frontend) to align with all 6 RULES.md question types; added CATEGORY_HINTS map; hint shown as <small aria-label="question type hint"> below selector; 2 new tests (hint renders, hint updates on select); 1024 total pass; build clean |
| 61 | 2026-03-11 | Hider timeout victory | server/index.js, server/server.test.js, spec/TASKS.md | When seeking phase timer expires without capture, broadcast capture event with winner: 'hider' and call dbUpdateGameStatus('finished'); _seekingStartedAt map tracks phase entry time; _capturingGames cleaned up on finish; 4 new tests; 1028 total pass; build clean |
| 64 | 2026-03-12 | Powerup card: false zone broadcast | functions/cards.js, functions/cards.test.js, server/index.js, server/server.test.js, src/components/GameMap.jsx, src/components/GameMap.test.jsx | notifyFalseZone fires /internal/notify; server generates offset decoy zone (0.5–2 km, random direction) + stores in _falseZones Map; broadcasts false_zone WS event; seeking-phase false_zone_expiry task broadcasts false_zone_expired after 5 min; GameMap renders dashed purple circle on false_zone, removes on false_zone_expired, hider sees decoy labeled; 10 new tests; 1070 total pass; build clean |
| 63 | 2026-03-12 | Time bonus card: extend server-side phase timer | server/gameLoopManager.js, server/index.js, server/gameLoopManager.test.js, server/server.test.js, functions/cards.js, functions/cards.test.js | extendPhase/getPhaseExtension on GameLoopManager; buildTimerSync includes extension; /internal/notify handles time_bonus; playCard fires notifyTimeBonus; 15 new tests; 1060 total pass; build clean |
| 65 | 2026-03-12 | Fix questions schema category constraint | db/schema.sql, db/gameStore.test.js | Added measuring+transit to CHECK constraint; idempotent ALTER TABLE migration; 2 new parameterised tests; 1072 total pass; build clean |
| 62 | 2026-03-11 | Curse card enforcement | db/schema.sql, db/gameStore.js, db/gameStore.test.js, functions/cards.js, functions/cards.test.js, functions/questions.js, functions/questions.test.js, src/components/GameMap.jsx, src/components/QuestionPanel.jsx, src/components/QA.test.jsx, spec/TASKS.md | curse_expires_at on games; dbSetCurse/dbGetCurseExpiry; playCard activates curse + notifies server; submitQuestion returns 409 curse_active while blocked; _curses in-process map shared between cards.js and questions.js; GameMap handles curse_active WS event; QuestionPanel shows countdown banner + disabled submit; 17 new tests; 1045 total pass; build clean |
| 66 | 2026-03-12 | Photo question expiry by game scale | db/gameStore.js, db/gameStore.test.js, functions/questions.js, functions/questions.test.js | PHOTO_EXPIRY_MS_BY_SCALE map (small→10 min, medium→15 min, large→20 min); dbCreateQuestion accepts optional gameScale param and SELECTs games.size when photo+no scale supplied; submitQuestion accepts gameScale in body and passes to dbCreateQuestion; 9 new tests (3 DB scale tests + SELECT-games auto-detect test + non-photo no-extra-query test + 3 in-process scale tests + pool-with-gameScale test); 1081 total pass; build clean |
| 67 | 2026-03-12 | Seeker transit mode | server/gameState.js, server/gameState.test.js, server/wsHandler.js, server/wsHandler.test.js, server/captureDetector.js, server/captureDetector.test.js, src/components/GameMap.jsx, src/components/GameMap.test.jsx | onTransit boolean on player state (default false); setPlayerTransit/getPlayerTransit on GameStateManager; set_transit WS message → updates GSM + broadcasts player_transit to game; checkCapture excludes seekers with onTransit=true (same as null-location exclusion); GameMap: toggle button for seekers in seeking phase + sends set_transit; player_transit WS handler updates marker transit status; marker tooltip + opacity reflect transit state; 21 new tests; 1102 total pass; build clean |
| 68 | 2026-03-12 | Scale-aware phase durations | server/gameLoopManager.js, server/gameLoopManager.test.js, server/index.js, server/server.test.js, spec/TASKS.md | Per-game duration overrides on GameLoopManager.startGame(gameId, opts); getGameDuration(gameId, phase) returns per-game override or constructor default; _tick uses per-game durations; SCALE_DURATIONS constant (small=30min, medium=60min, large=180min); POST /internal/games/:gameId/start endpoint accepts {scale}, calls startGame+beginHiding with scale durations; buildTimerSync uses getGameDuration; 18 new tests; 1120 total pass; build clean |
| 69 | 2026-03-12 | Wire game start from serverless | functions/games.js, functions/games.test.js, functions/router.js, src/api.js, src/components/WaitingRoom.jsx, src/components/WaitingRoom.test.jsx | POST /games/:gameId/start serverless handler (handleStartGame) fires notify to /internal/games/:gameId/start on managed server; startGame API fn in src/api.js; WaitingRoom Start Game button calls startGame({gameId, scale}) instead of direct WS send; 15 new tests (5 handleStartGame + 10 WaitingRoom); 1153 total pass; build clean |
| 71 | 2026-03-12 | End Game intermediate phase with hider freeze | server/gameState.js, server/gameState.test.js, server/wsHandler.js, server/wsHandler.test.js, server/index.js, server/server.test.js, src/components/GameMap.jsx, src/components/GameMap.test.jsx | endGameActive + getPlayerRole on GameStateManager; _handleLocationUpdate rejects hider updates during End Game; capture_check now sets endGameActive + broadcasts end_game_started (not capture); onSpotConfirmed broadcasts capture(winner=seekers) + DB calls; _endGameTimers for 10-min End Game timeout; _hiderWinBroadcast guard prevents double-hider-win; frontend banner + GPS freeze for hider; 15 new tests; 1195 total pass; build clean |
| 72 | 2026-03-12 | Hider location privacy | server/wsHandler.js, server/wsHandler.test.js, server/server.test.js, server/connection.test.js, server/integration.test.js, spec/TASKS.md | Hider GPS not broadcast to seekers; hider receives own echo (player_location) for self-marker; GSM still stores location for capture detection; outgoing broadcast type renamed location_update→player_location to match frontend expectation; 4 new tests (privacy: hider location not broadcast to others, hider echo, seeker broadcast); 1199 total pass; build clean |
| 73 | 2026-03-12 | End Game countdown timer sync | server/index.js, server/server.test.js, src/components/GameMap.jsx, src/components/GameMap.test.jsx, spec/TASKS.md | _endGameStartedAt Map tracks End Game start time; buildTimerSync extended to support phase end_game (uses startedAt + endGameTimeoutMs); timer_sync broadcast alongside end_game_started; periodic tick uses end_game phase when endGameActive; _endGameStartedAt cleared on finish; GameMap shows "You win if not spotted in" (hider) or "Find the hider in" (seeker) countdown banner during End Game; 5 new tests (2 server, 3 frontend); 1204 total pass; build clean |
| 74 | 2026-03-12 | Configurable hiding duration within scale range | server/index.js, server/server.test.js, functions/games.js, functions/games.test.js, src/api.js, src/components/WaitingRoom.jsx, src/components/WaitingRoom.test.jsx, spec/TASKS.md | SCALE_DURATION_RANGES constant (small 30–60, medium 60–180, large 180–360 min); server-side validation returns 400 when custom duration is out of range; handleStartGame accepts hidingDurationMin and converts to hidingDurationMs; WaitingRoom shows duration number input constrained to scale range when host; startGame API fn accepts hidingDurationMin; 14 new tests; 1218 total pass; build clean |
| 75 | 2026-03-13 | Hider journey trail | src/components/GameMap.jsx, src/components/GameMap.test.jsx, spec/TASKS.md | MAX_TRAIL_POINTS=500 constant; locationTrail state accumulates {lat,lon} from player_location events for hider's own playerId; cap enforced via slice (oldest dropped); L.polyline rendered on map for hider, setLatLngs on subsequent updates; trail resets to [] on phase_change→hiding; no polyline for seekers; 5 new tests; 1223 total pass; build clean |
| 127 | 2026-03-19 | Rate limiter store-full reject instead of clear | functions/rateLimiter.js, functions/rateLimiter.test.js | Removed store.clear() fallback in prune(); saturated store now returns {allowed:false} to caller; console.warn logged; two old tests updated, two new tests added (saturated-reject, expired-prune-admit); 1514 tests pass; build clean |
| 107 | 2026-03-19 | Button component | src/components/Button.jsx, src/components/Button.module.css, src/components/Button.test.jsx | Reusable Button with primary/ghost/danger variants; 44 px min touch target; press animation; focus ring; 11 tests; 1534 total pass; build clean |
| 76 | 2026-03-13 | Single hider enforcement | server/gameState.js, server/gameState.test.js, server/wsHandler.js, server/wsHandler.test.js, server/server.test.js, server/connection.test.js, db/schema.sql, src/components/GameMap.jsx, src/components/GameMap.test.jsx | getHiderId on GameStateManager; addPlayerToGame throws HIDER_SLOT_TAKEN when second different player joins as hider (idempotent for same player rejoin); wsHandler catches error, rolls back gameClients/playerGames, sends error WS response; partial unique DB index game_players_one_hider; GameMap joinError state + error banner (data-testid=join-error-banner); fixed existing tests that joined two players with default hider role; 12 new tests; 1235 total pass; build clean |
| 170 | 2026-03-22 | Seeker deductions notepad (Investigation Book) | src/components/SeekerNotes.jsx, src/components/SeekerNotes.module.css, src/components/SeekerNotes.test.jsx, src/components/GameMap.jsx, src/components/GameMap.test.jsx, spec/TASKS.md | NOTES_STORAGE_KEY_PREFIX + NOTES_DEBOUNCE_MS constants; SeekerNotes component with debounced localStorage persistence; lazy-loaded from GameMap (seeker-only, emits separate 0.71 kB chunk); 9 new tests (label, placeholder, restore, debounce timing, unmount cleanup, seeker/hider visibility); 1766 total pass; build clean |

- [x] **71** — End Game intermediate phase with hider freeze: RULES.md requires seekers to physically spot the hider after entering the hiding zone, and the hider must stay put once End Game begins. Currently `checkCapture` immediately finishes the game on zone entry, with no intermediate End Game state. Changes needed: (1) `server/gameState.js` — add `endGameActive` boolean to game entries; add `setEndGameActive(gameId, bool)`, `isEndGameActive(gameId)`, and `getPlayerRole(gameId, playerId)` methods. (2) `server/wsHandler.js` — in `_handleLocationUpdate`, when the sender is the hider and `gameStateManager.isEndGameActive(gameId)`, silently ignore the update (no GSM update, no broadcast). (3) `server/index.js` — change `capture_check` task: when `checkCapture` returns `captured: true`, set `endGameActive`, broadcast `end_game_started` (not `capture`), set an `_endGameTimers` timeout for `endGameTimeoutMs` (default 10 min, configurable via `createServer` param) after which hider wins; update `onSpotConfirmed` to add to `_capturingGames`, broadcast `capture { winner: 'seekers' }`, do DB store calls, then call `finishGame`; in `onPhaseChange → finished` add `_hiderWinBroadcast` guard to prevent double hider-win broadcast when both End Game timeout and seeking phase timeout fire; clear `_endGameTimers` entry in `onPhaseChange → finished`. (4) Frontend `src/components/GameMap.jsx` — add `endGameActive` state + `endGameActiveRef`; handle `end_game_started` WS event; show "End Game: Stay put!" banner for hider when active; in GPS polling, skip sending if hider and `endGameActiveRef.current`. Tests: `server/gameState.test.js` (new methods), `server/wsHandler.test.js` (location rejected for hider during End Game), `server/server.test.js` (capture_check now emits `end_game_started`; spot confirmed in End Game emits `capture { winner: seekers }`; End Game timeout emits `capture { winner: hider }`), `src/components/GameMap.test.jsx` (banner and GPS freeze).

---

## Next Up

Tasks are ordered by dependency. Complete them top to bottom.

### Phase 1 — Project Scaffolding

- [x] **1** — Initialize project repository: set up Git, README, LICENSE, and basic folder structure (`src/`, `functions/`, `docs/`, `config/`).
- [x] **2** — Set up project build and deployment tools (Node.js, npm scripts, CI/CD placeholder).
- [x] **3** — Create `DESIGN.md` and `TASKS.md` files to track design decisions and tasks.

### Phase 2 — Core Infrastructure

- [x] **4** — Define environment configuration: dev, staging, prod with environment variables for API keys, DB endpoints, and feature toggles.
- [x] **5** — Set up managed server instances for persistent components (game loop, WebSocket handling).
- [x] **6** — Implement serverless endpoints for short-lived tasks (player registration, score submission, basic queries).
- [x] **7** — Establish hybrid architecture documentation showing which services are serverless and which are managed.

### Phase 3 — Persistence Layer

- [x] **8** — Choose database(s) for game state: e.g., DynamoDB or PostgreSQL.
- [x] **9** — Implement basic schema or table structure for players, sessions, and game states.
- [x] **10** — Add serverless functions to read/write game state to the database.
- [x] **11** — Add automated tests for database interactions, verifying correct read/write.

### Phase 4 — WebSocket & Real-Time Tracking

- [x] **12** — Implement WebSocket server on managed backend for real-time game state updates.
- [x] **13** — Add serverless endpoints to initiate/terminate WebSocket sessions.
- [x] **14** — Implement basic heartbeat/ping system to keep connections alive and detect disconnects.
- [x] **15** — Write tests for connection reliability and message delivery.

### Phase 5 — Game Loop Infrastructure

- [x] **16** — Implement skeleton game loop logic on managed servers (without specific rules yet).
- [x] **17** — Add state update dispatcher: takes current game state and triggers serverless tasks for computation.
- [x] **18** — Implement start/stop hooks for game loop to spin down managed servers when idle.
- [x] **19** — Create logging mechanism to track loop iterations, errors, and performance metrics.

### Phase 6 — API & Admin Tools

- [x] **20** — Implement serverless API endpoints for retrieving live game state.
- [x] **21** — Build basic admin dashboard to view active sessions, connected players, and loop health.
- [x] **22** — Add authentication/authorization for admin access.
- [x] **23** — Add rate limiting and error handling for API endpoints.

### Phase 7 — Analytics & Monitoring

- [x] **24** — Integrate logging/monitoring services (CloudWatch, Datadog, or equivalent) for both serverless and managed components.
- [x] **25** — Track metrics like active connections, loop iterations per minute, and database reads/writes.
- [x] **26** — Implement alerting for failure scenarios (server crashes, DB errors, connection drops).

### Phase 8 — Testing & CI/CD

- [x] **27** — Add unit tests for serverless functions and managed game loop logic.
- [x] **28** — Add integration tests to simulate multiple players connecting, updating state, and disconnecting.
- [x] **29** — Set up CI/CD pipeline to run tests and deploy both serverless and managed components.
- [x] **30** — Add staging environment to validate system behavior before production deployment.

### Phase 9 — Optimization & Cost Management

- [x] **31** — Implement auto-scaling for managed servers based on activity.
- [x] **32** — Optimize serverless functions to reduce invocation costs (minimal memory, short execution).
- [x] **33** — Implement full shutdown option to reduce idle costs to zero.
- [x] **34** — Document cost-saving strategies in `DESIGN.md` for future reference.

### Phase 10 — Documentation & Knowledge Transfer

- [x] **35** — Finalize `DESIGN.md` with architecture diagrams and explanations.
- [x] **36** — Update `README.md` with setup instructions, API endpoints, and deployment notes.
- [x] **37** — Create `RALPH.md` instructions for future task execution using RALPH loops.
- [x] **38** — Write onboarding guide for new developers to run and extend the project.

### Phase 11 — Production Wiring

- [x] **39** — Wire Vercel API adapters to real Postgres: update `api/players.js`, `api/games/[id].js`, and `api/scores.js` to create a `pg.Pool` from `DATABASE_URL` and pass it to each handler. Use `createPool` from `db/db.js` and call `createTables` on cold start. Handlers already accept an optional pool argument and fall back to in-memory when omitted.

### Phase 12 — Core Gameplay Features

- [x] **40** — Implement zone calculation service: given game bounds and scale, fetch transit stations from the OSM Overpass API and compute hiding zones (circles of 500 m for small/medium, 1 km for large). Expose as `GET /api/zones?bounds=&scale=`. Pure handler in `functions/zones.js`, Vercel adapter in `api/zones.js`.
- [x] **41** — Implement question system API: `POST /api/questions` (submit question), `GET /api/questions?playerId=` (list questions for a player), `POST /api/answers/:questionId` (submit answer). Persist questions/answers in new DB tables. Trigger seeker-notification broadcast via managed server when answer is submitted.
- [x] **42** — Implement challenge card system: Hider Deck with time-bonus, powerup, and curse cards. `GET /api/cards?gameId=&playerId=` returns current hand (max 6). `POST /api/cards/:cardId/play` applies effect. Card draws triggered by question answers via DB layer.
- [x] **43** — Implement proximity / capture detection in the game loop: every tick check all seeker locations against hider hiding zone; when all seekers are within zone radius and off transit, transition phase to `finished`, broadcast winner, persist result, update scores.
- [x] **44** — Build frontend game lobby: player registration form, create/join game form with scale selector and map bounds picker. Wire to existing `/api/players` and `/api/games` endpoints.
- [x] **45** — Build frontend map view: Leaflet + OSM map showing game bounds, hiding zones overlay, live player positions. Location updates throttled to 10–20 s via WebSocket. Redraw only on state changes.
- [x] **46** — Build frontend question/answer UI: seekers see question form with category selector and submit button; hider sees pending questions with answer form and optional photo upload.
- [x] **47** — Build frontend hider card panel: display hand of up to 6 cards; tap to play; show effect confirmation. Wire to `/api/cards` endpoints.

### Phase 13 — Gameplay Completeness

- [x] **48** — Fix Vercel 12-function limit: `api/[...path].js` catch-all was already created and `functions/router.js` already accepts `opts.pool`. Delete the 13 individual `api/` adapter files (`api/players.js`, `api/games/[id].js`, `api/scores.js`, `api/sessions.js`, `api/liveState.js`, `api/admin.js`, `api/zones.js`, `api/questions.js`, `api/answers/[questionId].js`, `api/cards.js`, `api/cards/[cardId]/play.js`, `api/games/index.js`, `api/api.test.js`). Update `vercel.json` functions glob from `api/**/*.js` to `api/[...path].js`. Update DESIGN.md section 17 and 20 to remove stale adapter file references. This is a deployment blocker.

- [x] **49** — Hider zone selection: during the hiding phase the hider must tap a transit station on the map to lock their hiding zone. Add `POST /api/games/:gameId/zone` serverless endpoint (body: `{ stationId, lat, lon, radius }`); persist in a new `game_zones` table column or extend `games`; broadcast `zone_locked` WS event so the game server's `captureDetector` uses the chosen zone instead of a default. Frontend: highlight selectable stations during hiding phase, tap-to-select with confirm dialog, disable re-selection once confirmed.

- [x] **50** — Question timing enforcement: the rules require seekers to wait for the current question to be answered before asking a new one, and the hider must answer within time limits (5 min standard, 10–20 min photo). Add `status` (`pending|answered|expired`) and `expires_at` columns to the `questions` table. `POST /questions` rejects with 409 if a pending question exists for that game. A game-loop StateDispatcher task (or serverless cron) marks questions `expired` when deadline passes and broadcasts a `question_expired` WS event. `GET /questions` returns `status` and `expires_at` so the frontend can show a countdown.

- [x] **51** — Photo question support: photo questions require the hider to upload a photo. Add `POST /api/questions/:questionId/photo` endpoint accepting a base64-encoded image in the JSON body; store in a new `question_photos` table (or a `photo_data` column on `questions`). Add `GET /api/questions/:questionId/photo` to retrieve it. Frontend `AnswerPanel`: when `category === 'photo'`, show a file-input that reads the file as base64 and calls the upload endpoint before submitting the text answer.

- [x] **52** — Game timer display: players need to see how much hiding time remains and, during seeking, how long until each question expires. The game server should broadcast a `timer_sync` WS message on phase change (and periodically, at most every 30 s) containing `{ phaseEndsAt: <ISO timestamp> }`. Frontend `GameMap` receives `timer_sync` and shows a countdown banner: "Hiding ends in 23:47" during hiding, "Question expires in 4:12" when a pending question exists. Derive phase duration from game scale (small: 30 min, medium: 60 min, large: 180 min).

- [x] **53** — Post-game results screen: when the frontend receives `phase_change` → `finished`, display a full-screen results overlay showing: winner (Hider or Seekers), elapsed hiding time, card time-bonuses applied, and final score. Score calculation: base = elapsed seconds hidden; bonus = sum of time_bonus card values played. `POST /api/scores` should accept and persist `bonus_seconds`. Add a "Play Again" button that resets state to the lobby (re-uses same `playerId`).

- [x] **54** — Leaderboard: add `GET /api/scores?limit=20&gameId=` serverless endpoint returning ranked scores with player name and scale; backed by a JOIN across `scores`, `players`, and `games`. Add a leaderboard tab/modal to the lobby frontend showing top scores across all games with columns: rank, player name, scale, hiding time (formatted mm:ss).

- [x] **55** — Two-teams seeker variant: optional gameplay mode where seekers are split into two competing teams. Add a `seeker_teams` config field to game creation (0 = off, 2 = two teams). Game server tracks team membership; questions and location updates are scoped per team; capture is credited to the first team to spot the hider. Frontend lobby shows team assignment on game join.

### Phase 14 — Reliability & UX Polish

- [x] **56** — WebSocket reconnection: when a player's WS connection drops mid-game, the frontend should automatically attempt to reconnect with exponential backoff (1 s, 2 s, 4 s … up to 30 s). Show a "Reconnecting…" banner while disconnected. On reconnect, re-send `join_game` to restore server-side game membership. Server-side: give disconnected players a 30 s grace period before removing them from game state, and broadcast `player_disconnected` (not `player_left`) during the grace window; cancel the timer if the player reconnects. This prevents mid-game reconnects from appearing as a full leave+rejoin to other players.

- [x] **57** — Game invite URL: after a game is created, display a shareable link (e.g. `?gameId=xxx`) that pre-fills the Join tab in the lobby. Parse `?gameId` on page load and activate the Join tab automatically. No backend changes needed — purely frontend.

- [x] **58** — Progressive Web App: add `public/manifest.json` (name, icons, theme colour, `display: standalone`) and a minimal service worker that caches the app shell. Register the service worker in `src/main.jsx`. Allows players to add the game to their home screen on iOS/Android.

- [x] **59** — Question history: seekers should see all previous Q&A pairs (question text, category, answer, timestamp) above the current question form in `QuestionPanel`. Fetch from `GET /api/questions?gameId=` on mount and refresh after each `question_answered` WS event. No new backend endpoint needed — extend the existing `listQuestions` API fn to accept `gameId`.

### Phase 15 — Rules Completeness

- [x] **60** — Add measuring and transit question categories: `RULES.md` defines 6 question categories (Matching, Measuring, Transit, Photo, Thermometer, Tentacle) but the backend and frontend only recognise 4 (`matching`, `thermometer`, `photo`, `tentacle`). Add `'measuring'` and `'transit'` to `VALID_CATEGORIES` in `functions/questions.js` and to the `CATEGORIES` array in `src/components/QuestionPanel.jsx`. Add a `CATEGORY_HINTS` map in `QuestionPanel` so each option shows a one-line description (e.g. "Measuring — Am I closer to X than you?"). Show the hint as a `<small>` element below the select. Update tests in `functions/questions.test.js` and `src/components/QA.test.jsx` to cover the two new categories.

### Phase 16 — Gameplay Correctness

- [x] **61** — Hider timeout victory: when the seeking phase timer expires without a seeker capture, the server broadcasts a `capture` event with `winner: 'hider'` and updates DB game status to `'finished'`. Previously only a `phase_change` event was emitted, leaving winner undefined and DB status un-persisted. Track seeking start time in `_seekingStartedAt` and check `_capturingGames` in `onPhaseChange` to distinguish timeout from seeker capture.

### Phase 17 — Card Mechanics

- [x] **62** — Curse card enforcement: when a hider plays a curse card (`{ action: 'block_questions', durationMs: 120_000 }`), seekers must be blocked from submitting new questions for the curse duration. Add `curse_expires_at` column to the `games` table. Add `dbSetCurse`/`dbGetCurseExpiry` to `gameStore.js`. In `playCard`, if the played card is a curse, persist the expiry and fire-and-forget notify the managed server with `{ type: 'curse_active', curseEndsAt }` so it can broadcast to all players. In `submitQuestion`, check the active curse before creating the question (409 `curse_active` if blocked). In-process paths use a shared `_curses` Map exported from `cards.js`. Frontend: `GameMap` handles `curse_active` WS event and passes `curseEndsAt` to `QuestionPanel`; `QuestionPanel` shows a countdown and disables the submit button until the curse expires (checked locally via the existing 1-second tick).

### Phase 18 — Card Effect Completeness

- [x] **63** — Time bonus card: extend server-side phase timer. When a hider plays a `time_bonus` card, the managed server must extend the current phase duration so all players' countdown timers reflect the extra time. Currently `minutesAdded` is only tracked locally in the frontend for score calculation. Changes needed: (1) Add `extendPhase(gameId, extraMs)` to `GameLoopManager` using a per-game `phaseExtensionMs` field reset on each phase transition. Add `getPhaseExtension(gameId)` for inspection. Update `_tick` to include the extension when checking elapsed vs duration. (2) In `server/index.js`, update `buildTimerSync` to add the extension to the base duration. Handle `{ type: 'time_bonus', gameId, minutesAdded }` in `/internal/notify`: call `gameLoopManager.extendPhase` then immediately broadcast an updated `timer_sync`. (3) In `functions/cards.js`, when playing a `time_bonus` card fire-and-forget notify `/internal/notify` (same pattern as `activateCurse`). Tests: `server/gameLoopManager.test.js` (extendPhase extends duration, resets on transition), `server/server.test.js` (notify endpoint calls extendPhase + broadcasts timer_sync), `functions/cards.test.js` (playCard fires notify for time_bonus).

- [x] **64** — Powerup card: false zone broadcast.

### Phase 19 — Schema Correctness

- [x] **65** — Fix `questions` schema: Task 60 added `'measuring'` and `'transit'` to `VALID_CATEGORIES` in `functions/questions.js` and the frontend, but forgot to update the `CHECK` constraint in `db/schema.sql`. Any real Postgres deployment will reject questions with these two categories with a constraint violation. Fix `db/schema.sql`: (1) update the `CREATE TABLE questions` block to list all 6 categories; (2) add an idempotent migration (`ALTER TABLE questions DROP CONSTRAINT IF EXISTS questions_category_check; ALTER TABLE questions ADD CONSTRAINT questions_category_check CHECK (...)`) so existing databases are patched on next deploy. Add tests in `db/gameStore.test.js` verifying `dbCreateQuestion` succeeds for `'measuring'` and `'transit'` categories.

### Phase 20 — Rules Accuracy

- [x] **66** — Photo question expiry by game scale: `RULES.md` states photo questions expire in 10–20 min depending on game size, but `questions.js` uses a fixed 15 min for all games. Modify `dbCreateQuestion` to join with the `games` table and derive the photo deadline from `size` (small → 10 min, medium → 15 min, large → 20 min). For the in-process path, add an optional `gameScale` param to `submitQuestion`'s request body and compute the same mapping. Update tests in `db/gameStore.test.js` and `functions/questions.test.js` to cover all three scales.

- [x] **67** — Seeker transit mode: `RULES.md` requires End Game to trigger only when seekers are "off transit". Add `onTransit` boolean to player state in `GameStateManager` (default `false`). Accept a `set_transit` WS message (`{ type: 'set_transit', gameId, playerId, onTransit: true|false }`) in `WsHandler`; update state and broadcast `player_transit` to all players in the game. Modify `checkCapture` to exclude seekers with `onTransit === true` from the capture check (same as null-location exclusion). Frontend `GameMap`: add a "On Transit / Off Transit" toggle button visible to seekers during seeking phase; send `set_transit` on toggle; render an icon on each seeker's map marker indicating transit status. Tests: `server/wsHandler.test.js` (set_transit message updates state + broadcasts), `server/captureDetector.test.js` (on-transit seekers excluded from capture check), `src/components/GameMap.test.jsx` (toggle button + marker icon). When a hider plays a `powerup` card (`{ action: 'false_zone' }`), the managed server generates a decoy zone near one of the game's existing zones and broadcasts it as a `false_zone` WS event to all players. Seekers see the decoy zone on the map as a real zone; the hider sees it labeled as their own decoy. The decoy expires after 5 minutes and the server broadcasts `false_zone_expired`. Changes needed: (1) `functions/cards.js`: `activateFalseZone` fires `/internal/notify` with `{ type: 'false_zone', gameId }`. (2) `server/index.js`: handle `false_zone` in `/internal/notify` — pick a random zone from `gameStateManager.getGameZones(gameId)`, offset its lat/lon by a random 0.5–2 km, set a 5-minute expiry in `_falseZones` Map, broadcast `{ type: 'false_zone', gameId, zone }`. A seeking-phase StateDispatcher task expires stale false zones and broadcasts `false_zone_expired`. (3) Frontend `GameMap`: handle `false_zone` by adding the decoy to a `falseZones` state array (rendered as dashed circle); handle `false_zone_expired` by removing it. Tests for all three layers.

### Phase 21 — Rules Accuracy (continued)

- [x] **68** — Scale-aware phase durations: `RULES.md` specifies hiding/seeking durations per game scale (small: 30 min, medium: 60 min, large: 180 min), but `GameLoopManager` uses server-wide constants (`hidingDuration` / `seekingDuration` defaulting to 2 min / 10 min) for all games. Additionally there is no mechanism to start a game's loop from the serverless layer. Changes needed: (1) `GameLoopManager.startGame(gameId, { hidingDurationMs, seekingDurationMs } = {})` — store per-game durations in the `_games` entry; `_tick` uses `entry.hidingDurationMs ?? this.hidingDuration` (and seeking equivalent); add `getGameDuration(gameId, phase)` public method returning the effective duration. (2) `server/index.js` — add `SCALE_DURATIONS` constant (`{ small: 30*60_000, medium: 60*60_000, large: 180*60_000 }`); add `POST /internal/games/:gameId/start` HTTP endpoint accepting `{ scale }` in the JSON body that calls `startGame(gameId, scaleOpts)` then `beginHiding(gameId)` and responds 204; update `buildTimerSync` to call `gameLoopManager.getGameDuration(gameId, phase)` instead of the server-level `hidingDuration`/`seekingDuration` variables. (3) Tests: `server/gameLoopManager.test.js` — `startGame` with per-game overrides uses those durations in `_tick`; `getGameDuration` returns override or fallback; (4) `server/server.test.js` — `POST /internal/games/:gameId/start` with `scale: 'small'` starts loop with 30 min durations; unknown scale falls back to constructor default; missing body uses constructor default.

- [x] **69** — Wire game start from serverless: when a game host clicks "Start Game" in the frontend, notify the managed server to begin the hiding phase. Add `POST /api/games/:gameId/start` serverless endpoint; handler calls `POST /internal/games/:gameId/start` on the managed server (fire-and-forget, same pattern as cards notify). Update `WaitingRoom`'s "Start Game" button to call `POST /api/games/:gameId/start` instead of directly sending `begin_hiding` WS. Add tests in `functions/games.test.js` and `src/components/WaitingRoom.test.jsx`.

- [x] **70** — Two-phase End Game capture: `RULES.md` requires seekers to physically *spot* the hider (within GPS practical range, ~30 m) after entering the hiding zone. Currently `checkCapture` finishes the game the moment all seekers are inside the zone radius. Add a `spot_hider` WS message that a seeker sends when they believe they can see the hider. Server records the spotting seeker; only triggers `finishGame` if the spotter is within a configurable `spotRadiusM` (default 30 m) of the hider's last reported location. Add `spotRadiusM` option to `createServer`. Tests: `server/wsHandler.test.js` (spot_hider message), `server/captureDetector.test.js` (spot within/outside radius), `src/components/GameMap.test.jsx` ("I See the Hider!" button in seeking phase).

### Phase 22 — Rules Accuracy (Privacy)

- [x] **72** — Hider location privacy: `RULES.md` §Hiding Rules states seekers see only "possible hider zones" — not the hider's live GPS coordinates. Currently `_handleLocationUpdate` in `WsHandler` broadcasts the hider's `player_location` event to all players in the game, leaking the hider's position to seekers. Fix: when the player sending a `location_update` is the hider (role check via `GameStateManager.getPlayerRole`), only echo the event back to the hider themselves (not to other players). Seekers continue to receive their own location broadcasts. The server still stores the hider's location in `GameStateManager` for capture detection. Changes needed: (1) `server/wsHandler.js` — in `_handleLocationUpdate`, after updating GSM, check `getPlayerRole(gameId, playerId)`. If `'hider'`, send `player_location` only to that player's own WebSocket (using `this._send`). If seeker (or role unknown), use the existing team-broadcast or full-game broadcast. (2) `server/wsHandler.test.js` — add tests: hider location_update stores in GSM but does NOT broadcast to other players; seeker location_update still broadcasts to game; hider location echoed only to hider's own socket. Tests: `server/wsHandler.test.js` (3 new tests).

### Phase 24 — Game Configuration

- [x] **74** — Configurable hiding duration within scale range: `RULES.md` specifies hiding duration *ranges* per scale (small: 30–60 min, medium: 60–180 min, large: 180+ min) but Task 68 uses fixed minimums. Allow the game host to specify the hiding duration (in minutes) within the valid range for the chosen scale when starting the game. Changes needed: (1) `server/index.js` — add `SCALE_DURATION_RANGES` constant (`{ small: { min: 30, max: 60 }, medium: { min: 60, max: 180 }, large: { min: 180, max: 360 } }`); update `POST /internal/games/:gameId/start` to accept optional `hidingDurationMs` and `seekingDurationMs` in the body (validated against scale bounds, returning 400 if out of range); when provided these override the scale defaults. (2) `functions/games.js` — `handleStartGame` accepts optional `hidingDurationMin` from the request body; validates it falls within the scale's range (400 if out of bounds); passes `hidingDurationMs` to `notifyGameStart`. (3) `src/api.js` — `startGame` accepts optional `hidingDurationMin` param and passes it in the body. (4) `src/components/WaitingRoom.jsx` — add a number input for hiding duration (label "Hiding duration (min)", min/max constrained to scale range, default = scale minimum) visible only when `onStart` is provided; pass the value to `startGame`. Tests: `server/server.test.js` (custom duration accepted; out-of-range returns 400; unknown scale falls back to defaults), `functions/games.test.js` (handleStartGame passes hidingDurationMs; out-of-range returns 400), `src/components/WaitingRoom.test.jsx` (duration input renders with correct range; correct value passed to startGame).

### Phase 25 — Hider UX

- [x] **75** — Hider journey trail: during the hiding phase the hider's map should show a polyline trace of their own route so they can answer transit and measuring questions accurately without relying on memory. Changes needed: (1) `src/components/GameMap.jsx` — add a `locationTrail` state array (each entry `{ lat, lon }`); accumulate entries from `player_location` WS events when `role === 'hider'`; cap the array at `MAX_TRAIL_POINTS = 500` (drop oldest) to bound memory; render a `<Polyline>` via `react-leaflet` with the trail only when `role === 'hider'`; clear the trail on `phase_change` → `hiding` (fresh start). (2) `src/components/GameMap.test.jsx` — tests: trail accumulates on consecutive `player_location` events for hider; cap of 500 is enforced (501st entry drops oldest); `Polyline` is rendered in the hider's view; `Polyline` is NOT rendered for seekers; trail resets on `hiding` phase change.

### Phase 26 — Rules Compliance

- [x] **76** — Single hider enforcement: `RULES.md` states "One Hider" but nothing prevents two players from joining the same game as hider, corrupting capture detection, location privacy, and End Game logic. Changes needed: (1) `server/gameState.js` — add `getHiderId(gameId)` returning the playerId of the current hider (or `null`); in `addPlayerToGame`, when `role === 'hider'` and a *different* player is already registered as hider, throw `new Error('HIDER_SLOT_TAKEN')` (same player rejoining as hider is idempotent and allowed). (2) `server/wsHandler.js` — in `_handleJoinGame`, wrap the `gameStateManager.addPlayerToGame` call in try/catch; on `HIDER_SLOT_TAKEN` undo the `gameClients`/`playerGames` additions and send `{ type: 'error', code: 'HIDER_SLOT_TAKEN', message: 'A hider has already joined this game' }` to the client. (3) `db/schema.sql` — add idempotent partial unique index `CREATE UNIQUE INDEX IF NOT EXISTS game_players_one_hider ON game_players (game_id) WHERE role = 'hider'` so the DB enforces the constraint independently of server logic. (4) `src/components/GameMap.jsx` — add `joinError` state; handle `msg.type === 'error'` in `handleMessage` by setting `joinError` to `msg.message`; render an error banner below the reconnecting banner so players see why they cannot join. Tests: `server/gameState.test.js` (second hider throws; same player re-join is idempotent; seekers unaffected); `server/wsHandler.test.js` (second hider join sends error, not joined_game; gameClients not polluted); `src/components/GameMap.test.jsx` (error WS message renders banner).

### Phase 49 — Bug Fixes

- [x] **141** — Game creation is failing in production. Investigate and fix. Symptoms: `POST /api/games` returns an error (exact status/body unknown — needs reproduction). Likely causes to check in order: (1) `db/gameStore.js` `dbCreateGame` — verify the `games` table schema matches the columns sent in the INSERT (check `size`, `bounds`, `seeker_teams`, `status` columns match `db/schema.sql`); (2) `functions/games.js` `createGame` handler — verify it reads `size`, `bounds`, `seekerTeams` from `req.body` correctly and passes them to `dbCreateGame`; confirm it returns a 201 with `{ gameId, status }` rather than crashing before the response; (3) `api/[...path].js` catch-all — confirm it correctly parses JSON bodies for POST requests (`req.body` populated) and does not swallow errors; (4) `src/api.js` `createGame` — confirm it POSTs to `/api/games` with the correct JSON shape and propagates error bodies. Add a reproduction test in `functions/games.test.js` that exercises the full in-process `createGame` path with a mock pool and asserts a 201 response. Fix whatever is broken; do not mark complete until `npm test` passes and the root cause is documented in the commit message.

### Phase 50 — Game Creation Location Picker

- [x] **142** — Geocoding search in GameForm: replace the raw "Map Bounds" fieldset with a location search input that uses the Nominatim OSM API. Changes needed: (1) `src/components/GameForm.jsx` — add `locationQuery` state (string) and `locationResults` state (array); add a text `<input>` labelled "Search for a city, town or country…" with a 500 ms debounce; on debounce fire call `fetchNominatim(q)` which fetches `https://nominatim.openstreetmap.org/search?q=<encodeURIComponent(q)>&format=json&limit=5&addressdetails=0`; render results as a `<ul role="listbox">` below the input (show `display_name`, truncated to 60 chars); on result select: set `center` state `{ lat: parseFloat(r.lat), lon: parseFloat(r.lon) }`, compute `radiusKm` from scale default (`small→5`, `medium→15`, `large→50`), recompute `bounds` from `centerRadiusToBounds(center, radiusKm)`, close dropdown; on scale change recompute `radiusKm` to new default and recompute bounds; (2) Add pure helper `centerRadiusToBounds({ lat, lon }, radiusKm)` → `{ lat_min, lat_max, lon_min, lon_max }` (lat delta = radiusKm / 111, lon delta = radiusKm / (111 * cos(lat * π/180))); (3) Retain the four lat/lon fields in a `<details><summary>Advanced</summary>` collapsible section — they remain editable and auto-populate from map changes; (4) Keep the existing form submit path (`bounds` → `createGame`) unchanged — no backend changes. Tests: `src/components/Lobby.test.jsx` (or a new `GameForm.test.jsx`) — (a) search input renders with correct label; (b) typing triggers debounced Nominatim fetch (mock `fetch`); (c) result dropdown appears with `display_name` text; (d) selecting a result populates bounds fields; (e) changing scale after a result is selected updates radiusKm and recomputes bounds; (f) `centerRadiusToBounds` unit test (lat/lon delta within 1% of expected for a known city).

- [x] **143** — Preview map in GameForm: after a location is selected (Task 142), show a Leaflet preview map inside the Create Game form. Changes needed: (1) `src/components/GameForm.jsx` — when `center` state is set (non-null), render a `<div className={styles.previewMap}>` containing a `react-leaflet` `<MapContainer>` (CartoDB dark tiles, same as `GameMap`); map height 260 px mobile / 320 px at ≥ 600 px; `scrollWheelZoom={false}` to prevent accidental scroll-hijack on mobile; (2) Add a `<LocationCircle center={center} radiusKm={radiusKm} />` child component (internal to GameForm file) that renders a `<Circle>` at `center`, radius = `radiusKm * 1000` m, fill `rgba(240,135,48,0.15)`, stroke colour `#F08730` (--color-sunset-2); updates when either prop changes using a `useEffect`; (3) Add a draggable `<Marker>` at `center` with a custom small accent-coloured icon; on `dragend` update `center` state to new marker LatLng, recompute `bounds`; (4) `src/components/GameForm.module.css` — add `.previewMap` (`height: 260px; border-radius: 6px; overflow: hidden; margin: 0.75rem 0`) and `@media (min-width: 600px) { .previewMap { height: 320px } }`; (5) Map must not render before `center` is set to avoid a blank grey box before search. Tests: `src/components/Lobby.test.jsx` — (a) map container absent before location is selected; (b) map container present after selecting a result (check `role="region"` or a `data-testid`); (c) dragging centre marker updates bounds fields (simulate `dragend` on the mock marker with new LatLng).

- [x] **144** — Draggable zone radius resize: allow the host to adjust the zone radius on the preview map by dragging a resize handle. Changes needed: (1) `src/components/GameForm.jsx` — add a second `<Marker>` (the "resize handle") positioned at `{ lat: center.lat, lon: center.lon + lonDelta(radiusKm, center.lat) }` where `lonDelta` converts km to degrees longitude; icon: a small circle SVG (`--color-accent`, 12 px diameter) with a crosshair cursor; `draggable={true}`; on `drag` (continuous) compute new `radiusKm = haversineKm(center, handleLatLng)` (pure function, no imports — implement inline using the Haversine formula); call `setRadiusKm` and `setBounds(centerRadiusToBounds(center, newRadius))`; on `dragend` finalise; handle snaps back to the east edge of the circle after any map re-render; (2) Add a read-only (or editable) `<output>` element beneath the map showing "Zone radius: X.X km" that updates as the handle is dragged — gives numeric feedback without requiring the Advanced section to be open; (3) Keep the resize handle out of tab order (use `tabIndex={-1}` on the marker container) — keyboard users can use the Advanced lat/lon fields instead; (4) `centerRadiusToBounds` and `haversineKm` must be pure functions with no side effects — extract to `src/components/gameUtils.js` alongside `formatCountdown` so they can be unit-tested independently. Tests: `src/components/gameUtils.test.js` (or extend existing) — (a) `haversineKm` returns ~0 for identical points; (b) `haversineKm` returns ~111 km for 1° latitude difference at equator; (c) `centerRadiusToBounds` round-trips: `haversineKm(center, { lat: bounds.lat_max, lon: center.lon })` ≈ `radiusKm` (within 1%); `src/components/Lobby.test.jsx` — (d) resize handle marker present after location selected; (e) simulating `drag` event on handle with a new LatLng updates the "Zone radius:" display; (f) `bounds` fields update after handle `dragend`.

### Phase 51 — Game Creation UX Correctness

- [x] **145** — Manual bounds fields do not sync the preview map: `setBound` in `GameForm.jsx` updates the `bounds` state but never updates `center` or `radiusKm`, so editing any lat/lon field in the "Advanced" collapsible section leaves the preview circle and radius display stale. `DESIGN.md §22` specifies: "Manual edits to any field update the map circle accordingly (centre = midpoint of bounds, radius = half the shorter dimension)." Changes needed: (1) `src/components/GameForm.jsx` — update `setBound(key, value)` to compute `next = { ...bounds, [key]: value }`, parse all four fields as floats, and — if all four are valid finite numbers and `lat_max > lat_min` and `lon_max > lon_min` — compute `newCenter = { lat: (lat_min + lat_max) / 2, lon: (lon_min + lon_max) / 2 }`, `latRadiusKm = haversineKm(newCenter, { lat: lat_max, lon: newCenter.lon })`, `lonRadiusKm = haversineKm(newCenter, { lat: newCenter.lat, lon: lon_max })`, then call `setCenter(newCenter)` and `setRadiusKm(Math.min(latRadiusKm, lonRadiusKm))`; always call `setBounds(next)`. (2) `src/components/GameForm.test.jsx` — add tests: (a) filling all four Advanced fields with valid values causes the preview map to appear (center becomes non-null); (b) filling only some fields (incomplete bounds) does NOT render the preview map; (c) filling all four valid fields updates the "Zone radius:" output to a non-zero value matching the half-shorter-dimension distance.

### Phase 52 — Performance / Mobile

- [x] **146** — Lazy-load non-critical views: RALPH.md §Performance / Mobile mandates "Lazy-load non-critical views. Components like `Leaderboard` and `CardPanel` that are not needed on initial load should use `React.lazy` + `Suspense`." Currently `Lobby.jsx` statically imports `Leaderboard` and `AdminDashboard`; `GameMap.jsx` statically imports `CardPanel` and `ResultsScreen`. These four components are conditionally rendered (never shown on initial load) yet are bundled into the main JS chunk, increasing cold-start parse cost on mobile. Changes needed: (1) `src/components/Lobby.jsx` — replace `import Leaderboard from './Leaderboard.jsx'` and `import AdminDashboard from './AdminDashboard.jsx'` with `const Leaderboard = lazy(() => import('./Leaderboard.jsx'))` and `const AdminDashboard = lazy(() => import('./AdminDashboard.jsx'))`; add `lazy, Suspense` to the React import; wrap each conditional render site in `<Suspense fallback={null}>`. (2) `src/components/GameMap.jsx` — replace `import CardPanel from './CardPanel.jsx'` and `import ResultsScreen from './ResultsScreen.jsx'` with lazy equivalents; add `lazy, Suspense` to the React import; wrap each conditional render in `<Suspense fallback={null}>`. (3) Tests: update `Lobby.test.jsx` admin-shows test to use `findByLabelText` (async) instead of `getByLabelText` since the component now loads asynchronously; update `GameMap.test.jsx` results-overlay test to use `findByRole` instead of `getByRole`; all other tests should continue passing because they already use `waitFor` or `findBy*`. (4) Build: the Vite build should now emit separate chunks for each lazy component — verify by checking that `dist/` contains more than one JS file after the build.

### Phase 53 — Mobile UX

- [x] **147** — Copy invite link to clipboard in WaitingRoom: on mobile, copying the invite URL from an `<a>` tag requires a long-press-then-copy flow that is tedious and error-prone. The WaitingRoom shows the full invite URL as a clickable link, but there is no one-tap copy action. Add a "Copy Link" button next to the invite link that uses `navigator.clipboard.writeText` to copy the URL to the clipboard and briefly shows "Copied!" to confirm. Changes needed: (1) `src/components/WaitingRoom.jsx` — add `CLIPBOARD_RESET_MS = 2_000` constant; add `copied` state (boolean, default `false`) and `copyTimerRef` ref; add `handleCopyLink` async function that calls `navigator.clipboard.writeText(inviteUrl)`, on success sets `copied = true` and schedules `setCopied(false)` after `CLIPBOARD_RESET_MS` via `copyTimerRef`; add a cleanup `useEffect` that returns `() => clearTimeout(copyTimerRef.current)` to prevent state updates after unmount; render a `<button>` alongside the invite `<a>` with `aria-label="Copy invite link"` and label text `copied ? 'Copied!' : 'Copy Link'`; the `<a>` is retained so desktop users can right-click/open and keyboard users can follow the link. (2) `src/components/WaitingRoom.module.css` — add `.inviteRow` (`display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap`); add `.copyBtn` (`background: none; border: 1px solid var(--color-border); border-radius: 4px; padding: 0.25rem 0.6rem; cursor: pointer; font-size: 0.875rem; color: var(--color-text-secondary); min-height: 44px; min-width: 44px`); `.copyBtn:focus-visible` (`outline: 2px solid var(--color-accent)`). (3) `src/components/WaitingRoom.test.jsx` — add tests: (a) "Copy Link" button is present; (b) clicking it calls `navigator.clipboard.writeText` with a URL containing the gameId; (c) after click, button label changes to "Copied!"; (d) after `CLIPBOARD_RESET_MS` ms, label reverts to "Copy Link" (use `vi.useFakeTimers`); (e) timer is cleared on unmount (spy on `clearTimeout`).

### Phase 23 — UX Correctness

- [x] **148** — GPS errors silently suppressed in GameMap: `navigator.geolocation.getCurrentPosition` error callback is empty (`() => {}`), so players with denied permissions, unavailable GPS, or unsupported devices get no feedback — they appear to be playing normally but their location is never tracked, breaking capture detection and zone enforcement. Changes needed: (1) `src/components/GameMap.jsx` — add `gpsError` state (null | string); replace the empty error callback with a handler: code 1 (PERMISSION_DENIED) → set `gpsError` to "Location access denied — enable location in your browser settings to continue playing."; code 2 (POSITION_UNAVAILABLE) → set `gpsError` to "Location unavailable — your device cannot determine its position."; code 3 (TIMEOUT) → no banner (transient; next tick retries); on a successful position fix, if `gpsError` is non-null, call `setGpsError(null)` to clear the banner; when `navigator.geolocation` is absent (early return on line 472), call `setGpsError('Your device does not support location access — this game requires GPS.')` immediately. (2) `src/components/GameMap.jsx` JSX — render `gpsError` as a dismissible `role="alert"` banner using `className={styles.alertBanner}` alongside the existing banners; dismiss button calls `setGpsError(null)`. Tests: `src/components/GameMap.test.jsx` — (a) when `navigator.geolocation` is undefined, renders GPS-unsupported alert; (b) error callback with code 1 renders "Location access denied…" alert; (c) error callback with code 2 renders "Location unavailable…" alert; (d) error callback with code 3 (TIMEOUT) renders no GPS error alert; (e) successful position fix after error clears the GPS error banner.

- [x] **155** — Pre-populate hider answer from computed hint: for non-photo question categories (thermometer, tentacle, measuring, transit, matching) the server already computes the factual answer when the question is created, but the AnswerPanel textarea starts empty and the hider must manually type the answer they can already see in the hint. Changes needed: (1) `src/components/questionHints.js` — add five exported functions `thermometerAnswerText(current, previous)`, `tentacleAnswerText(withinRadius, distanceKm)`, `measuringAnswerText(hiderIsCloser, hiderDistanceKm, seekerDistanceKm)`, `transitAnswerText(nearestStationName, nearestStationDistanceKm)`, `matchingAnswerText(featuresMatch, featureType, hiderFeatureName, seekerFeatureName)` — each returns a natural-language answer string (without the "X hint: " prefix) when data is available, or an empty string when data is null/unavailable (so the hider must type manually); (2) `src/components/AnswerPanel.jsx` — import the five new functions; in the `useEffect` that calls `listQuestions`, after `setQuestions(data.questions ?? [])`, derive initial answer text for each pending question using the appropriate `*AnswerText` function based on `q.category` and `q.*` computed fields; call `setAnswers(prev => ({ ...initialAnswers, ...prev }))` to pre-fill without overwriting any text the hider has already typed; (3) `src/components/QA.test.jsx` — add tests: (a) thermometer question with computed data pre-fills textarea with answer text; (b) tentacle question with computed data pre-fills textarea; (c) measuring question with computed data pre-fills textarea; (d) transit question with computed data pre-fills textarea; (e) matching question with computed data pre-fills textarea; (f) question with null computed data leaves textarea empty; (g) pre-fill does not overwrite existing answer text when refreshTrigger fires (functional `setAnswers` merge preserves user edits); (h) photo question is never pre-filled.

- [x] **159** — Pending-question countdown and submit-lock in QuestionPanel: when a seeker has a pending question (status = 'pending') the submit form remains enabled and they must attempt submission and receive a 409 error to discover they are blocked. `RULES.md` §Question Timing says "Ask a question only after the previous question is answered." The seeker also has no visibility into how much time the hider has left to answer. Changes needed: (1) `src/components/QuestionPanel.jsx` — after computing `allQuestions`, derive `pendingQuestion = allQuestions.find(q => q.status === 'pending') ?? null`; add `PENDING_SUBMIT_LABEL` constant (`'Waiting for hider…'`) and `PENDING_BANNER_LABEL` constant (`'Waiting for hider to answer'`); disable the submit button when `pendingQuestion != null` (in addition to `submitting` and `isCurseActive`); when `pendingQuestion` is set and has `expiresAt`, show a `<p role="status">` banner above the form using `className={styles.pendingBanner}` with text `"Waiting for hider to answer — {formatCountdown(pendingQuestion.expiresAt)} remaining"` — the existing 1-second tick already causes re-renders so the countdown stays live without extra work. (2) `src/components/QuestionPanel.module.css` — add `.pendingBanner` using an `--color-accent` left border to distinguish it from the red curse banner: `background: rgba(240,135,48,0.10); border-left: 3px solid var(--color-accent); color: var(--color-text-primary); border-radius: 0 4px 4px 0; padding: 0.25rem 0.5rem;`. (3) `src/components/QA.test.jsx` — add tests: (a) when `listQuestions` returns a pending question, submit button is disabled; (b) pending-question banner renders with "Waiting for hider to answer" text and a time string when `expiresAt` is in the future; (c) when history contains only answered/expired questions, submit button is not blocked by pending logic (enabled in normal conditions); (d) pending banner is absent when there is no pending question.

- [x] **73** — End Game countdown timer sync: when End Game begins, the server broadcasts `end_game_started` but no `timer_sync`, so neither hider nor seekers see the 10-minute End Game timeout countdown. `buildTimerSync` only handles `'hiding'` and `'seeking'` phases; `_endGameTimers` tracks the timer server-side but no expiry time is surfaced to clients. Changes needed: (1) `server/index.js` — add `_endGameStartedAt` Map; extend `buildTimerSync` to support `phase: 'end_game'` (returns `{ type: 'timer_sync', gameId, phase: 'end_game', phaseEndsAt }` computed from `startedAt + endGameTimeoutMs`); when `capture_check` triggers End Game, record `_endGameStartedAt` and broadcast `timer_sync` with `phase: 'end_game'` immediately after `end_game_started`; update the periodic tick to use `phase: 'end_game'` when `isEndGameActive`; clear `_endGameStartedAt` in `onPhaseChange → finished`. (2) `src/components/GameMap.jsx` — add End Game countdown case to the timer banner: when `endGameActive && phaseEndsAt`, show "You win if not spotted in X:XX" (hider) or "Find the hider in X:XX" (seeker). Tests: `server/server.test.js` (timer_sync with phase end_game broadcast alongside end_game_started; periodic tick broadcasts end_game timer_sync); `src/components/GameMap.test.jsx` (hider End Game banner; seeker End Game banner; end_game countdown takes priority over phase timer).
### Phase 55 — Zone Privacy

- [x] **163** — Zone privacy: `zone_locked` must not reveal the hider's zone to seekers; seekers see the zone only when End Game starts. Currently `gameZone.js` sends `POST /internal/notify` with `{ type: 'zone_locked', gameId, zone, lockedBy }`, which `server/index.js` relays via `wsHandler.broadcastToGame` to ALL players — seekers immediately see the exact zone on their map, negating all question-based deduction (`DESIGN.md §6` "seekers see possible hider zones only"; `RULES.md §Hiding Rules` rule 2). Changes needed: (1) `server/wsHandler.js` — add `sendToPlayer(gameId, playerId, message)` method: `const ws = this.gameClients.get(gameId)?.get(playerId); if (ws) this._send(ws, message);`. (2) `server/index.js` — in the `/internal/notify` else branch, handle `zone_locked` specially: instead of `wsHandler.broadcastToGame`, call `wsHandler.sendToPlayer(gameId, rest.lockedBy, { type: 'zone_locked', ...rest })` when `rest.lockedBy` is a non-empty string; when `lockedBy` is missing/null, silently skip (do not broadcast). (3) `server/index.js` — include the hider's zone in the `end_game_started` broadcast: `const endZone = gameStateManager.getGameZones(gameId)[0] ?? null;` then `wsHandler.broadcastToGame(gameId, { type: 'end_game_started', gameId, zone: endZone })` — seekers need the zone to navigate during End Game. (4) `src/components/GameMap.jsx` — in the `end_game_started` handler (currently just sets `endGameActive`), add `if (msg.zone) setLockedZone(msg.zone)` so seekers see the zone circle from End Game start onwards. Tests: `server/server.test.js` — (a) `POST /internal/notify` with `{ type: 'zone_locked', gameId, lockedBy: hiderPlayerId, zone }` sends the event only to the hider's WebSocket (not to a seeker's WebSocket); (b) when `lockedBy` is absent from a `zone_locked` notify, no WebSocket message is sent; (c) `end_game_started` broadcast includes a `zone` field matching `gameStateManager.getGameZones`. `src/components/GameMap.test.jsx` — (d) `end_game_started` WS event with `zone` field causes the zone circle to appear on the map (seeker sees zone at End Game start); (e) `zone_locked` WS event (hider path) still updates `lockedZone` for the hider as before.

### Phase 54 — End Game Rules Compliance

- [x] **161** — Spot-hider gate: require End Game active and spotter off transit: `RULES.md §End Game` states spotting only occurs after seekers enter the hider's zone AND are off transit. Currently `checkSpot` in `server/captureDetector.js` only checks proximity — it does not verify the spotter is off transit (`spotter.onTransit`). `_handleSpotHider` in `server/wsHandler.js` does not verify End Game is active before accepting a spot claim. The "I See the Hider!" button in `GameMap.jsx` renders throughout the entire seeking phase (not just End Game), including when the seeker is on transit. Changes needed: (1) `server/captureDetector.js` `checkSpot` — add transit check: if `spotter.onTransit === true`, return `{ spotted: false, distance: null, reason: 'on_transit' }`; extend the function's return type with an optional `reason` string field so callers can distinguish proximity rejection from transit rejection. (2) `server/wsHandler.js` `_handleSpotHider` — add an End Game pre-check before calling `checkSpot`: if `this.gameStateManager && !this.gameStateManager.isEndGameActive(gameId)`, send `{ type: 'spot_rejected', gameId, spotterId: playerId, reason: 'end_game_not_started', distanceM: null, spotRadiusM: this.spotRadiusM }` and return early; pass the `reason` from `checkSpot` through to `spot_rejected` messages so the frontend can show specific error text. (3) `src/components/GameMap.jsx` — add `SPOT_ON_TRANSIT_LABEL` constant (`'Board off transit to spot'`); change the spot button render condition from `player.role === 'seeker' && phase === 'seeking'` to `player.role === 'seeker' && endGameActive`; add `|| myOnTransit` to the button's `disabled` condition; update the button label expression to include `myOnTransit ? SPOT_ON_TRANSIT_LABEL : ...` so the seeker understands why the button is disabled; hide the `spot-rejected-msg` banner similarly when `!endGameActive`. (4) Tests: `server/captureDetector.test.js` — (a) `checkSpot` returns `{ spotted: false, reason: 'on_transit' }` when spotter has `onTransit: true` and is within radius; (b) spotter off transit within radius still returns `{ spotted: true }`; `server/wsHandler.test.js` — (c) `spot_hider` when `isEndGameActive` returns false sends `spot_rejected` with `reason: 'end_game_not_started'`; (d) `spot_hider` when End Game active but spotter on transit sends `spot_rejected` with `reason: 'on_transit'`; (e) End Game active + off transit + within radius sends `spot_confirmed` as before; `src/components/GameMap.test.jsx` — (f) spot button absent when `endGameActive` is false (phase seeking, end game not started); (g) spot button present when `endGameActive` is true and `myOnTransit` is false; (h) spot button disabled with "Board off transit to spot" label when `endGameActive` is true and `myOnTransit` is true.

### Phase 56 — Zone UX

- [x] **167** — Hider in-zone status indicator during seeking phase: real-time "✓ In zone" / "⚠ Outside zone" banner derived from the local GPS fix. See Current Task for full spec.

### Phase 57 — Reconnect Correctness

- [x] **166** — Zone circle absent after page refresh/reconnect: `syncedZones` state in `GameMap.jsx` is set from `game_state_sync` but never used for rendering — the zone circle is rendered from `lockedZone` state which is only set by `zone_locked` and `end_game_started` WS events. If a player refreshes their browser during seeking or End Game, `lockedZone` remains `null` and the zone circle does not appear. For the hider this means their own zone circle disappears after refresh; for a seeker who reconnects during End Game, the revealed zone is not shown on their map. Changes needed: (1) `src/components/GameMap.jsx` — in the `game_state_sync` handler, after processing phase/endGameActive, restore `lockedZone` from the sync: `const syncZone = Array.isArray(msg.zones) && msg.zones.length > 0 ? msg.zones[0] : null`; call `setLockedZone(syncZone)` when `player.role === 'hider'` and `syncZone` is non-null; call `setLockedZone(syncZone)` when `msg.endGameActive` is true and `syncZone` is non-null (seeker sees zone at End Game); remove the unused `syncedZones` state (`useState` + `setSyncedZones` call) entirely — it is set but never read. (2) `src/components/GameMap.test.jsx` — update the existing `game_state_sync updates locked-zone state without throwing` test (line ~1413) to assert the zone circle is actually rendered (not just that the component doesn't throw); add 3 new tests: (a) hider receives `game_state_sync` with `zones: [zone]` → `L.circle` is called with the zone's lat/lon; (b) seeker receives `game_state_sync` with `endGameActive: true` and `zones: [zone]` → `L.circle` is called with the zone's lat/lon (seeker sees zone at End Game); (c) seeker receives `game_state_sync` with `endGameActive: false` and `zones: [zone]` → zone circle is NOT rendered (zone privacy preserved).

### Phase 58 — Card UX

- [x] **168** — Card-drawn toast notification for hider: when the hider answers a question a card is drawn (fire-and-forget via `functions/questions.js`) and a `card_drawn` WS event arrives at `GameMap.jsx`. Currently the only effect is `setCardRefresh((n) => n + 1)`, which silently re-fetches the CardPanel hand. The hider has no immediate visual feedback telling them what card they just received. Changes needed: (1) `src/components/gameUtils.js` — add two exported frozen-object constants: `CARD_LABELS` (`{ time_bonus: 'Time Bonus', powerup: 'Power-Up', curse: 'Curse' }`) and `CARD_DESCRIPTIONS` (`{ time_bonus: '+10 min hiding time', powerup: 'Create a false zone', curse: 'Block seeker questions for 2 min' }`). (2) `src/components/CardPanel.jsx` — import `CARD_LABELS` and `CARD_DESCRIPTIONS` from `./gameUtils.js` instead of re-defining them locally (removes duplication per RALPH no-shortcut rules). (3) `src/components/GameMap.jsx` — add `CARD_TOAST_DURATION_MS = 4_000` constant; import `CARD_LABELS`, `CARD_DESCRIPTIONS` from `./gameUtils.js`; add `drawnCardToast` state (`null | { type, label, description }`); in the `card_drawn` WS handler (after `setCardRefresh`) call `setDrawnCardToast({ type: msg.cardType, label: CARD_LABELS[msg.cardType] ?? msg.cardType, description: CARD_DESCRIPTIONS[msg.cardType] ?? '' })`; add a `useEffect` that watches `drawnCardToast` — when non-null, sets a `setTimeout` of `CARD_TOAST_DURATION_MS` to call `setDrawnCardToast(null)` and returns the `clearTimeout` as cleanup; render a `<p role="status" data-testid="card-drawn-toast">New card: {label} — {description}</p>` banner using `className={styles.infoBanner}` when `drawnCardToast !== null` and `player.role === 'hider'`. Tests: `src/components/GameMap.test.jsx` — (a) `card_drawn` event for hider shows toast with card label; (b) `card_drawn` event for hider shows card description in toast; (c) toast is not shown to a seeker when another player's `card_drawn` event arrives; (d) toast auto-dismisses after `CARD_TOAST_DURATION_MS` (use fake timers).

- [x] **164** — ZoneSelector station search filter: large or medium games can have hundreds of transit stations, making the ZoneSelector list very difficult to scroll on mobile. The hider must manually find their station by scrolling, which is slow and error-prone for large games (`DESIGN.md §6` "Hiding zones around transit stations"; `RULES.md §Hiding Rules` rule 2). Changes needed: (1) `src/components/ZoneSelector.jsx` — add `SEARCH_PLACEHOLDER` constant (`'Search stations…'`); add `searchQuery` state (`''`); compute `filteredZones = searchQuery.trim() ? zones.filter(z => (z.name ?? '').toLowerCase().includes(searchQuery.trim().toLowerCase())) : zones`; render a `<label>` with an `<input type="search">` above the zone list, using `className={styles.searchInput}`, value bound to `searchQuery`, `onChange` updating `searchQuery`; render `filteredZones` instead of `zones`; when `filteredZones.length === 0` and `searchQuery.trim()` is non-empty show `<p>No stations match your search.</p>`; hide the search input when `locked` (the read-only confirmation view). (2) `src/components/ZoneSelector.module.css` — add `.searchInput` rule: `width: 100%; margin-bottom: 0.5rem;` (global input styles from `index.css` apply the rest). (3) `src/components/ZoneSelector.test.jsx` — add tests: (a) search input is rendered with correct label; (b) typing "Central" into the search filters the list to show only matching stations, hiding non-matching ones; (c) typing a query that matches nothing shows "No stations match your search." message; (d) clearing the search query restores the full station list; (e) search input is absent after zone is locked.

### Phase 60 — Investigation Book

- [x] **170** — Seeker deductions notepad (Investigation Book): `RULES.md §Players` states "Seekers get the Investigation Book" — a place to record deductions based on question answers. The current `QuestionPanel` shows question history but provides no dedicated scratchpad for coordinating conclusions. Seekers need to correlate thermometer readings, tentacle radii, and matching features to triangulate the hider's position; a persistent notepad reduces cognitive load without requiring server support. Changes needed: (1) `src/components/SeekerNotes.jsx` — export default `SeekerNotes({ gameId })` with `NOTES_STORAGE_KEY_PREFIX = 'jetlag_notes_'` and `NOTES_DEBOUNCE_MS = 500` constants; `notes` state initialised from `localStorage.getItem(NOTES_STORAGE_KEY_PREFIX + gameId) ?? ''`; `saveTimerRef = useRef(null)`; `handleChange(e)` — calls `setNotes(e.target.value)`, clears any pending save timer, schedules a new `setTimeout` of `NOTES_DEBOUNCE_MS` to `localStorage.setItem(NOTES_STORAGE_KEY_PREFIX + gameId, e.target.value)`; `useEffect` returns cleanup `() => clearTimeout(saveTimerRef.current)`; render a `<section>` with `aria-label="Investigation Notes"` containing a `<label htmlFor="seeker-notes">Investigation Notes</label>` and `<textarea id="seeker-notes" value={notes} onChange={handleChange} placeholder="Write your deductions here…" className={styles.notesArea} />`. (2) `src/components/SeekerNotes.module.css` — `.notesSection` (`margin-top: 0.75rem`); `.notesArea` (`width: 100%; min-height: 120px; resize: vertical; font-family: 'Inter', sans-serif; font-size: 0.875rem; padding: 0.5rem; box-sizing: border-box; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 4px; color: var(--color-text-primary)`) plus focus ring `2px solid var(--color-accent)`. (3) `src/components/GameMap.jsx` — lazy-load `SeekerNotes` (`const SeekerNotes = lazy(() => import('./SeekerNotes.jsx'))`); render `<Suspense fallback={null}><SeekerNotes gameId={game.gameId} /></Suspense>` immediately after the existing `<QuestionPanel …>` (inside the `player.role === 'seeker'` guard — visible throughout the seeking phase). Tests: `src/components/SeekerNotes.test.jsx` — (a) renders with label "Investigation Notes"; (b) textarea has placeholder "Write your deductions here…"; (c) existing localStorage value is loaded as initial content; (d) typing in textarea triggers `localStorage.setItem` after `NOTES_DEBOUNCE_MS` ms (use fake timers); (e) save is debounced — rapid successive keystrokes produce only one `localStorage.setItem` call; (f) on unmount, pending debounce timer is cleared so no `localStorage.setItem` fires after unmount; `src/components/GameMap.test.jsx` — (g) `SeekerNotes` is rendered when `player.role === 'seeker'`; (h) `SeekerNotes` is not rendered when `player.role === 'hider'`.

### Phase 61 — Two-Team Results Attribution

- [x] **171** — ResultsScreen missing captureTeam attribution in two-team mode: when `seeker_teams = 2` and seekers win, the `capture` WebSocket event includes `captureTeam` ('A' or 'B') and `GameMap.jsx` already uses it to show a toast ("Seekers win! (Team A)"), but `captureTeam` is never stored in `gameResult` or passed to `ResultsScreen`, so the full-screen result overlay always shows "Seekers Win!" without indicating which team won. Changes needed: (1) `src/components/GameMap.jsx` — add `captureTeamRef = useRef(null)` alongside `captureWinnerRef`; in the `capture` WS handler store `captureTeamRef.current = msg.captureTeam ?? null`; in the `phase_change → finished` handler include `captureTeam: captureTeamRef.current` in the `setGameResult({…})` call; pass `captureTeam={gameResult.captureTeam}` to `<ResultsScreen>`. (2) `src/components/ResultsScreen.jsx` — add `captureTeam` prop (optional, string 'A'|'B'|null); update JSDoc; when `!isHiderWin && captureTeam` render "Seekers Win! (Team A)" instead of "Seekers Win!". Tests: (a) `src/components/ResultsScreen.test.jsx` — seekers win without captureTeam renders "Seekers Win!"; seekers win with captureTeam='A' renders "Seekers Win! (Team A)"; seekers win with captureTeam='B' renders "Seekers Win! (Team B)"; hider win with captureTeam set still renders "Hider Wins!" (captureTeam ignored for hider wins). (b) `src/components/GameMap.test.jsx` — `capture` event with `captureTeam: 'A'` followed by `phase_change finished` event passes `captureTeam='A'` to `ResultsScreen`; `capture` event without `captureTeam` followed by `phase_change finished` passes `captureTeam` as null/undefined to `ResultsScreen`.

### Phase 59 — Transit Coverage

- [x] **169** — Expand Overpass transit query to include ferry terminals and tram stops: `RULES.md §Game Scales` line 43 states "Include transit systems: trains, buses, ferries, trams". The `buildOverpassQuery` function in `functions/zones.js` fetched four node types but was missing `amenity=ferry_terminal` (ferries) and `railway=tram_stop` (trams). Added both clauses; updated JSDoc; added 3 new tests verifying query content and node inclusion.
